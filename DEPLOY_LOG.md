# Deploy Log
| Date/Time | Commit | Branch | Summary | Actions |
|-----------|--------|--------|---------|---------|
| 2026-06-13 20:43 UTC | 0b1a37f | master | CLI-cron guard for bokun_sync + deploy skill, log, docs | FAIL — "Deploy Backend" step: scp got empty SSH_PORT (SSH_HOST/SSH_USERNAME/SSH_PORT secrets unset in repo). Build passed; git push to master OK. Prod still serving prior code (site 200, tours API 401=auth-enforced). |
| 2026-06-13 22:52 | 0b1a37f (deploy.sh) | master | Task 1 live-refresh + bokun_sync CLI guard shipped via deploy.sh | manual deploy |
| 2026-06-15 13:03 UTC | 7dcd037 (deploy.sh) | master | Guide Reports — read-only per-guide monthly tour verification (new guide-tour-report.php endpoint + GuideReports page, PDF/CSV export) | manual deploy. Backup withlocals_backup_20260615_130253. report endpoint 401 (auth-enforced), frontend 200, live bundle index-BsGhMmGE.js. |
| 2026-06-15 13:22 UTC | 6c01e84 (deploy.sh) | master | Guide Reports category breakdown (Combo/Uffizi/Pitti/Accademia/Other) in header chips, Type column, PDF + CSV exports | manual deploy. Backup withlocals_backup_20260615_132157. report endpoint 401 (auth-enforced), frontend 200, live bundle index-CPjo5Fg4.js. |
| 2026-06-15 13:58 UTC | fcb8ab5 (deploy.sh) | master | Guide Reports all-guides overview: per-guide category breakdown columns (Guide \| Combo \| Uffizi \| Pitti \| Accademia \| Other? \| Total), PDF + CSV export updated | manual deploy. Backup withlocals_backup_20260615_135816. report endpoint 401 (auth-enforced), frontend 200, live bundle index-BxHXbhGg.js. |
| 2026-06-15 14:28 UTC | 295201f (deploy.sh) | master | Phase 1 guide-availability: Dashboard "needs a guide in next 7 days" alert + double-booking guard on guide assignment (tours.php PUT returns 409 guide_double_booked unless force=true; frontend confirm+force retry). No payment logic touched. | manual deploy. Backup withlocals_backup_20260615_142817. frontend 200, tours API 401 (auth-enforced), live bundle index-DJQ3d4tb.js. |
| 2026-06-15 14:43 UTC | e04c63b (deploy.sh) | master | Dashboard needs-guide rows deep-link to /tours?date=YYYY-MM-DD; Tours.jsx seeds single-date mode from ?date= param (timezone-safe parse, no double fetch) | manual deploy. Backup withlocals_backup_20260615_144340. frontend 200, live bundle index-BM1ubEz7.js. |
| 2026-06-15 15:04 UTC | 3a2ee3d (deploy.sh) | master | Phase 2 backend: guide-requests.php — availability_requests table (self-provision) + public token endpoints (accept/decline, double-booking guard, no customer data) + owner endpoints (create/list/recent/cancel). No payment logic touched. | manual deploy (backend only). Backup withlocals_backup_20260615_150408. owner mode 401 (auth), bogus token 404 (public branch reachable). |
| 2026-06-15 15:12 UTC | 0677ef5 (deploy.sh) | master | Phase 2 frontend: public guide Accept/Decline page at /respond/:token (Italian, mobile-first, standalone — no sidebar/login; plain fetch, no auth interceptor). Route added in App.jsx alongside /login. | manual deploy. Backup withlocals_backup_20260615_151258. /respond/bogus 200 (SPA serves invalid-link state), live bundle index-Zxv6Kwbt.js. |
| 2026-06-15 15:24 UTC | 400f61f (deploy.sh) | master | Fix Vite base from './' to '/' so directly-loaded deep routes (e.g. /respond/:token) load assets from /assets/... instead of /respond/assets/... (was rendering blank). | manual deploy. Backup withlocals_backup_20260615_152432. dist + live index.html now reference /assets/... (leading slash) at both root and deep route. Live bundle index-BMEGC2be.js. |
| 2026-06-15 15:34 UTC | 9749a57 (deploy.sh) | master | Phase 2 Ask-a-guide flow on Tours page: "Ask" button on unassigned tours opens language-matched guide picker (speakers first), creates availability request via guide-requests.php, pre-fills Italian WhatsApp message + wa.me link (copy-link fallback), shows "Richiesto a X — in attesa" note. Frontend only. | manual deploy. Backup withlocals_backup_20260615_153457. frontend 200, live bundle index-CZ5O85Ni.js. |
| 2026-06-15 16:00 UTC | 38ec772 (deploy.sh) | master | Phase 2 3b: extracted reusable AskGuideModal component (used on Tours + Dashboard); Dashboard "needs a guide" rows get Ask button + new "Recent guide responses" panel (?action=recent); Tours persistent request badges (pending=amber, declined=grey) from new ?action=open endpoint. No payment logic. | manual deploy. Backup withlocals_backup_20260615_160022. frontend 200, guide-requests ?action=open 401 unauth, live bundle index-B_3gR_c9.js. |
| 2026-06-15 16:28 UTC | d1d2fb5 (deploy.sh) | master | HOTFIX: /tours TDZ crash ("Cannot access 'Pt' before initialization") from Step 3b — moved `loadOpenRequests` useCallback (+ openAskGuide/closeAskGuide/handleGuideRequested) to be declared BEFORE the mount useEffect whose dependency array references it (render-time eval). No logic change, no payment logic. | manual deploy. Backup withlocals_backup_20260615_162821. frontend 200, live bundle index-BxhrultK.js. |
| 2026-06-15 20:49 UTC | 91d0e45 (deploy.sh) | master | Fix: hide "Ask a guide" button + request-status badges on cancelled tours (gate guide-cell extras on `!tour.guide_id && !tour.cancelled`). TourCardMobile has no Ask button — no change needed. Frontend only, no payment logic. Gated by build + 56/56 tests. | manual deploy. Backup withlocals_backup_20260615_204912. frontend 200, live bundle index-IWdfTa4D.js. |
| 2026-06-15 22:08 UTC | 7ecf852 (deploy.sh) | master | Sync reliability Step A: shrink regular Bokun sync window DEFAULT_SYNC_DAYS 120→60 (bokun_sync.php) so each auto/cron sync stays under the 2000-result page cap and completes (was 28+ min, never finished under rate limits). No payment logic. | manual deploy. Backup withlocals_backup_20260615_220817. Post-deploy timed cron: EXIT 0, ~60s, 600 bookings synced, 0 failed (window 06-08..08-14). |
| 2026-06-15 22:16 UTC | 0f5350f (deploy.sh) | master | Fix latent parse error in bokun_cron.php: the crontab example `*/15` in the docblock contained the block-comment terminator, closing the `/** */` early → file unparseable (EXIT 255) since commit de0b869. Rewrote schedule as 0,15,30,45. CLI cron now parses/runs. No payment logic. | manual deploy. Backup withlocals_backup_20260615_221552. server php -l clean; cron runs to completion. |
| 2026-06-15 22:30 UTC | 8e344aa (deploy.sh) | master | Fix Bokun sync pagination drop (BokunAPI.php getBookings): paginate by PAGE FULLNESS (`hasMore = count(items)===pageSize`) instead of `((page+1)*pageSize) < totalHits`. Bokun's totalHits under-reports (787 vs real 987), so the old check stopped after page 3 and silently dropped page 4+ bookings (incl. cancelled/rescheduled). No payment logic. | manual deploy. Backup withlocals_backup_20260615_223041. Verified: timed cron EXIT 0 ~148s, synced_count 787 (was 600), failed 0. Stale rows corrected: 1007 old Renee → cancelled=1, 1005 Tony time → 14:15, both last_sync=now. |
| 2026-06-22 16:00 UTC | b6bcedc (deploy.sh) | master | Webhook step zero: bokun_webhook.php now self-provisions bokun_webhook_logs (CREATE TABLE IF NOT EXISTS, same pattern tours.php uses for products) — the table never existed in prod so every webhook 500'd at the logging step before any handler ran. logWebhook wrapped in try/catch (non-fatal); topic handler catches Throwable and returns HTTP 200 (error recorded) so Bokun does not retry-storm. Handlers unchanged. No payment logic. | manual deploy. build + 56/56 tests passed, php -l clean. Verified: POST X-Bokun-Topic bookings/test -> HTTP 200 (was 500); table auto-created; test row captured (id 1 raw {"hello":"world"}, id 2 annotated "Unknown webhook topic"). frontend 200, live bundle index-IWdfTa4D.js. |
| 2026-06-22 18:42 UTC | 124d419 (deploy.sh) | master | Real-time Bokun webhook: replaced dead header-based topic switch (Bokun sends empty X-Bokun-* headers) with body-driven targeted sync. Extracts affected date(s) from activityBookings (startDateTime epoch ms -> Europe/Rome; fallback date/dateString) and calls syncBookings(D,D,"webhook",bookingId) per day through the proven path (handles create/update/reschedule/cancel/product-reg/auto-group). Fallback today..+2d if no date. bokun_sync.php guard extended with !defined("BOKUN_SYNC_LIB") so it can be included as a library; direct HTTP still auth-gated. Keeps step-zero capture, non-fatal, always 200, marks log processed. No payment logic. | manual deploy. Backup withlocals_backup_20260622_183603. build + 56/56 tests, php -l clean both files. Verified live: direct sync endpoint still 401; replay of captured ABNB-95083625 payload -> HTTP 200 in 6.2s, synced_dates ["2026-06-23"]; booking row 5245 date 2026-06-23 time 11:00 cancelled 0 guide_id 16 preserved, last_sync bumped; webhook log row processed=1. Note: empty-body {} hits 3-day fallback and can exceed gateway timeout (504) — real per-booking path always 1-day. Note: server log_errors=Off so error_log() lines are not persisted. |
| 2026-06-22 18:50 UTC | 683b180 (deploy.sh) | master | Webhook robustness: (1) bokun_sync.php guards top-level applyRateLimit("bokun_sync") with php_sapi_name()!=="cli" && !defined("BOKUN_SYNC_LIB") so only direct HTTP is charged — webhook include no longer double-spends rate budget / risks 429 on bursts (keeps own webhook 30/min). (2) bokun_webhook.php: dateless events now SKIP the sync (capture + 200 fast) instead of a today..+2d fallback that could 504 -> Bokun retries. Real per-booking path unchanged (1-day sync). No payment logic. | manual deploy. Backup withlocals_backup_20260622_184942. build + 56/56 tests, php -l clean both. Verified live: empty-body {} -> 200 in 0.30s (was 504), synced_dates []; direct sync still 401; replay ABNB-95083625 -> 200 in 6.4s, synced_dates ["2026-06-23"]. |
| 2026-06-22 19:11 UTC | ac4d309 (deploy.sh) | master | Fix: guide dropdowns capped at first 20 guides (new guides missing). Added getAllGuides() in mysqlDB.js — pages through guides.php (per_page=100) and concatenates all pages into a flat array. Repointed picker callers: Tours.jsx (assignment + Filter-by-Guide, incl. mobile via shared guides state), Dashboard.jsx (Ask-a-guide modal), GuideReports.jsx, EditTour.jsx (guide assignment). Smoke mocks updated to expose getAllGuides. Guides.jsx management page left paginated; PaymentRecordForm.jsx not touched (payment component). Frontend only, no payment logic. | manual deploy. Backup withlocals_backup_20260622_191023. build + 56/56 tests, frontend 200. Live bundle assets/index-Celm2Tle.js. |
| 2026-06-22 19:21 UTC | ac2a50e (deploy.sh) | master | Fix: PaymentRecordForm.jsx guide picker capped at first 20 guides (new guides missing when recording a payment). loadGuides() swapped getGuides() -> getAllGuides() (flat array, all pages). DATA SOURCE ONLY — no payment calc/grouping/amounts/logic changed. Single frontend file. | manual deploy. Backup withlocals_backup_20260622_192050. build + 56/56 tests, frontend 200. Live bundle assets/index-Celm2Tle.js. |
| 2026-06-22 19:24 UTC | c36626b (correction) | master | CORRECTION to the row above: PaymentRecordForm.jsx is DEAD CODE — nothing imports/renders it, so it is tree-shaken out of the bundle (proven: pre-change getGuides build and post-change getAllGuides build both produce byte-identical index-Celm2Tle.js). The getAllGuides swap is therefore a no-op at runtime (harmless, kept for consistency if the file is ever revived). The REAL payment-recording form is Payments.jsx, whose loadGuides() already fetches guides.php?per_page=100 (lists all guides up to 100) — it does NOT have the 20-cap bug. No live bug existed; no functional change shipped. |
| 2026-06-25 13:39 UTC | 2e39bb4 (deploy.sh) | master | Remove dead PaymentRecordForm.jsx (749 lines). Confirmed zero importers via `grep -rn PaymentRecordForm src/` (only the file's own def/export matched). It was already established as tree-shaken dead code (see c36626b row); the live payment form is Payments.jsx. No payment logic touched — pure dead-file deletion. | manual deploy. Backup withlocals_backup_20260625_133958. build + 56/56 tests passed (incl. smoke tests). Verified live: frontend 200, tours.php 401 (auth-enforced), live bundle index-BupYPAVT.js (unchanged hash — file was already tree-shaken out, confirming it contributed nothing to the bundle). |
| 2026-06-25 14:15 UTC | (toast+401, deploy.sh) | master | Visible toast notifications + graceful 401 session-expiry handling. NEW: src/components/Toast/ToastProvider.jsx (custom, no new dep — fixed portal container, top-right desktop / top-center near-full mobile, z-9999, auto-dismiss 5s/err 7s + manual X, Tuscan styling) with provider-safe useToast(); src/services/sessionExpiry.js (notifySessionExpired → clears token + dispatches window 'app:session-expired', skips when already on /login, dedupes bursts); src/services/authFetch.js (shared Bearer wrapper, fires session-expiry on 401). WIRED: App.jsx wraps app in <ToastProvider> inside Router + a <SessionExpiryListener> that toasts "Your session has expired…" and navigates to /login. mysqlDB.js axios response interceptor → notifySessionExpired on 401. MIGRATED easily-missed top banners → toasts on Tours/Guides/Dashboard/GuideReports (setError/setSuccess rerouted via thin wrappers, banner JSX removed) + Payments (showNotification → toast, notification banner removed; full-page load-error state kept). Dashboard/Payments/ticketsService now use the shared authFetch. Inline field validation untouched. NO payment calc/grouping/amounts changed — message display + session handling only; payments.php untouched. | manual deploy. build + 56/56 tests passed (smoke tests wrapped in <ToastProvider>; the existing axios 401 unit test still green). Verified live: frontend 200, tours.php 401 (auth-enforced — now surfaces a toast + /login redirect client-side instead of a raw "status code 401"), live bundle index-B1VrIYp0.js. 401→login bridge: interceptor/authFetch can't use hooks, so they dispatch a window event that the React <SessionExpiryListener> turns into a toast + navigate. |
| 2026-06-28 02:06 UTC | 30c12d1 (deploy.sh) | master | Guide WhatsApp reminder backend — Twilio scheduled "1 hour before tour" reminder to the assigned guide, reconciled on every sync + on guide assignment, FLAG-GATED OFF. NEW api/twilio_reminders.php (no top-level side effects, safe to include): guideReminderConfig / normalizeWhatsapp (digits→whatsapp:+E.164, null if unusable, no country-code guessing) / twScheduleReminder (POST Messages.json, MessagingServiceSid + ContentSid HX04be4d… + ContentVariables {1:guideFirst,2:title,3:HH:MM}, ScheduleType=fixed, SendAt=start−LEAD_MIN UTC) / twCancelReminder / reconcileGuideReminders. Self-provisions guide_reminders table (CREATE TABLE IF NOT EXISTS: tour_id UNIQUE, guide_id, twilio_sid, send_at_utc, status enum scheduled/canceled/sent/failed, last_error). Reconcile: schedules within-window assigned tours, reschedules on tour-move/guide-change (book new then cancel old), cancels when cancelled/unassigned/past/outside the 7-day window; excludes ticket products via the same NOT EXISTS products lookup; start times computed in Europe/Rome. HOOKS: bokun_sync.php syncBookings (after auto-group, before return) + tours.php PUT (after a guide-assignment update; no payment fields), each wrapped in try/catch so a reminder error can NEVER break sync or assignment. Gated by TWILIO_REMINDERS_ENABLED (default false) — reconcile returns immediately when off: zero Twilio calls, zero DB writes (verified with null $conn). Auth token read from env, never logged. No payment logic. | manual deploy. Backup withlocals_backup_20260628_020549. php -l clean local+server on twilio_reminders/bokun_sync/tours; npm run build OK; 56/56 tests pass. Verified live: frontend 200, tours.php 401 (auth-enforced), live bundle index-B1VrIYp0.js (unchanged — backend-only change). Server .env TWILIO_REMINDERS_ENABLED ABSENT (OFF) — NOT enabled yet (template guide_tour_reminder HX04be4d… pending Meta/WhatsApp approval). |
| 2026-06-28 21:16 UTC | 195ddc7 (deploy.sh) | master | Guide phone international-format validation (Add/Edit Guide form). NEW isValidGuidePhone() in Guides.jsx: strips spaces/dashes/dots/parens then requires /^(\+|00)\d{8,15}$/ (must have "+" or "00" country-code prefix + 8-15 digits; empty=invalid). Inline terracotta warning under the phone input when non-empty+invalid ("Use international format with country code — e.g. +39 333 123 4567. Without it, the WhatsApp tour reminder cannot be sent.") + required message on submit when empty; handleSubmit now blocks save on invalid phone (keeps form open, fires useToast error). Accepts spaced/dashed intl numbers and leading 00; blocks bare local numbers (e.g. 3392863290) and emails — the exact bad data that made Twilio reminders fail. Prevents future reminder failures at entry. Frontend only, no payment logic. | manual deploy. Backup withlocals_backup_20260628_211644. build + 56/56 tests passed. Verified live: frontend 200, tours/guides 401 (auth-enforced), live bundle index-BAJBYkeK.js; confirmed new validation string present in the deployed bundle. |
| 2026-06-28 23:49 UTC | 9a1de4e (deploy.sh) | master | Tours grouping UX fixes (frontend only, no payment logic). (1) Cancelled bookings no longer counted toward group PAX, the bookings-count badge, or the FULL badge — new shared src/utils/tourCapacity.js (countActivePax/countActiveBookings compute from group.tours excluding cancelled); applied in TourGroup.jsx, TourGroupCardMobile.jsx, and the date/period/mobile-period/summary PAX totals in Tours.jsx (ungrouped cancelled tours also excluded from PAX sums). Cancelled bookings still shown struck-through in the expanded list. (2) Capacity-aware FULL + merge guard via getMaxPax(title): uffizi (incl. uffizi+accademia combo)=9, accademia/david=19, else 9. FULL badge now fires at >= getMaxPax(display_name) instead of hard-coded 9 (TourGroup + mobile, which now also shows X/maxPax). Manual-merge validation (drag-drop + mobile select) replaced totalPax>9 with min(getMaxPax) across merged tours (excludes cancelled) and the error states the actual max. (3) Light-green background (bg-green-50, hover deeper) for tours/groups that HAVE a guide and are not cancelled — desktop rows, group container, mobile TourCardMobile + TourGroupCardMobile. Priority: cancelled red > assigned green > default; drag-over/selection styles still override. | manual deploy. Backup withlocals_backup_20260628_234907. npm run build OK; 56/56 tests pass (Tours smoke incl.). Verified live: frontend 200, tours/guides 401 (auth-enforced), live bundle index--9Mf7fe-.js; confirmed bg-green-50 + new merge message present in the deployed bundle. 3 Jul Uffizi&Accademia 09:30 group (Jennifer 8 active + Connie 1 cancelled) now counts 8 PAX, not FULL (max 9). |
| 2026-06-29 00:36 UTC | 172ca45 (deploy.sh) | master | Product-aware grouping + private-tour flag (no payment logic). NEW api/tour_classification.php (single source of truth, no DB/side-effects): isPrivateBooking(productId,rateId,rateTitle) [FULLY_PRIVATE_PRODUCTS 11 ids; MIXED_PRIVATE_RATES 962885=>2266785, 1130528=>2244449; rateId from productBookings[0].fields.rateId, fallback rateTitle contains 'private'], getMaxPaxForTitle (uffizi 9 / accademia|david 19 / else 9), bokunRateInfo. SCHEMA: tours.is_private TINYINT(1) NOT NULL DEFAULT 0 + idx, self-provisioned in bokun_sync (ensureIsPrivateColumn) and tours.php. SYNC: bokun_sync sets is_private on every insert/update via helper. GROUPING (autoGroupAfterSync) reworked: key = product_id|date|HH:MM (was title); EXCLUDES cancelled + is_private + null-product from auto groups; rebuild detaches auto-group tours in range (manual merges untouched) then regroups; display_name = most-frequent title (tie->most PAX) so same-product/diff-title now merge; per-product capacity via getMaxPaxForTitle stored on tour_groups.max_pax; cancelled/private get group_id cleared. FRONTEND: purple Private badge (bg-purple-100) on Tours.jsx rows + TourCardMobile (+ defensive in group expanded rows); is_private returned via t.* . Backfill (temp CLI, deleted): scanned 5409, flagged 109 private; regroup 213 groups/532 tours. | manual deploy. Backup withlocals_backup_20260629_003424. php -l clean (3 files); npm build OK; 56/56 tests. Verified live: frontend 200, tours/guides 401, bundle index-8OJ-Lj-u.js (Private pill present), tour_classification.php uploaded. Verify queries: (a) 962885 2026-07-09 09:30 two titles share ONE group_id 1245; (b) is_private=1 count=109; (c) 962885 'Private Tour - 4 Hours' rows is_private=1 group_id NULL (rateId 2266785). |
| 2026-06-29 00:52 UTC | 4c74cdf (deploy.sh) | master | Tours date filter: added '◀ Prev month' / 'Next month ▶' buttons (single-date branch only) that jump to the 1st of the prev/next month via date-fns startOfMonth(subMonths/addMonths(filterDate||today,1)) and reset showUpcoming/showPast/showDateRange so the list filters to that date immediately (switches out of Upcoming/Past/Range mode). Subtle 'MMMM yyyy' label between them. Reused Today/Upcoming period-button styling (min-h-[44px], touch-friendly, flex-wrap). Frontend only (src/pages/Tours.jsx), no payment logic. | manual deploy. Backup withlocals_backup_20260629_005233. npm build OK; 56/56 tests pass. Verified live: frontend 200, bundle index-RXvgzW3E.js ('Prev month' present). |
| 2026-06-29 01:07 UTC | 2cbf75d (deploy.sh) | master | Themed in-app calendar for the Tours date filter. NEW src/components/DateFilter.jsx (themed, terracotta/stone/renaissance/rounded-tuscan): button trigger (calendar icon + 'EEE, d MMM yyyy', 'Pick a date' when in Upcoming/Past/Range), custom popover calendar (date-fns startOfMonth/endOfMonth/startOfWeek/endOfWeek/eachDayOfInterval; selected=terracotta filled, today=inset terracotta ring, out-of-month muted, hover stone, cells >=32px), outside-click + Escape close, month chevrons jump to the 1st (startOfMonth(addMonths/subMonths(filterDate||today,1)), popover stays open), plus a segmented Today/Upcoming/Past 40 Days/Date Range control and the restyled range inputs. Tours.jsx swapped the native input + prev/next row + loose buttons for <DateFilter/> (all existing state/handlers reused: filterDate/showUpcoming/showPast/showDateRange/range*). Frontend only, no payment logic. | manual deploy. Backup withlocals_backup_20260629_010746. npm build OK; 59/59 tests (added DateFilter.test.jsx, 3 tests; Tours smoke still green). Verified live: frontend 200, tours/guides 401, bundle index-CpG5SX8R.js ('Pick a date' present). |
| 2026-06-30 15:06 UTC | dd3d8d3 (deploy.sh) | master | Fix guide WhatsApp reminders never delivered (no payment logic). ROOT CAUSE: reconcileGuideReminders() used the 15-min Twilio floor (minSendAt=now+15min) both to gate CREATION and, wrongly, RETENTION — a tour inside the floor dropped out of $keptTourIds so the removal pass cancelled its already-booked Twilio scheduled message ~12 min before SendAt (confirmed cancelled_at = SendAt−11..−13 min; Twilio date_sent=null). FIX 1: split WANTED (valid phone + future start within 7-day window) from CREATABLE (sendAt>=now+15min); WANTED alone decides retention so imminent reminders are never cancelled; extracted pure unit-tested reminderPlan() (create/recreate/reschedule/cancel_stale/keep/leave/skip/none). FIX 2: one reminder per DEPARTURE not per booking — candidate query collapses each group to its lowest active tour id (dedup) so grouped guides get ONE WhatsApp. FIX 3: tour-groups.php updateGroup reconciles after a group-level guide change (exception-isolated, flag-gated); backfilled tours.guide_id on 13 current/future group-guided tours. Files: api/twilio_reminders.php, api/tour-groups.php. | manual deploy. Backup by deploy.sh. php -l clean (both); npm run build OK; 59/59 tests; reminderPlan logic test 11/11. Verified live: frontend 200, tours/guides 401, bundle index-CpG5SX8R.js (backend-only). Post-deploy reconcile: matched 19 (retained), canceled 9 (duplicates removed), failed 0; scheduled 28→19, canceled 8→17; dedup 0 groups with >1 scheduled; 0 wanted+creatable departures missing a reminder; Twilio cross-check 3 SIDs status=scheduled. Auth token never printed. |
| 2026-07-02 23:52 UTC | 3944129 (deploy.sh) | master | Exclude cancelled tours from tour COUNTS + category breakdown in Summary (frontend only, no payment logic). FIX 1: tour counts previously included cancelled tours while PAX excluded them ("12 tours · 56 PAX" inconsistent). New shared computeItemStats(items) in Tours.jsx returns {activeTours (groups + standalone non-cancelled), cancelledCount (standalone cancelled), pax (unchanged, cancelled-excluded)}; new CountLabel renders "{activeTours} tours · {pax} PAX" + muted text-stone-400 "· {N} cancelled" only when N>0. Applied to day header, desktop period sub-header, mobile period divider, and totalData/Summary total. FIX 2: NEW tourCategory(title) in src/utils/tourCapacity.js (mirrors backend classifyTourCategory: uffizi; accademia incl. david; pitti incl. boboli/palatina/palatine; 2+ = Combo; else Other). categorySummary memo buckets each ACTIVE departure once over the filtered groupedTours (group -> display_name shared; standalone -> is_private splits into Private Uffizi/Accademia/(other)); Summary card renders a 4-col/2-col grid of tiles in fixed order [Combo,Uffizi,Accademia,Pitti,Private Uffizi,Private Accademia,Private (other),Other] hiding 0-buckets, Private tiles tinted purple. Added "{N} tours still need a guide" line next to Unassigned Report button (unchanged). | manual deploy. Backup by deploy.sh. npm run build OK; 66/66 tests pass (added tourCapacity.test.js, 7 tourCategory cases incl. david/combo). Verified live: frontend 200, tours API 401 (auth-enforced), live bundle index-DdMQvzuV.js matches fresh build. |
| 2026-07-03 00:05 UTC | 9c4ce56 (deploy.sh) | master | Refine Summary category breakdown: private buckets now mirror shared categories fully + fix needs-a-guide grammar (frontend only, no payment logic). CHANGE 1: a private departure previously bucketed only into Private Uffizi/Private Accademia/Private (other), so a private COMBO landed wrongly in Private (other). Now key = cat==='Other' ? 'Private (other)' : `Private ${cat}` — private mirrors shared exactly (Private Combo, Private Uffizi, Private Accademia, Private Pitti, Private (other)). CATEGORY_ORDER + PRIVATE_CATEGORIES updated to: Combo, Uffizi, Accademia, Pitti, Other, Private Combo, Private Uffizi, Private Accademia, Private Pitti, Private (other); still hides 0-buckets; private tiles stay purple, shared neutral. CHANGE 2: needs-a-guide line grammar — N===1 -> "1 tour still needs a guide"; else "{N} tours still need a guide" (still only shown when N>0). Bucket mapping lives in Tours.jsx categorySummary (not tourCategory), so no test change. | manual deploy. Backup by deploy.sh. npm run build OK; 66/66 tests pass. Verified live: frontend 200, tours API 401 (auth-enforced), live bundle index-GQOd0n4i.js matches fresh build. |
| 2026-07-03 18:14 UTC | 0ea0853 (deploy.sh) | master | Show adults/children/infants breakdown in Tours PAX view (frontend only, no payment/schema/backend). NEW helpers in src/utils/tourCapacity.js: getPaxBreakdown(tour) -> {adults,children,infants,total} parses bokun_data productBookings[0].fields.priceCategoryBookings, SUMS el.quantity (default 1) grouped by pricingCategory.ticketCategory (ADULT/CHILD/INFANT; absent enum falls back to title/bookedTitle keyword infant/child), robust fallback to totalParticipants||participants when missing/empty/unparseable; aggregateBreakdown(tours) sums getPaxBreakdown excluding cancelled (parallels countActivePax); formatBreakdown({adults,children,infants}) spelled-out non-zero parts with singular/plural (e.g. '5 adults, 1 child'), returns '' when all-adults. DISPLAY (muted text-stone-500, shown ONLY when children>0||infants>0 via formatBreakdown truthiness): standalone rows Tours.jsx People col + TourCardMobile append ' ({breakdown})'; group headers TourGroup.jsx + TourGroupCardMobile append ' · {aggregate breakdown}'; expanded booking rows (TourGroup + mobile group card) append ' ({breakdown})' after the per-booking qty. Numeric PAX/FULL logic unchanged. | manual deploy. Backup by deploy.sh. npm run build OK; 82/82 tests pass (added getPaxBreakdown/aggregateBreakdown/formatBreakdown cases: quantity:N, one-per-participant, mixed adult+child+infant, title-keyword fallback, missing/empty/malformed bokun_data). Verified live: frontend 200, tours API 401 (auth-enforced), live bundle index-FUJ-wTMP.js matches fresh build. |
| 2026-07-03 18:58 UTC | 0a637a9 (deploy.sh) | master | Fix adults/children/infants breakdown missing on GROUPED rows + compact Booking Details modal (frontend + read-only GET backend; NO payment logic). ROOT CAUSE: group member rows come from tour-groups.php getGroupTours() which SELECTs a fixed column list WITHOUT bokun_data, so frontend getPaxBreakdown() had no priceCategoryBookings and hit its all-adults fallback (standalone rows use tours.php SELECT t.* which includes bokun_data, so they worked). FIX (server-computed, never depends on per-row bokun_data): NEW pure helper computePaxBreakdown($bokunData,$fallbackParticipants) in api/tour_classification.php (sums productBookings[0].fields.priceCategoryBookings[].quantity default 1 by pricingCategory.ticketCategory ADULT/CHILD/INFANT, title-keyword fallback, else totalParticipants||participants as all-adults; mirrors the JS rule; no DB/side-effects). tours.php GET adds pax_adults/pax_children/pax_infants to every returned tour; tour-groups.php getGroupTours() now selects t.bokun_data, computes the same 3 fields per member, then unsets bokun_data (payload stays small). Frontend src/utils/tourCapacity.js getPaxBreakdown() PREFERS tour.pax_adults/pax_children/pax_infants when present, else the old bokun_data parse, else participants — so standalone rows, grouped member rows, group headers and mobile all agree. PART B: BookingDetailsModal.jsx compacted — smaller header band (px/py, title text-2xl->text-lg), section spacing space-y-6->4 + mb-3->2, card p-4->p-3, h3 text-lg->base, section icons text-xl->lg, and the 3 oversized text-2xl Adults/Children/Total cards condensed into a compact inline chip row; ALL fields kept (modal still ignores infants in its Total, by design). | manual deploy. Backup by deploy.sh. php -l clean (tour_classification/tours/tour-groups); npm run build OK; 86/86 tests pass (added server pax_* preference cases). Verified live: frontend 200, tours/tour-groups 401 unauth, live bundle index-IU1Q8AUm.js matches fresh build; authed GET confirms tours.php + tour-groups.php members return pax_* (grouped members show e.g. 2 adults/2 children, 4 adults/1 infant). |
| 2026-07-06 00:09 UTC | 7dea5a7 (deploy.sh) | master | Fix: unassigning a guide failed with "Failed to assign guide: No fields to update" (NO payment logic). ROOT CAUSE: frontend sends guide_id:null to unassign (TourGroup.jsx/TourGroupCardMobile.jsx send selectedGuideId||null; JSON null), but PHP isset() returns FALSE for a key whose value is null — so tour-groups.php updateGroup() and tours.php PUT skipped the guide_id field entirely, $setFields stayed empty, and the request 400'd with "No fields to update". Assigning worked because a real id passes isset(). FIX: isset() -> is_array($data) && array_key_exists() at all guide_id detection points ("field omitted" = don't touch; "field present but null/empty" = SET guide_id = NULL): tours.php PUT SET-clause + reminder-reconcile trigger; tour-groups.php updateGroup SET-clause + propagateGuideToTours (NULL now propagates to member tours) + reminder-reconcile trigger — so reconcileGuideReminders($conn) (exception-isolated) now also runs on unassign and cancels the guide's scheduled WhatsApp reminder. Double-booking guard untouched (only fires for non-null/non-empty guide, so unassign is never blocked). Frontend: Tours.jsx saveGuideAssignment sends explicit guide_id:null (was '') + toast now says "Guide unassigned successfully!"; TourGroup.jsx/TourGroupCardMobile.jsx toast "Guide unassigned from group" (was Guide "None" assigned). Files: api/tours.php, api/tour-groups.php, Tours.jsx, TourGroup.jsx, TourGroupCardMobile.jsx. | manual deploy. Backup by deploy.sh. php -l clean (both PHP); condition reproduce-check: old isset=false vs new=true for {"guide_id":null}, omitted field still ignored; npm run build OK; 86/86 tests pass. Verified live: frontend 200, tours + tour-groups APIs 401 (auth-enforced), live bundle index-B4wKhuUR.js matches fresh build, deployed tours.php/tour-groups.php contain the array_key_exists fix (grep on server: 4/5 hits). |
| 2026-07-06 00:20 UTC | a926acf (deploy.sh) | master | Two frontend-only changes (NO backend, NO payment logic). CHANGE 1 — DateFilter.jsx segmented control is now Today / Tomorrow / Upcoming / Date Range: removed the "Past 40 Days" button (showPast state/plumbing in Tours.jsx intentionally left unused; the report-label string at Tours.jsx ~1042 remains harmlessly), added "Tomorrow" (setFilterDate(startOfDay(addDays(today,1))) + clearFlags, same single-date mode as Today; active = single-date mode AND filterDate == tomorrow). Calendar trigger + popover untouched. CHANGE 2 — mixed merged groups made visible for correct guide pay: new local CategoryBadge (tourCategory(booking.title); neutral stone styling, Combo gold bg-gold-100/text-gold-700) rendered per booking in expanded group member rows — new "Type" column in TourGroup.jsx desktop table (between Customer and Channel), inline under customer name in TourGroupCardMobile.jsx; group HEADER computes distinct tourCategory set over NON-cancelled member bookings and when >1 shows a gold "Mixed" badge next to the title (title-attr tooltip with breakdown e.g. "Combo x1, Uffizi x1") on both desktop header row and mobile card title. Display only — grouping, PAX, and Summary category logic unchanged. Files: DateFilter.jsx, TourGroup.jsx, TourGroupCardMobile.jsx, DateFilter.test.jsx (new segment test). | manual deploy. Backup withlocals_backup_20260706_021739. npm run build OK; 87/87 tests pass (added DateFilter segment test: buttons present, Past 40 Days absent, Tomorrow picks tomorrow at midnight + clears flags). Verified live: frontend 200, live bundle index-C1W-VNVp.js matches fresh build; bundle contains "Tomorrow" (segment) + "Mixed" (badge), no Past-40-Days button (only the unused report label string). |
| 2026-07-06 00:35 UTC | 59f7747 (deploy.sh) | master | Guide Reports now flags MIXED merged groups (READ-ONLY report; NO payment calculation touched). PROBLEM: guide-tour-report.php classified each group unit by the group display_name, so a group merging different products (e.g. 2 Combo + 1 Uffizi bookings) silently counted as just "Uffizi", under-representing combo work at invoice check. BACKEND: new pure buildComposition($titles) -> [category, composition[], composition_label]; getGuideReport restructured from skip-later-members (continue) to two-pass accumulate-then-emit: standalone rows emit directly, group member titles accumulate per group_id, then ONE representative row per group classified by MEMBERS (1 distinct category -> that category; >1 -> "Mixed") keeping the group's canonical date/time/display_name; each tours[] row gains composition (ordered [{category,count}]) + composition_label ("Combo ×2, Uffizi ×1"; '' for non-mixed); summary_by_category adds "Mixed" (fixed order Combo, Uffizi, Pitti, Accademia, Other, Mixed) — a Mixed group increments ONLY Mixed so bucket sum still equals total_tours (1 group = 1 unit unchanged). Same member-based classification applied to getAllGuidesOverview (_seen dedup replaced by per-guide _groups accumulation; by_category gains Mixed). FRONTEND GuideReports.jsx: CATEGORY_ORDER += Mixed; overviewCategoryColumns hides Other/Mixed columns when all-zero; single-guide table Type cell shows gold Mixed badge + composition_label; summary chips gain gold Mixed chip with tooltip; overview highlights non-zero Mixed cells gold; PDF + CSV exports show "Mixed (Combo ×2, Uffizi ×1)" via categoryExportLabel() and include Mixed in summaries/columns (all existing columns kept). | manual deploy. Backup withlocals_backup_20260706_023259. php -l clean; buildComposition unit-checked (4/4 incl. target "Combo ×2, Uffizi ×1"); npm run build OK; 87/87 tests pass. Verified live: frontend 200, bundle index-56Trf7O_.js matches fresh build, server file has buildComposition; authed spot-check guide 14 (Caterina) 2026-07-03: 14:15 group 73157 (2 Combo + 1 Uffizi bookings) returns ONE unit category=Mixed composition_label="Combo ×2, Uffizi ×1", summary {Uffizi:1, Accademia:1, Mixed:1} total 3; all-guides overview shows same Mixed bucket, bucket sum == total_tours. |
| 2026-07-06 08:55 UTC | 2d8bfbf (deploy.sh) | master | NEW FEATURE — admin-only Daily P&L tracker (NO payment logic touched; reads tours/groups read-only, writes ONLY to its own self-provisioned pnl_settings + pnl_tour_costs tables). BACKEND api/pnl.php: Middleware::requireRole($conn,'admin'); GET ?date=YYYY-MM-DD -> per-tour-unit rows (group g<id> / standalone t<id>, same unit key as payments) + day totals; GET ?start&end (max 92 days) -> per-day totals + grand totals + monthly overhead + profit_after_overhead; GET/POST ?action=settings (whitelisted keys: guide rates by category, museum ticket cost/person adult+child, radio/gelato per person, monthly overheads, fallback commission % per channel); POST ?action=costs upserts per-unit overrides (array_key_exists pattern — explicit null CLEARS an override). Revenue auto-extracted per booking from stored bokun_data (resellerInvoice total/totalCommission/totalSansCommission preferred, then sellerCommission+customerInvoice/totalPrice, else channel-% estimate flagged estimated:true); costs auto-computed (tickets = per-museum-in-title × adult/child PAX, guide = category rate — Mixed group pays highest member rate, radio/gelato per person; ticket products get no guide/radio cost); cancelled bookings excluded. FRONTEND src/pages/DailyPnL.jsx: Day/Month views, summary cards (net revenue, costs, profit, volume), day table with click-to-override cost + net cells (terracotta = manual, ↺ reset to auto), Rates & Costs settings modal, month table with per-day rows -> click into day + overhead footer. Route /daily-pnl (App.jsx, ProtectedRoute), sidebar item gated adminOnly via userInfo.role filter (ModernLayout.jsx), 5 service functions in mysqlDB.js (getPnlDay/getPnlRange/getPnlSettings/savePnlSettings/savePnlCosts — axios, so Bearer token via interceptor). | manual deploy. Backup by deploy.sh. php -l clean (pnl.php); npm run build OK; 87/87 tests pass. Locally verified end-to-end (php -S 8080 + vite 5173, dev DB): login dhanu -> /daily-pnl in sidebar (admin) -> 2026-05-27 renders 2 units net €407.77 matching bokun_data invoices (non-estimated); settings save guide_rate_uffizi=80 -> tour row guide cost €80/profit recomputed, ticket-product row correctly €0; cost override other_cost=15 persisted + flagged manual + totals recomputed; null cleared override (array_key_exists path); Month view 10 days + overhead footer OK; test values reset after. Verified live: frontend 200, pnl.php 401 unauth (endpoint live, admin auth enforced), live bundle index-CH1-Fp9r.js matches fresh build. |
| 2026-07-06 09:30 UTC | 20784a7 (deploy.sh) | master | UI redesign of the Daily P&L DAY view (frontend only, src/pages/DailyPnL.jsx; NO backend, NO payment logic; Month view + settings modal + API untouched). The flat wide table (EditableCell, removed) is replaced by sectioned cards: "Guided Tours" grouped by category (Combo/Uffizi/Accademia/Pitti/Other) with per-category subtotal rows, a separate "Tickets & Audio Guides" section, and a collapsed "Cancelled" list; each section header shows in/out/profit subtotals. NEW components: UnitCard (time + category/Ticket-Audio badges, title, PAX·guide·channel line, big +/− profit with in−out breakdown, chip row), EditableChip (Money in / Tickets / Guide / Radio / Gelato / Staff / Other — click to type amount, Enter commits, Esc cancels; terracotta = manual override with ↺ reset-to-auto; same savePnlCosts null-clears-override contract), SectionTotals (in €X · out €Y · ±Z). | manual deploy. Backup withlocals_backup_20260706_112815. npm run build OK; 87/87 tests pass; file complete (719 lines, ends with MonthTable closing brace, no EditableCell refs remain). Locally verified (dev DB, 2026-05-27): Guided Tours (1) with Uffizi subtotal + Tickets & Audio Guides (1) sections render; Guide chip edit €75 saved -> orange chip, section subtotal out €75.00/+€273.82 and day cards recomputed; ↺ reset returned chip to auto €0.00 neutral styling and totals to +€407.77. Verified live: frontend 200, pnl.php 401 (auth-enforced), live bundle index-Dj_eilht.js matches fresh build. |
| 2026-07-06 09:54 UTC | 7a745a6 (deploy.sh) | master | Daily P&L update (api/pnl.php + src/pages/DailyPnL.jsx; NO payment logic — writes stay confined to pnl_settings/pnl_tour_costs). (1) OUTSOURCED flag per tour unit: new pnl_tour_costs.outsourced TINYINT(1) NOT NULL DEFAULT 0, self-provisioned via SHOW COLUMNS guard (ALTER TABLE ... ADD COLUMN for installs created before the feature; CREATE TABLE includes it for fresh installs); when set, auto guide/radio/gelato = 0 and other_cost auto = new outsource_fee setting (ticket cost KEPT — you still pay the museum); accepted in POST ?action=costs whitelist, stored strictly 0/1 (never NULL, unlike money overrides); rows return outsourced:true/false. (2) UFFIZI PM PRICING: new settings ticket_uffizi_adult_pm/ticket_uffizi_child_pm used instead of the day rate when the BOOKING's own time >= '16:00' (string compare on HH:MM; per member booking, not group header time). (3) BUSINESS DEFAULTS for fresh (never-saved) setting keys: uffizi adult 29 (25+4 reservation), uffizi adult PM 20 (16+4, from-16:00 price since 1 Jan 2026), accademia adult 20 (16+4), outsource_fee 10 — DB-saved values always win. (4) UI: "Given to agency?" dashed toggle chip on each UnitCard (indigo ✓ state) + indigo "Given to agency" badge; settings modal gains the 2 Uffizi-PM fields and the agency fee field ("Per-person extras & agency fee"). | manual deploy. Backup withlocals_backup_20260706_115211. php -l clean; npm run build OK; 87/87 tests pass; both files complete (pnl.php 670 lines ends $conn->close(); DailyPnL.jsx 737 lines ends MonthTable brace). Locally verified (dev DB): outsourced column auto-added by SHOW COLUMNS guard to the pre-existing table (SHOW COLUMNS lists outsourced tinyint(1)); settings return new keys (ticket_uffizi_adult_pm 20, outsource_fee 10 defaults; DB-saved keys unchanged); 17:00 Uffizi booking t266 2 adults -> ticket_cost 40 = 2 x PM 20 while day-rate key is 0 (proves PM key selection); UI toggle ON: guided tour guide €80->€0, radio €8->€0, Other €10, indigo badge + ✓ chip, day totals €10/+€397.77; toggle OFF: back to guide €80/radio €8/no badge, totals €88/+€319.77; test rates reset after. Verified live: frontend 200, pnl.php 401 (auth-enforced), live bundle index-DsB8cq5B.js matches fresh build. |
| 2026-07-06 10:09 UTC | a14ece0 (deploy.sh) | master | Daily P&L: Borghese museum support + tour/ticket unit split (api/pnl.php + src/pages/DailyPnL.jsx; NO payment logic). (1) BORGHESE: pnlMuseumsInTitle detects 'borghese'; new category 'Borghese' in pnlCategory (single-museum branch, after Accademia; 2+ museums still = Combo); new settings ticket_borghese_adult/ticket_borghese_child, BOTH default €17 (Borghese has no child ticket — adult price applies); no Borghese guide rate — a guided Borghese tour falls to guide_rate_other by design. UI: teal Borghese badge (bg-teal-100 text-teal-800), Borghese in CATEGORY_ORDER for day-view sections, 2 new settings fields. (2) UNIT SPLIT: pnlTotals now returns tour_units + ticket_units alongside units (kept for compat); Volume card shows "N tours · M tickets · P PAX" (tickets segment hidden when 0), month-view Tours column and Month-total footer show tour_units with a muted "+Mt" ticket suffix; `?? totals.units` fallbacks keep old cached/api shapes rendering. | manual deploy. Backup withlocals_backup_20260706_120658. php -l clean; npm run build OK; 87/87 tests pass; files complete (pnl.php 677 lines ends $conn->close(); DailyPnL.jsx 749 lines ends MonthTable brace). Locally verified (dev DB, 2026-07-07): Borghese ticket t370 (1 adult) -> category "Borghese", ticket_cost 17 = 1 x €17 default, profit 21.85; UI shows teal Borghese badge + Tickets €17.00 chip; Volume card "5 tours · 9 tickets · 41 PAX" matches API totals (units 14 = tour_units 5 + ticket_units 9). Verified live: frontend 200, pnl.php 401 (auth-enforced), live bundle index-BTI_GSyp.js matches fresh build. |
| 2026-07-06 11:26 UTC | 03e3ef7 (deploy.sh) | master | Daily P&L: WEEK view + profit-by-product tiles (api/pnl.php + src/pages/DailyPnL.jsx; NO payment logic). (1) BACKEND: range response (?start&end) now also returns by_category[] — one entry per product line present in the range, fixed order Combo/Uffizi/Accademia/Pitti/Borghese/Mixed/Other/Tickets (ticket-product units bucket to "Tickets" regardless of title category), each with units/pax/net/cost/profit (rounded); cancelled-only units excluded. (2) FRONTEND: Day|Week|Month segmented toggle; new Week view = Mon–Sun range via mondayOf() ((getDay()+6)%7, Sunday belongs to the week it ends), prev/next-week arrows + "6 Jul – 12 Jul" label + "This week" button, reuses the range endpoint + MonthTable day rows; new CategoryTiles ("Profit by product") grid rendered in BOTH week and month views (badge colors incl. purple Tickets tile; units× · PAX, ±profit, in/out line); summary card label now Day/Week/Month Profit; MonthTable gains showOverhead prop — monthly-overhead footer row shown ONLY in month view (a week is not a whole month); empty-state text "No bookings in this period." loadMonth refactored into shared loadRange(start,end). | manual deploy. Backup withlocals_backup_20260706_132314. php -l clean; npm run build OK; 87/87 tests pass; files complete (pnl.php 704 lines ends $conn->close(); DailyPnL.jsx 818 lines ends MonthTable brace). Locally verified (dev DB): range API 2026-07-06..12 returns by_category [Combo 3u/5pax, Uffizi 6u/18pax, Accademia 1u, Tickets 16u/44pax cost 437] in order; Week view renders "6 Jul – 12 Jul" (today = Mon 6 Jul), WEEK PROFIT card, tiles, NO overhead row; prev arrow -> "29 Jun – 5 Jul"; This week button present; Month view (2026-07) shows tiles AND overhead footer row. Verified live: frontend 200, pnl.php 401 (auth-enforced), live bundle index-C84-02Oo.js matches fresh build. |
| 2026-07-06 12:00 UTC | 2518807 (deploy.sh) | master | Daily P&L: PRIVATE-TOUR guide rate (api/pnl.php + src/pages/DailyPnL.jsx; NO payment logic). New setting guide_rate_private, default €240 (= €60/h x 4h private; shared combo default also set to €210 = 3.5h — defaults apply only to never-saved keys). pnlBuildRows SELECT gains t.is_private; unit accumulator gains is_private (true if ANY member row has is_private=1 — privates are never auto-grouped so effectively per-tour); auto guide cost precedence now: outsourced > ticket-product > PRIVATE flat rate > Mixed highest-member-rate > category rate. Row output gains is_private. UI: purple "Private" badge (bg-purple-100 text-purple-800, same style as tourCapacity Private badge on Tours page) on UnitCard; settings group retitled "Guide pay per tour (€60/h — shared 3.5h, private 4h)" with new "Private tour (any museum, 4h)" field. | manual deploy. Backup withlocals_backup_20260706_135658. php -l clean; npm run build OK; 87/87 tests pass; files complete (pnl.php 714 lines ends $conn->close(); DailyPnL.jsx 822 lines ends MonthTable brace). Locally verified (dev DB): private t376 "David & Accademia Private Tour" (2026-07-28) -> category Accademia + is_private:true + guide_cost 240 (private rate beats the 0 Accademia rate); shared combo t389 (2026-07-26, guide_rate_combo set 210 in dev) -> Combo + is_private:false + guide_cost 210 (category rate, private rate NOT applied); UI shows purple Private badge + Guide €240.00 chip on the private card, Combo card Guide €210.00 with no Private badge; settings modal shows the new field (240). Verified live: frontend 200, pnl.php 401 (auth-enforced), live bundle index-DeFhkknL.js matches fresh build. |
| 2026-07-08 14:19 UTC | aa07b10 (deploy.sh) | master | Daily P&L: CostDetailModal (frontend only, src/pages/DailyPnL.jsx; NO backend, NO payment logic). Tap/click any UnitCard in the day view to open a mobile-first popup (bottom sheet on <sm, centered dialog on desktop; sticky header + footer): shows HOW each amount is calculated — Money in (Retail − channel commission, "~" when estimated), per-museum ticket formula ("Uffizi: 4 adult × €29", "(after 16:00)" PM variant, child line when children>0), guide-rate source (Private flat / Mixed highest-member / category rate / ticket product / given-to-agency), radio+gelato "N people × €X" — with editable inputs (inputMode=decimal) for net revenue + all cost fields, per-field "↺ Auto: €X" reset links, Given-to-agency checkbox, notes textarea, and a LIVE "in − out = profit" preview. Save sends ONE savePnlCosts call: values equal to auto send null (clears override, only if currently overridden), differing values send the number, notes trimmed (empty -> null), outsourced 0/1. Cards: onClick opens modal; chip row now hidden on mobile (hidden sm:flex) with "Tap to see & edit costs ▸" hint; EditableChip + agency button + editing chip get stopPropagation so desktop inline editing still works. | manual deploy. Backup withlocals_backup_20260708_161518. npm run build OK; 87/87 tests pass; file complete (1061 lines, ends MonthTable brace). Locally verified (dev DB, 2026-05-27): card click opens modal with retail/commission + "Uffizi tour rate from Settings" + "4 people × €0" formulas; Guide edited 75 -> live preview "€348.82 in − €75.00 out", save -> orange Guide €75.00 chip; reopen -> "Auto: €0.00" link restores + save clears override (neutral chip); note "test note from modal" persisted and re-shown on reopen; agency checkbox from modal -> badge + Other €10 on card, then toggled back off; 380px viewport: chip row hidden, tap hint visible, no horizontal scroll, modal renders as bottom sheet. Verified live: frontend 200, live bundle index-DRTWvDCE.js matches fresh build. |
| 2026-07-15 07:58 UTC | 544fd71 (deploy.sh) | master | PWA support — app is now installable on iOS/Android home screens (NO payment logic, NO backend PHP changes). New: public/manifest.webmanifest (standalone display, FwL Tours short name, Tuscan colors #C75D3A/#FAF6F0, 192/512/512-maskable icons), public/sw.js (conservative service worker: NEVER intercepts /api/ — auth/rate limits untouched; navigations network-first with cached index.html offline fallback so fresh deploys are picked up on next load; /assets/* stale-while-revalidate; CACHE_VERSION fwl-v1), public/icons/ (4 PNGs incl. apple-touch-icon). Modified: index.html (manifest link, theme-color, apple-touch-icon, iOS standalone meta, viewport-fit=cover), src/main.jsx (SW registration gated on import.meta.env.PROD — dev HMR unaffected), public/.htaccess (AddType application/manifest+json .webmanifest + Cache-Control no-cache/no-store for sw.js so SW updates are never stale). | manual deploy. Backup withlocals_backup_20260715_095455. npm run build OK; 87/87 tests pass; all files complete. GOTCHA: deploy.sh does NOT ship .htaccess (scp dist/* misses dotfiles; rsync branch excludes it explicitly) — uploaded manually via scp after diffing remote (remote was identical to repo minus new PWA block). Verified live: / 200, live bundle index-oBWcxvsz.js matches fresh build, /manifest.webmanifest 200 application/manifest+json, /sw.js 200 with Cache-Control no-cache,no-store,must-revalidate, /icons/icon-192.png 200 image/png, tours.php + guides.php 401 (auth enforced). |
| 2026-07-17 21:19 UTC | 6a130fd (deploy.sh) | master | Dashboard compaction (src/components/Dashboard.jsx only; NO payment logic, NO backend changes). Sections now collapsed by default with a per-section "Show all (N) ▾ / Show less ▴" toggle (44px touch target): needs-guide alert previews 3 items, recent guide responses 3, Upcoming Tours 5, Needs Attention 5. Display-only — list data, sort order, and caps (10/15) unchanged; ShowMore button renders only when the list exceeds its preview count. New expanded state + visibleItems helper + ShowMore component. Added src/components/__tests__/Dashboard.collapse.test.jsx (4 interaction tests: preview counts, expand/collapse per section, Ask button opens modal on expanded rows). | manual deploy. Backup withlocals_backup_20260717_231609. npm run build OK; 91/91 tests pass (87 + 4 new); files complete (Dashboard.jsx ends export default). Local browser check skipped (dashboard is auth-gated; behavior proven by the new interaction tests instead). Verified live: frontend 200, tours.php + guides.php 401 (auth enforced), live bundle index-D4FtyJRj.js matches fresh build. |
