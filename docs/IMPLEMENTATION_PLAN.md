# Florence With Locals — Tour Management System
## Implementation Plan (one step at a time, each verified)

**Created:** 10 September 2026 · **Companion doc:** `TECHNICAL_ANALYSIS_2026-09-10.md` (findings referenced as §x.y)
**Decisions taken:** staging environment first · security before features · radios = 1 receiver per person + 1 transmitter per guide, every departure · GYG missing tours: to be checked (likely products not connected to Bokun)

### How every step is executed (the rule)
Each step below is small enough to finish and verify in one sitting. For every step, Claude Code (or you) follows the same loop — this is what the `.claude/skills/implement-step` skill enforces:

1. `git checkout -b step/<id>` from `master` (the deploy branch — `origin/main` is an old divergent branch, never use it); tree must be clean.
2. Make **only** the change described in the step. No drive-by refactors.
3. Verify locally: `php -l` on touched PHP · `npm run test:run` · `npm run build` · the step's own **Verify** checklist.
4. Deploy to **staging** (`scripts/deploy.sh --target staging`) and run the **Smoke test** (below) plus the step's checklist against staging.
5. Commit with the step id in the message, merge to `master`, deploy to production, re-run the smoke test on production.
6. Tick the step in this file (`[x]`) and append one line to `docs/CHANGELOG.md`. If anything fails at 4 or 5, roll back (each step lists how) and do **not** proceed to the next step.

### Smoke test (run after every deploy, staging and production — 3 minutes)
- [ ] Login as admin → Tours page loads today's tours with groups shown as groups (PAX badges visible)
- [ ] Assign a guide to one tour, refresh → still assigned; unassign → cleared
- [ ] Guides page opens; Payments → Pending tab opens; Daily P&L opens (admin)
- [ ] Bokun Integration → "Sync now" completes (status `success`, not `failed`)
- [ ] Login as **viewer** → Guides/Payments show no edit controls; `curl -X DELETE …/api/tours.php/1 -H "Authorization: Bearer <viewer token>"` → **403**
- [ ] `curl -I https://<host>/api/health.php` → 200; `curl -I https://<host>/api/migrate_database.php` → 404/403
- [ ] Open the PWA on the phone on mobile data → Tours list appears

---

## Phase 0 — Safety net (do first, ~1 day)

### 0.1 Staging environment on Hostinger — [x] DONE 2026-09-10 (commit 1be84c6, merged to master; production not redeployed — goes live with 0.2)
- Create subdomain `stagingwithlocals.deetech.cc` → `…/public_html/stagingwithlocals/`; create DB `u803853690_withlocals_stg` from a **fresh dump of production** (with a `UPDATE users SET password=…` for a staging-only admin password); copy the server `.env` with `APP_ENV=staging`, staging DB creds, **`BOKUN_SYNC_ENABLED=false`** by default (so staging never writes reminders/Twilio to real guides — add a `TWILIO_DRY_RUN=true` flag honoured in `twilio_reminders.php`).
- `scripts/deploy.sh --target staging|production` (target selects path + `VITE_API_URL`); production is refused unless `--target production` is explicit.
- `config.php`: environment from `APP_ENV` (`EnvLoader`), **default = production** (§2.12); CORS list includes the staging origin.
- **Verify:** staging login works; sync on staging with `BOKUN_SYNC_ENABLED=true` reads from Bokun fine; Twilio dry-run logs but does not send. **Rollback:** none needed — production untouched.

**Notes from 0.1 (observed, not changed — for later steps):**
- Real staging hostname is `stagingwithlocals.deetech.cc` (hPanel created it without the dot); docroot `public_html/stagingwithlocals/`; DB + user `u803853690_withlocals_stg` (Hostinger prefix — `~/.florence/credentials.md` lists them without it). No Hostinger MCP server is configured on the owner's PC; DNS/subdomain/DB were created in hPanel by hand.
- **Staging admin password is still the production one**: the "Staging admin login" section of the credentials file was a placeholder, so the `UPDATE users SET password=…` of this step was skipped. Fill it in, then run that one UPDATE on the staging DB (do not reuse production passwords on staging).
- `config.php` was gitignored (`.gitignore:64`) although 0.1/2.1 change it and 0.2 deploys from `git ls-files`; it is now tracked (it holds no secrets).
- The plan assumed a `BOKUN_SYNC_ENABLED` env flag; the only existing flag was `bokun_config.sync_enabled` in the DB (staging inherits `1` from the dump). The env gate was added to `bokun_sync.php` (`bokunSyncEnabledByEnv()`, reported by `sync-info`).
- `deploy.sh` still ships the working tree (6 untracked `check_*`/`*_test`/`migrate_*` files went to staging), does not ship `.htaccess` (root + api — copied from production by hand on staging), and its health check treats the 401 from `tours.php`/`guides.php` as failure (exit 1 on every deploy) → all 0.2.
- **Production finding (untouched):** every browser-triggered sync (`triggered_by` = `periodic`/`startup`/`visibility`, full 67-day window) dies at the 60 s web timeout and leaves `sync_logs.status='started'` forever — 158 such rows in 24 h vs 275 completed. Only `cron` (240–330 s) and `webhook` complete, and the cron fires **twice** per 15-min slot (two rows at the same second → duplicate crontab entry). CLAUDE.md rule 7 ("Hostinger cron does not fire") is wrong. Proposed step for Phase 3: browser sync uses a short window (today → +7 d) or is dropped in favour of cron + webhook; dedupe the cron; mark `started` rows older than 10 min as `failed`.
- `public_html/withlocals/api/error_log` (10 MB) **is** written on the host and contains the Bokun access key in logged request headers (`BokunAPI: Headers:` lines) → add to the 0.3 rotation list and to 2.1 (never log auth headers).
- Staging `sync_logs` row 13855 is a stuck `started` row from the same full-window attempt on staging (harmless).

### 0.2 Repo hygiene that blocks nothing — [x] DONE 2026-09-11 (commits 3c20511, 776f5e2, merge 6c2e55e, follow-up 82fc41f; live on staging and production)
- Add `api/health.php` (no auth: `{ok:true, db:true, env, sha}`), used by deploy checks (§2.12).
- Add `tools/` (outside `public_html`) and **move** `fix_tour_dates.php`, `migrate_database.php`, `migrate_bokun_credentials.php`, `check_environment.php`, `compression_test.php`, `sentry_test.php` there with a CLI guard at the top of each. Delete them from the live server (`ssh … rm`) — verify with `curl` → 404.
- Add the `FilesMatch` deny block to `public_html/api/.htaccess` (§1.3) and make `deploy.sh` ship `public_html/api/.htaccess` and `dist/.htaccess`.
- `deploy.sh`: deploy from `git ls-files` allowlist, refuse dirty tree, always `npm ci`, replace `rsync --delete` with an explicit include set (never delete `.env*`, `logs/`), health check accepts 200 from `health.php`.
- **Verify:** smoke test on staging then production; `curl` the six removed scripts → 404. **Rollback:** `deploy.sh` keeps the previous release in `backups/`; restore that folder.

**Notes from 0.2 (observed):**
- **Resolved 2026-09-11 (owner approved):** the old one-off files in the production web root (`CLAUDE.md`, `docs/`, `test.php`, `setup_database.php`, `migrate_production_database.php`, `create_views_*.php`, `execute_payment_views.php`, `check_*.php`, `*.sql`) and the api leftovers (`BokunAPI.php.backup-*`, `check_*.php`, `test_bokun_oct24.php`, `bokun_cron.log`, `*.sql`, `add_missing_columns.php`, `bokun_debug.php`, `verify_fix.php`) were moved to `~/backups/prod-root-leftovers-20260910/` (nothing deleted; also inside `pre-0.2-20260910_215631.tgz`). Root `.htaccess` now also denies `*.md`, `*.bak*`/`*.backup*` and the `(debug|test|check|fix|migrate|setup|create_views|execute)_*.php` / `test.php` families (commit 64da79d, live on both targets). Note: missing paths under `/` fall through to the SPA `index.html` (200), so a 200 there is not a leak — check the body.
- `check_environment.php` lives on as `tools/env_check.php` (a scanner on the owner's PC deletes/blocks the original filename under `tools/`). Four `tools/legacy/*.php` stay untracked on purpose (gitignore families; one holds the old password) — 0.3 decides their fate.
- `deploy.sh` transfers with tar + a server-side compare-and-copy (no rsync on the owner's PC; server bash has no `/dev/fd`, so no process substitution). `bokun_cron.php` is shipped (live cron entry point) but 403 over HTTP. Backups are ordered by name — `cp -a` keeps old mtimes, so `ls -t` was picking the wrong one.
- First production deploy removed 159 stale hashed asset files from old builds; `api/error_log` (10 MB) moved to `~/logs/api-error_log-20260910`; api leftovers `check_*.php`, `test_bokun_oct24.php`, `bokun_cron.log`, `*.sql`, `BokunAPI.php.backup-*` remain in the api folder but answer 403.
- HSTS is only sent by the API (PHP); `/` has none — step 2.3. — resolved in 2.3 (HSTS from the site `.htaccess` on every URL).
- Staging admin password still equals production's (credentials file section still a placeholder; its "Production admin login" section holds `withlocals_stg`, which looks mis-filled).

### 0.3 Purge secrets & PII from git (§1.4) — [x] DONE 2026-09-16 (tree purge be6bd1a + doc scrubs, history rewritten with git filter-repo and force-pushed as master 40c0557; rotation support dab0764, gitleaks guard 73c4b5e, Sentry DSN 2b870e8; live on staging and production)
- `git rm --cached` + `git filter-repo --invert-paths` for: `fix_users.php`, `server/db.js`, `backend/`, `tours_temp.json`, `database_backup_before_payment_system.sql`, `tickets_import.csv`, `RE Your Bókun support case 00301047.txt`, `product_details.txt`, `getyourguide_data.txt`, `add_username_column.php`, `update_dhanu_email.php`, both stale `public_html` builds. Strip all `INSERT` rows from `database_schema_updated.sql`. Remove passwords from `CLAUDE.md` / `docs/API_DOCUMENTATION.md` / `docs/ENVIRONMENT_SETUP.md` / `docs/IMPROVEMENTS_SUMMARY.md` / `DEPLOYMENT_CHECKLIST.md` / `DEPLOYMENT_PLAN.md` / `DEPLOYMENT_SUCCESS.md` / `FINAL_DEPLOYMENT_INSTRUCTIONS.md` / `HOSTINGER_DEPLOYMENT_GUIDE.md` / `public_html/api/create_users_table.sql` (all still contain the old admin password; a grep for the old admin password and for `password_hash(` with a literal must return nothing afterwards). Force-push; re-clone.
- **Rotate**: admin + viewer passwords, Bokun key/secret, `ENCRYPTION_KEY` (re-encrypt `bokun_config` with `tools/migrate_bokun_credentials.php`), old DB user password, Sentry DSN. Use a different `ENCRYPTION_KEY` for dev.
- 0.3 result (2026-09-16): 27 tracked files removed + 7 more dead scripts found only in history (`import_tickets*.php`, `migrate_tickets.php`, `create_users.php`, `bokun_diagnostics.php`, `analyze_getyourguide.php`, `config_local.php`); 15 secret strings replaced across 168 commits (admin/viewer passwords, old Bokun access + secret key, three DB passwords, the Hostinger SSH password, a migration URL secret, bcrypt hashes, the viewer Gmail username). `git log --all -- tours_temp.json` empty; `gitleaks detect` over the rewritten history: 0 leaks. GitHub default branch moved `main` → `master`; `main` and the `claude/placeholder-*` branch deleted; anyone with an old clone must re-clone. Pre-rewrite bundle: `~/backups/guide-florence-with-locals-pre-0.3.bundle` (owner's PC).
- 0.3 rotated: admin password (prod + staging, staging has its own), viewer password + username → `viewer` (the second admin `sudesh` untouched), production DB password (owner in hPanel, `api/.env` updated), Hostinger SSH password (owner), Bokun access/secret pair (saved through `action=config`, cron sync 846/846 ok, old key to be deleted in Bokun by the owner), `ENCRYPTION_KEY` per environment (staging ≠ production ≠ dev; `tools/migrate_bokun_credentials.php --action=rekey --old-key-env=NAME --confirm=yes`), Sentry DSN (frontend `src/main.jsx` + server root `.env`; test event from staging accepted). Old `.env` copies: server `~/backups/env/`.
- 0.3 leftovers for later steps: `bokun_config` row id 2 still holds the OLD plaintext access key (app reads `LIMIT 1` = id 1; delete the row in 1.2/1.6); `GET bokun_sync.php?action=config` still returns the decrypted pair (1.2); `sync_logs` rows from browser-triggered syncs stay `started` (2.x); `.env.production` on the owner's PC holds the pre-rotation production key (owner to delete).
- 0.3 finding (2026-09-16): the leaked MySQL user `u803853690_guideDhanu` (from the purged `server/db.js`) still exists but is an orphan: it only has `ALL PRIVILEGES` on the leftover database `u803853690_guide` (not `u803853690_withlocals` / `_stg`). hPanel does not list users without an attached database; drop the `u803853690_guide` database in hPanel (or ask Hostinger support to drop the user) when convenient. `u803853690_guidefl` no longer connects.
- Add `gitleaks` pre-commit hook + CI job.
- **Verify:** `git log --all -- tours_temp.json` empty; `gitleaks detect` clean; login works with new passwords; sync works with new Bokun creds.

---

## Phase 1 — Critical security (~1 day) — **complete 2026-09-17 (1.1–1.6 live on production)**

| Step | Change | Verify | Rollback |
|---|---|---|---|
| **1.1** Server-side roles (§1.1) — [x] DONE 2026-09-16 (merge c120ab9; live on staging + production; `Middleware::requireAdminForWrites()` on 9 endpoints incl. the guide-requests owner branch, `bokun_sync.php` admin-only except GET sync-info/unassigned, uniform 403 `{success:false,error:'forbidden'}`; `AdminRoute`, `ModernLayout` role/name from `useAuth()`, one throttled 403 toast, viewer auto-sync gated client-side; observed on staging: viewer DELETE/POST/PUT/sync/config → 403, GET tours/sync-info → 200, 0 sync requests in 17 min, admin writes 200/201; note: `GET action=config` still returns the secret for admins → 1.2) | `Middleware::requireRole($conn,'admin')` on every POST/PUT/DELETE in `tours.php`, `guides.php`, `tickets.php`, `tour-groups.php`, `payments.php`, and on all of `bokun_sync.php` except `sync`, `sync-info`, `unassigned`. Add `AdminRoute` in `App.jsx` for `/bokun-integration`, `/daily-pnl`; `ModernLayout` takes role from `useAuth()`. | Viewer token: DELETE tour → 403, POST payment → 403, GET tours → 200. Admin: all writes still work. Existing vitest suite green. | Revert commit. |
| **1.2** Stop leaking the Bokun secret (§1.2) — [x] DONE 2026-09-16 (merge ff4403b; live on staging + production; `maskedBokunConfig()` = {configured, sync_enabled (env AND row), vendor_id, last_sync, api_key_masked, updated_at} for GET and POST `action=config`; single config row (extra plaintext rows deleted: 1 on staging, 3 on production, backups in server `~/backups/bokun_config_extra_rows_*.sql`), save updates in place, empty key fields keep the stored keys, JSON false really disables; auto-sync calls `action=sync` only, `sync_disabled` = skip with a 15-min attempt throttle; observed: config/sync-info bodies contain no key or secret, browser makes exactly 2 requests in 15 min (startup 3 s, periodic 901 s), cron after cleanup 848/848 ok. Finding: an HTTP `action=sync` with the default 60-day window is cut by the host with a 307 after its proxy timeout and the `sync_logs` row stays `started` — that is the source of the old `started` rows; cron (CLI) is unaffected → 3.3/2.x. Staging trap: the Enable Sync checkbox shows env AND row, so saving the form on staging (env flag false) writes row flag 0) | `action=config` returns `{configured, sync_enabled, vendor_id, last_sync, api_key_masked}` only; `bokunAutoSync.js` stops calling `config` — `action=sync` returns `{success:false, error:'sync_disabled'}` when disabled. | Network tab on staging shows no `secret_key`/`api_secret` anywhere; auto-sync still runs every 15 min; disabling sync in settings stops it. | Revert. |
| **1.3** Rate limiter (§1.5) — [x] DONE 2026-09-16 (merge 75d840e; live on staging + production; `RateLimiter::getClientIp()` = REMOTE_ADDR unless listed in `TRUSTED_PROXIES` (new env, empty); one atomic INSERT … ON DUPLICATE KEY UPDATE on MySQL's clock; `login_attempts` self-provisioned on first login (was absent on both environments, so the second limiter had been silently off); failed logins limited per IP 5/min AND per username 10/15 min, success clears the username counter, 429 + Retry-After with a neutral body; observed on staging: 6 wrong logins with random X-Forwarded-For → 429 (Retry-After 23 s), 11 for the admin username paced 5/min → 11th 429 (Retry-After 734 s), correct login after the window → 200 and 0 counter rows; production first login created the table, a wrong login with a forged header was recorded under REMOTE_ADDR. Finding: the host's per-IP connection throttle drops bursts faster than ~1 req/s before PHP (HTTP 0) — `tools/ratelimit_probe.php --delay` paces around it) | `getClientIp()` → `REMOTE_ADDR` only (opt-in `TRUSTED_PROXIES` env); self-provision `login_attempts` like `rate_limits`; login limiter keyed on IP **and** username. | 6 wrong logins in a minute → 429 even with random `X-Forwarded-For` headers; `SHOW TABLES LIKE 'login_attempts'` exists on staging. | Revert. |
| **1.4** Webhook secret + caps (§2.5) — [x] DONE 2026-09-16 (merge c1a8049; live on staging + production; `?key=` compared with `hash_equals` against `WEBHOOK_SECRET` (missing secret → 503 `webhook_not_configured`, wrong/missing key → 401 `unauthorized`, no log row); dates de-duplicated, sorted, max 3 per event (`dates_found`/`dates_processed` in the response and in two self-provisioned `bokun_webhook_logs` columns); stored payload capped at 65536 raw bytes + `...[truncated]` inside a JSON wrapper (column has `json_valid` CHECK); pure logic in `webhook_helpers.php`, checked by `tools/webhook_helpers_check.php`; observed on staging: no key 401, wrong key 401, 1 date 200 (1/1), 50 dates 200 (50/3), 200 KB 200 with 65536+marker stored, 3 log rows, 0 sync_logs rows (staging sync disabled); production: 401/401 with no log row, smoke green. Secrets in the credentials file under "Webhook secret"; the owner pastes the new URL into Bokun) | `bokun_webhook.php` requires `?key=<WEBHOOK_SECRET>` (`hash_equals`); max 3 dates per event; payload stored truncated to 64 KB. Update the URL in Bokun. | POST without key → 401; with key → 200 and sync log row; a payload with 50 dates → only 3 processed. | Revert + restore old URL in Bokun. |
| **1.5** Logout + hashed tokens (§3) — [x] DONE 2026-09-16 (merge 71f853f; live on staging + production; `POST auth.php?action=logout` deletes the session row (401 for a dead token); `sessions.token` = sha256(token), hashed rows marked `session_id = 'sha256:<hash>'` (raw tokens are also 64 hex chars, so length cannot distinguish them); `Middleware::findSessionUser()` tries the hash, then the raw value and rewrites the row in place, so existing logins survived; `database/migrations/20260916_hash_session_tokens.sql` run on both (staging 1 row, production 5 rows, unmarked count 0 after); `AuthContext.logout()` calls the endpoint then clears storage, `ModernLayout` uses it with replace-navigation, `ProtectedRoute` also requires a stored token; observed on staging: login row 64-hex hash, tours 200, logout 200, same token 401, raw-row transition 200 + rewrite, UI logout POST 200 → /login, Back twice stays on /login; production: same login/logout/401 sequence, 5 other sessions untouched) | `auth.php?action=logout` deletes the session; `AuthContext.logout()` calls it; `ModernLayout` uses `useAuth().logout()`; store `sha256(token)` in `sessions.token` (migration: hash existing rows). Fix `username`/`userName` key so the header shows the name. | Logout → old token gets 401; header shows the user's name; Back button after logout lands on /login. | Revert; sessions table migration is reversible (`ALTER` not needed — column stays). |
| **1.6** Encryption fail-closed (§2.7) — [x] DONE 2026-09-17 (merge d6b8950; live on staging + production; ciphertext prefixed `enc:v1:`, cipher and MAC keys derived from `ENCRYPTION_KEY` with HKDF (the old code used one key for both), `isEncrypted()` = prefix only, `encrypt()` throws without a key and `saveBokunConfig()` answers 500 `encryption_unavailable` without writing; legacy un-prefixed ciphertext still decrypts; `tools/migrate_bokun_credentials.php --action=prefix` re-encrypted 1 row / 2 fields on each environment (unprefixed rows 0 after, idempotent second run), `tools/encryption_check.php` 200/200 encrypt+decrypt and 200/200 prefixed; production proof: manual default-window sync `sync_logs` 16489 completed, 839 found / 839 synced / 0 failed. **Phase 1 complete.** Pre-1.6 dumps: server `~/backups/bokun_config_pre_1.6_{staging,production}_*.sql`) | Ciphertext prefix `enc:v1:`; `isEncrypted()` checks prefix only; `saveBokunConfig()` returns 500 if key missing; re-encrypt existing rows via `tools/`. | Save Bokun config on staging 200× in a loop → 0 decrypt failures (was ~1.7 %); remove `ENCRYPTION_KEY` on staging → save returns 500, never plaintext. | Keep old decrypt path for un-prefixed values during transition. |

---

## Phase 2 — Config & deploy hardening (~half day) — **complete 2026-09-17 (2.1–2.4 live on production)**

- **2.1** `config.php`: `error_log` path outside web root; remove second `.env.local` parser; drop `Access-Control-Allow-Origin` for disallowed origins; drop plain-http origin; `SET time_zone='+00:00'` after connect. **Verify:** errors appear in the log file; CORS preflight from staging origin works. — [x] DONE 2026-09-17 (merge 7f2f3c4; live on staging + production; error log = `FWL_LOG_DIR` or `<home>/logs/api-error[-env].log` (never under public_html, startup line on creation), one parser (EnvLoader; `APP_ENV=local|development|production` override), CORS list `https://withlocals.deetech.cc` (+ staging origin on staging, `http://localhost:5173` in development), disallowed origin → no ACAO at all, plain-http origin dropped, preflight 204; `SET time_zone='+00:00'` after connect (owner decision: DB side only, PHP stays Europe/Rome by design — comment in config.php). Observed: evil/plain-http/cross-env origins → 0 ACAO, own origin OPTIONS → 204 + headers on both environments; log files receive web-side lines; staging before/after: tours upcoming 402/402, unassigned 439/439, 42 reminders identical; production browser: 390 rows, all API 200, startup sync fired. Notes: (a) PHP sends `Vary: Origin` but the Hostinger edge (`Server: hcdn`) replaces it with `Vary: Accept-Encoding` — API responses are `no-store`, revisit with the .htaccess consolidation in 2.3; (b) the existing per-request `BokunAPI: GET …` error_log lines now land in the file (~54 KB per sync) — trim or rotate in 3.4/3.9)
- **2.2** Server `.env` moved outside the web root; `EnvLoader` reads `FWL_ENV_FILE` first. **Verify:** `curl https://<host>/.env.local` → 403/404; app still works. — [x] DONE 2026-09-17 (merge e2e9cc9; live on staging + production; EnvLoader order: `FWL_ENV_FILE` → per-site file outside every web root → legacy in-tree locations; on the Hostinger layout the parent of the document root is deetech.cc's own web root shared by all sites, so the outside file is `~/env/<site>/.env` (`~/env/withlocals/.env`, `~/env/stagingwithlocals/.env`, mode 600) built from `<site>/.env` + `<site>/api/.env` (20 / 22 keys, no duplicates, values unchanged); `health.php` reports `env_source`; `tools/env_which.php`. Observed per stage: staging inside_webroot → outside_webroot after the copy → still outside_webroot after the move, production the same; smoke green at every stage; browser /tours 323 (staging) / 393 (production) rows all API 200; `/.env` and `/api/.env` → 403 on both; CLI (cron) resolves the outside file too. In-tree files kept in `~/backups/env_{staging,production}_webroot_<date>/`)
- **2.3** One `.htaccess` (from `public/`, plus HTTPS redirect + HSTS); real CSP on HTML only; `immutable` caching for `/assets/`, `no-cache` for `index.html`; remove duplicate headers and `ob_gzhandler` from PHP. **Verify:** `curl -I` shows exactly one of each header; Lighthouse "uses efficient cache policy" passes; app loads after a deploy without a hard refresh. — [x] DONE 2026-09-17 (merge abaa121; live on staging + production). One site `.htaccess` (`public/` → `dist/`, inherited by `/api/`): HTTPS redirect (skips https and `X-Forwarded-Proto: https`), `Header always set` HSTS / nosniff / `X-Frame-Options: DENY` / Referrer-Policy / Permissions-Policy, `X-Powered-By` unset; enforcing CSP on `index.html` only: `default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; font-src 'self'; img-src 'self' data: blob:; connect-src 'self' https://o4510711031201792.ingest.de.sentry.io; worker-src 'self' blob:; manifest-src 'self'; object-src 'none'; frame-ancestors 'none'; base-uri 'self'; form-action 'self'`; `/assets/*` `public, max-age=31536000, immutable`, `index.html` + `/` `no-cache, no-store, must-revalidate`, `sw.js` + manifest `no-cache`, mod_expires block dropped. API `.htaccess` = deny rules + rewrites + OPTIONS only (deflate/`Vary` append/`X-Compression` removed); `config.php` without `ob_gzhandler`, one `Cache-Control: no-store` line, no security/CSP `header()` calls (CORS kept); `health.php` nosniff line removed; `index.html` `!important` `<style>` block deleted (no inline script/style left); dead `public_html/.htaccess` deleted; `sw.js` untouched (navigations were already network-first with cached fallback). Observed on both environments (curl GET on `/`, `/index.html`, `/assets/index-*.js`, `/api/health.php`): every security header ×1, `Cache-Control` ×1 with the values above, `/api/tours.php` `Cache-Control` ×2 → ×1, http → 301 https, no loop; CSP report-only walk-through (tours, dashboard, daily-pnl, guides, bokun-integration + sync) = 0 violations, then enforcing = 0 violations, /tours 323 rows staging / 395 production, all API 200; normal-reload check: staging `index-DezTdk-t.js` → `index-Te1jXkRV.js`, production `index-Db3cNr_e.js` → `index-BmYc8s8t.js` (Lighthouse not run — verified with the header values instead). The package version (0.0.2) is now baked into the bundle as the Sentry `release` (`vite define __APP_VERSION__`), so bumping `package.json` changes the bundle hash. Notes: (a) the Hostinger layer sets `Content-Security-Policy: upgrade-insecure-requests` on every response and `Expires` +7d on assets — our `.htaccess` `set` overrides the CSP on `index.html`, the JS/JSON copies are harmless and outside our control; (b) `X-XSS-Protection` dropped on purpose (deprecated); (c) unhashed files (`/icons`, `/images`, favicon) now use the server default cache policy instead of 1 year; (d) browser-triggered syncs: the production UI "Sync Now" got HTTP 504 from the proxy after ~100 s while PHP finished it (`sync_logs` 16815 completed 308/308 in 172 s), and every full page load fires a startup sync that stays `started` — 3.3; (e) cron syncs run as pairs (two cron entries) of ~310 s each — 3.4; (f) production `api-error.log` is already 43 MB (BokunAPI GET lines + `Rescheduling detected … 14:15:00 → 14:15` spam, step 3.2) — 3.4/3.9)
- **2.4** Kill the two dead-weight workflows: keep `main.yml` as CI (scoped `php -l public_html/api`, `npm run test:run`, `npm run build`, gitleaks); delete `deploy.yml` or convert to FTPS with an allowlist. **Verify:** CI green on a PR. â [x] DONE 2026-09-17 (Part A merge 6fbb3c4 via PR #1, Part B merge 995821e; live on staging + production). Part A: `.github/workflows/ci.yml` is the only workflow (`push` to master + `pull_request`; jobs `php-lint` PHP 8.2 over `public_html/api` + `tools`, `frontend` Node 20 `npm ci`/`test:run`/`build`, `gitleaks` unchanged); `deploy.yml` deleted (never green, Hostinger blocks runner SSH); no deploy from CI. PR #1 run https://github.com/DhaNu1204/guide-florence-with-locals/actions/runs/35267692375: php-lint 11 s, frontend 42 s, gitleaks 7 s, all green (first push failed in 0 s: unquoted colon in a job name). Repository secrets `SSH_HOST`, `SSH_PORT`, `SSH_USERNAME`, `SSH_PRIVATE_KEY` are unused now â owner to delete. Part B (log flood from 2.1/2.3 notes): `BokunAPI::debugLog()` gates the 14 per-request/per-response lines and `bokun_sync.php` gates the per-booking `Rescheduling detected` line (it fires for every booking every sync until 3.2 fixes the seconds-vs-HH:MM compare â 3.2 should make it unconditional again) behind `EnvLoader::getBool('BOKUN_DEBUG_LOG', false)`; errors, warnings, rate-limit retries and the per-sync summary stay; `.env.example` documents `BOKUN_DEBUG_LOG=false`. Observed: local flag off â 3 error lines, flag on â 9 lines; staging one real Bokun request â log growth 0 bytes (the 1-day CLI sync is impossible on staging: `syncBookings()` itself is gated by `BOKUN_SYNC_ENABLED=false`); production cron slot before the deploy (22:00 Rome) wrote â245 KB API lines + â195 KB rescheduling lines, the first slot after it (22:15 Rome, rows 16852/16853 completed 850/850 in 302 s) wrote 1.9 KB in total; `api-error.log` (46.7 MB) rotated to `~/logs/api-error.log.20260917.gz` (2.4 MB), new file created by the next cron. Notes: (a) error_log stamps are Europe/Rome while `sync_logs` is UTC â subtract 2 h when correlating; (b) `bokun_cron.php` logs `PHP Warning: Undefined array key "REQUEST_METHOD"` on every run â 3.3/3.4; (c) two hPanel cron entries fire the same sync every 15 min (rows always come in pairs, ~300 s each) and every open browser tab adds a `periodic` run â 3.3/3.4)

---

## Phase 3 — Data integrity & the "unassigned" bug — ✅ COMPLETE 2026-09-20 (3.1–3.10 all done)

> **Phase 3 is closed: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6a, 3.7, 3.8, 3.9 and 3.10 are all live. 3.6 (one auto-grouper) was absorbed by 3.6a + 3.7 — `tour-groups.php` still has its own merge path, noted for Phase 8. Next: Phase 5 (frontend correctness), then Phase 4.3, then the owner's Phase 6 features.**
>
> **Resumed 2026-09-18 with 3.1** (4.0, 4.1, 4.1a, 4.2 are live; 4.3 still open).
>
> **Postponed on purpose (owner decision 2026-09-18):** the owner's #1 pain is mobile load time, so Phase 4 steps run first in the order **4.2 → 4.1 → 4.3**; Phase 3 resumes after 4.3. Nothing in 4.1–4.3 depends on a Phase 3 step.

- **3.1 Sync must not overwrite local state (§2.1).** Remove `paid`, `payment_status`, `total_amount_paid`, `expected_amount` from the sync UPDATE; add `bokun_total_price DECIMAL(10,2)` and write Bokun's amount there; badge in Tours/mobile card reads "Customer paid" from `bokun_total_price`, "Guide paid" from `payments`. **Verify:** set `paid=0` on a tour, run sync → still 0; P&L revenue unchanged (it reads `bokun_data`). — [x] DONE 2026-09-18 (step commits 98a6f01 + 44a4b3b, merge a061bdb; live on staging + production). **What §2.1 got wrong:** on production NOT ONE tour had `paid = 1` (0 of 6,167 active; 0 wrongly green badges before and after). `BokunAPI::transformBookingToTour()` looked for a top-level `totalPrice` / `paidAmount` that Bokun never sends (the price lives in `productBookings[0]`), so `$totalAmount` was always 0 and every sync rewrote the four local columns to `0 / 'unpaid' / 0.00 / 0.00` — anything set through `tours.php` PUT was wiped within 15 minutes, and the 130 tours whose guide really was paid (rows in `payments`) showed no badge at all. Pending Payments `expected_amount` summed to €0.00 on both environments (never the retail price). Changed: sync UPDATE no longer lists `paid`, `payment_status`, `total_amount_paid`, `expected_amount` (INSERT only, `paid` is a literal 0 in the mapper); new `tours.bokun_total_price DECIMAL(10,2)` + `bokun_currency VARCHAR(3)` written on INSERT, UPDATE and the duplicate-key path from the new pure helper `bokunCustomerPrice()` (tour_classification.php; same source order as the P&L retail figure: `resellerInvoice.total` > `customerInvoice.total` > positive `totalPrice`, over the booking, `productBookings[0]`, `activityBookings[0]`; currency from the same invoice — USD bookings carry EUR invoices); `ensureBokunPriceColumns()` self-provisions the columns and backfills once (`database/migrations/20260918_tours_bokun_total_price.sql` holds the same statements). Badge source — chosen: a server-computed, read-only `guide_paid` in `tours.php` (both views) = a `payments` row exists for the tour's current guide, group-aware (1 group = 1 payment), same rule as Pending Payments. NOT `tours.paid`, because nothing sets that flag when a guide payment is recorded, so it cannot mean "guide paid" even now that the sync leaves it alone. UI: `src/utils/paymentBadges.js` (`isGuidePaid`, `guidePaymentState`, `formatCustomerPrice`); Tours row, mobile card and the Dashboard chip/stat show a green **"Guide paid"** only from `guide_paid`; the booking details modal shows a labelled **"Customer paid: €x"** from `bokun_total_price` (hidden for 0 = cancelled bookings, whose invoice Bokun zeroes). Pending Payments was left as it is: `expected_amount` is now purely local (settable via PUT, no longer clobbered) — it was never the retail price, and wiring the P&L guide-fee logic into `guide-payments.php` would mean extracting it from `pnl.php` (own step). Observed: backfill staging 7,031 rows (2.4 s), production 7,198 rows (2.2 s), all EUR, SQL backfill = PHP mapper on every row (0 mismatches on both). Staging proof with a real one-date sync (24 bookings updated, `last_sync` moved): tour A set via API to `paid=0, expected_amount=123.45` → after sync `0 / unpaid / 0.00 / 123.45`, and its blanked `bokun_total_price` came back as 124.58 EUR; tour B set to `paid=1, payment_status=partial, expected_amount=67.89` (values the old sync wiped) → after sync `1 / partial / 0.00 / 67.89`, price 249.16 EUR. Staging before/after: upcoming 376 rows / 312 active / 759 PAX / 250 unassigned, pending 570, dashboard 56 / 250 / 570, P&L totals identical (retail 90,243.11 / profit 1,919.62); 46 tours 25 Mar–10 Apr now show "Guide paid" (23 through their group) with the legacy flag 0 on all. Production before/after: 464 rows / 385 active / 903 PAX / 195 unassigned, pending 580, old-field fingerprint identical, P&L identical (retail 96,673.28 / profit 4,313.02). Production cron check: snapshot of the four columns for all 7,198 tours at 18:54:52Z, first cron sync after the deploy (sync_logs 17148, completed, 827 found / 826 updated / 1 created, 137 s): 826 rows re-synced, 1 new booking inserted as paid=0 / unpaid / 0.00 / 0.00 with bokun_total_price 297.72 EUR, **0 rows** changed in paid / payment_status / total_amount_paid / expected_amount; all 827 synced rows carry a price. vitest 136 green, smoke 12/12 on both. Notes: (a) the production cron check is weak by nature — every local value in the sync window was already 0/unpaid, so "0 changed" would also have held with the old code; the staging proof with non-default values is the real evidence; (b) the staging sync ran through `FWL_ENV_FILE` pointing at a temporary 0600 copy of the staging env with only `BOKUN_SYNC_ENABLED` flipped (deleted right after) — the way to run a gated staging sync without editing the env; (c) that sync marked all 24 re-synced bookings `rescheduled` — the 3.2 bug, untouched here; (d) `total_amount_paid` is still the P&L's last-resort revenue fallback (`pnl.php:337`) and is always 0 — harmless while every booking has an invoice, revisit with 3.4; (e) 45 old rows still carry `payment_status='paid'` from before — not rewritten (no payment data changed in this step); the dead components `TourCards`, `CardView`, `UnpaidToursModal`, `UI/TourCard`, `UI/StatusBadge` still read `tour.paid` → delete in 3.9/5.x.
- **3.2 Time compare (§2.3).** `substr(...,0,5)` on both sides; `rescheduled_at` only when changed. **Verify:** run sync twice → `SELECT COUNT(*) FROM tours WHERE rescheduled_at > NOW()-INTERVAL 5 MINUTE` is 0 the second time. — [x] DONE 2026-09-18 (commit b2953d4, merge d6e9987; live on staging + production; **data repair run 2026-09-18 after the owner's confirmation**: ids + old `rescheduled_at` saved first to `~/backups/reschedule_repair_<site>_20260918_*.txt` (mode 600, reversible), then the UPDATE below in a transaction that rolls back unless affected = saved: staging 6,378 rows reset / 336 genuine kept, production 6,528 rows reset / 351 genuine kept, 0 falsely flagged left, 0 moved-but-unflagged; staging upcoming list: 5 flagged = 5 Rescheduled badges). `bokunIsRescheduled()` / `bokunNormalizeTime()` / `bokunNormalizeDate()` in `tour_classification.php` compare `HH:MM` and `Y-m-d`; the date compare was already like-for-like (both `Y-m-d`), only the time compare (`10:00:00` vs `10:00`) was always true. "Rescheduling detected" is unconditional again; per-request Bokun logging stays behind `BOKUN_DEBUG_LOG`. `tools/reschedule_check.php`: 15 checks (`10:00:00` vs `10:00` → no, `10:00` vs `14:30` → yes, unusable input never flags). Observed — staging, same window (2026-09-20..23, 74 bookings) synced three times: run 1 found 3 REAL reschedules (09:00 / 09:30 / 10:00 → 12:00 on 2026-09-22; staging data was days old) and logged them, 968 B; runs 2 and 3 stamped 0 rows (newest `rescheduled_at` anywhere 19:14:56Z, run 3 completed 19:19:01Z), 618 B and 693 B of log, no "Rescheduling detected" line. Production, first cron after the deploy (sync_logs 17150): completed, 827 found / 827 updated, 191 s, **0 rows** with a new `rescheduled_at` (the run before stamped 826), 0 in the last 5 minutes, log growth 517 B (was ~195 KB of rescheduling lines per run before 2.4). Repair evidence (production, 7,199 tours): `rescheduled = 1` on 6,878; really moved (`original_date`/`original_time` differ from `date`/`time`, the signal the badge uses) 351; flagged but not moved 6,527; moved but not flagged 0; upcoming: 641 flagged, 10 really moved. The badge already requires flag AND a real difference, so the repair changes nothing visible. Proposed (NOT run): `UPDATE tours SET rescheduled = 0, rescheduled_at = NULL WHERE rescheduled = 1 AND NOT ((original_date IS NOT NULL AND original_date <> date) OR (original_time IS NOT NULL AND LEFT(original_time,5) <> LEFT(time,5)))` → 6,527 rows on production; the 351 genuine rows keep their flag (their `rescheduled_at` is the time of the last buggy sync, the true moment is not recoverable). Note: `original_date` / `original_time` are written on every sync UPDATE as "first seen" values even for bookings that never moved — harmless, left as is.
- **3.3 Empty window ≠ failure (§2.4).** **Verify:** sync a date with no bookings → `sync_logs.status='success'`, groups on that date rebuilt. — [x] DONE 2026-09-18 (commit 57b2559, merge d6e9987; live on staging + production). `BokunAPI::getBookings()` returns `[]` when BOTH roles (SUPPLIER, SELLER) answered with a well-formed `items` array and neither has a booking; a role that throws or answers without an `items` array counts as failed, and only then are the legacy endpoints tried (they throw if they fail too) → a real failure is still `failed`. `syncBookings()` runs `autoGroupAfterSync()` also when Bokun returned 0 bookings (not when every booking failed). Size of the problem (production, last 7 days): 5 `failed` rows, 5 of 5 were empty results — all single-day webhook syncs, error `HTTP Error 404: Could not process this request` (the legacy endpoint's answer, reached only after an empty search; 0 "Failed to fetch with role" lines in the logs). Observed on staging: far-future 2027-12-25 → sync_logs 13864 `completed`, 0 found, 0.75 s (before: `failed`). Stale-group proof: two tours of a 2-member auto group (2026-07-19 09:30, product 962885) moved onto that quiet date → sync (0 bookings) → grouping ran: group 1299680 created with both members; both tours moved away again → the date holds an empty stale group → sync (row 13867 `completed`, 0 found) → 0 groups left on the date; tours restored and regrouped by a normal sync of 2026-07-19 (31 bookings, group 1299685). Genuine failure check: same empty date with a deliberately wrong secret → `getBookings()` throws (`HTTP Error 404`), nothing swallowed. Smoke 12/12 on both. Notes: (a) those 5 webhooks named 2027 dates on which booking-search finds nothing and our DB has no tour — look at them with 6.4 (missing GetYourGuide tours); (b) a legacy-endpoint 404 is what any total failure looks like in `sync_logs.error_message`, the real cause is only in the error log ("Failed to fetch with role"); (c) when only ONE role fails and the other returns bookings, the sync still succeeds with the partial list — unchanged here, belongs to 3.9; (d) group ids still change when a date is regrouped (3.7).
- **3.4 Sync speed: product cache, rate title from the payload, indexed booking lookup (§2.6 + the index note in 4.0-e).** **Verify:** `sync_logs.duration` for a full sync drops (record before/after); Bokun request count per sync logged. — [x] DONE 2026-09-19 (step commit d2da898, merge 52500e9; live on staging + production). **Measured before, production, 20 completed cron syncs 2026-09-19 07:45–12:30 UTC (809–814 bookings each): median 190.7 s, worst 263.8 s, best 125.2 s.** Where the time went (measured, not assumed): (a) 752 of the 815 bookings in the window reached `GET /activity.json/{productId}` — **one call per booking**, no cache — and those 752 calls cover only **19 distinct products**; a live product call from the server took 131/174/198 ms, so ≈126 s of a 190 s sync; (b) that call volume (≈350/min) trips Bokun's 400/min quota and `makeRequest` then does `sleep(60)` inside the request: `Rate limited, retrying in 60s` fired **634× on 17 Sep, 377× on 18 Sep, 91× by 12:46 UTC on 19 Sep** (the log only goes back to 2026-09-17, when 2.1 created it) — one sleep is exactly the 125 s → 190 s spread; (c) the per-booking existence lookup could not use an index at all (production EXPLAIN: `type=ALL key=NULL rows=3830` over a 65.6 MB table) — 5.4–7.9 ms per booking = 4.3–6.4 s per sync; (d) the ≈60-minute reminder reconcile is already a no-op since 3.10; grouping and the 815 UPDATEs are the small remainder. **Bokun requests per sync before: 764** (752 product + 12 search). **Cause A — the index.** `WHERE bokun_booking_id = ? OR external_id = ?` is now two indexed lookups: `external_id` (UNIQUE, `type=const`) first, `bokun_booking_id` only as a fallback; new `idx_tours_bokun_booking_id`, self-provisioned by `ensureBokunBookingIdIndex()` **and** `database/migrations/20260919_tours_bokun_booking_id_index.sql` (run on production 2026-09-19, `ALTER TABLE` 0.07 s). Both shapes were measured on staging with the index in place, 300 real ids × 3 runs: the OR form becomes an `index_merge` at **0.11–0.15 ms/booking**, the split form is `const` at **0.05–0.07 ms/booking** → **the split was kept** (≈2.5× faster, and it never touches the second index for a booking that exists). Production EXPLAIN after: `const rows=1` / `ref rows=1` (was `ALL rows=3830`). Proved equivalent first: 1,000 real production bookings, **0 rows where the OR form and the split form pick a different row**, 0 duplicated `bokun_booking_id`, 0 empty ids. **Cause B — the N+1 product call.** The language ladder now prefers `productBookings[0].rateTitle`, which Bokun already put in the booking (**present in 4,000 of 4,000 stored payloads**), and the fallback `getProduct()` runs through `getProductCached()` — one HTTP call per product per PHP process, failures cached as unavailable so one broken product cannot cost ≈800 failed calls. Proved equivalent first, over a whole sync window (815 bookings, 19 products, one product call each): the payload title equals the product endpoint's title for that `rateId` on **812 of 815**, the 3 that differ are reseller titles, and **0 bookings derive a different language**. The keyword ladder moved unchanged into the pure `BokunAPI::languageFromRateTitle()`. **Cause C — not changed, only counted** (it is a consequence of B): every sync now logs `bokun_requests`, `product_calls`, `product_cache_hits` and `rate_limit_sleeps` in one un-gated summary line. **Measured after.** Staging, same 815 bookings, steady state: **176.9 s and 244.9 s with the old code → 10.0 s and 7.14 s with the new one**, `bokun_requests=12 product_calls=0 product_cache_hits=0 rate_limit_sleeps=0` on both runs (12 = the paginated booking search over the two roles; 0 product calls because every payload carried its rate title). Production cron after the deploy: **9.19 s** (13:15 UTC) and **14.65 s** (13:30 UTC) against a before-median of 190.7 s, 815 bookings found and synced both times — the same range as every run before the deploy, nothing silently skipped. **Data identity (the step had to be invisible in the data).** Full per-column checksum snapshots of all 7,198 staging tours (46 columns) around a sync with the OLD code and around a sync with the NEW code produce the same profile: `last_sync` 815 / `updated_at` 815 (every synced row, by design), `group_id` 374, `bokun_data` 38–57 — and **no other column at all**; `language` changed on **0 rows**. The `group_id` churn is identical before and after and is step 3.7 (auto-groups are rebuilt every sync), the `bokun_data` rows are Bokun's own payload changes between runs. Production across the 13:30 cron sync: 1 genuinely new booking (id 7218), `last_sync`/`updated_at` on 815 rows, `group_id` on 337, `bokun_data` on 46 \u2014 and no other column; `language` on 0 rows. **Sync Now (the follow-on question, answered with data, nothing implemented).** A real manual sync through the hosting proxy returned **HTTP 200 in 6.44 s** (server side 6.12 s) and produced **exactly one** `sync_logs` row — no edge-retry twin. So yes: the button now completes far inside the ≈100 s proxy limit, and no step 3.4a is needed. That also closes note 4.0-(a) ("Sync Now does not complete on production") and removes the source of the stray `started` rows. Notes: (a) the `sleep(60)` inside `makeRequest` is still there — it is the correct behaviour for a real 429 and with B fixed a sync makes 12 requests, so it should never fire again; the counter will say if it does. (b) `getProductCached` is static, so a webhook process that syncs three dates reuses it; only the counters are reset per sync. (c) A rate title that names no language still means English (the product's default rate) — unchanged. (d) The 374-row `group_id` churn per sync is now the biggest remaining write load → 3.7. (e) The duplicate hPanel cron entry running `/usr/bin/php` (PHP 7.4) is still there — owner to delete (4.0-d). (f) `tools/` gained the measurement scripts used here (`sync_speed_check.php`, `sync_breakdown_check.php`, `lookup_equiv_check.php`, `ratetitle_equiv_check.php`, `tours_snapshot.php`) plus 16 pure checks in `sync_cache_check.php`; `tools/` is never deployed.
- **3.5 The unassigned-list bug — [x] DONE 2026-09-18 (details below).** Two verified causes and one probable:
  1. When `autoGroupAfterSync` recreates a group it copies the guide from the first member onto the **group** (`bokun_sync.php:905-922`) but never calls `propagateGuideToTours()` — so a booking that joins an already-assigned departure keeps `tours.guide_id = NULL` / `needs_guide_assignment = 1`. Anything that looks at tours individually (Dashboard "needs guide", `?action=unassigned`, guide filter, pending payments, the report when the group row is missing) shows it as unassigned. **Fix:** call `propagateGuideToTours($conn,$newGroupId,$guideId)` after setting the group guide, and make `propagateGuideToTours` a shared helper included by `bokun_sync.php`.
  2. The report (`Tours.jsx:1035-1135`) is built from the page's loaded state; when the group list is missing/paginated (§2.9) members render as standalone rows with their own (NULL) `guide_id` → false "unassigned". **Fix:** add `start_date/end_date` + full pagination to `tour-groups.php`; better, move the report server-side: `GET tours.php?action=unassigned-report&start=&end=` returning departures (groups **or** single tours) where the *effective* guide (`COALESCE(tg.guide_id, t.guide_id)`) is NULL. This same endpoint becomes an AI-assistant tool later (Phase 7).
  3. Probable: the localStorage cache (§2.8) serving a stale list from before an assignment. Fixed by 5.1.
  **Verify:** create a booking on an assigned departure on staging (or simulate via SQL), run sync → the new tour has `guide_id` set; the report for today matches a manual count; a tour assigned 1 minute ago never appears.
  **— [x] DONE 2026-09-18 (step commit b3db302, merge c3fa306; live on staging + production).** Cause 1 fixed: new shared `public_html/api/group_helpers.php` holds `propagateGuideToTours()` (moved unchanged from `tour-groups.php`, explicit assignment, overwrites) and `fillMissingGroupGuide()`; `autoGroupAfterSync()` calls the latter right after a rebuilt group inherits a member's guide — it fills ONLY members with `guide_id IS NULL` (sets `needs_guide_assignment = 0`), never overwrites a member that carries a different guide (that is logged instead; production has 1 such group), result key `guides_filled`. Cause 2 fixed: `GET tours.php?action=unassigned-report` (+ the list's own filters `upcoming | past | date | start_date+end_date`, `product_type`) returns one row per DEPARTURE whose effective guide is missing (`HAVING MAX(tg.guide_id) IS NULL AND MAX(t.guide_id) IS NULL`), cancelled and tickets excluded; `Tours.jsx` asks it, `src/utils/unassignedReport.js` formats the same text layout. Also: `bokun_sync.php?action=unassigned` (Bokun page list) now tests the effective guide instead of `needs_guide_assignment`, which a single-tour assignment never clears. Cause 3 (stale cache) stays with 5.1. Size before (production, upcoming): 36 active tours with `guide_id NULL` although their group has a guide (359 over all dates), 54 with a guide but `needs_guide_assignment = 1` (489 over all dates), 216 guide-less bookings vs 150 guide-less departures. Observed — staging: a member of a fully assigned 2-booking departure (group 1299640, guide 34) was set to `guide_id NULL / needs 1` to simulate a booking that just joined → real sync of that date (12 bookings, `guides_filled: 4`) → the tour has guide 34 / needs 0 again inside the rebuilt group 1299701 (the other 3 fills were real cases on that date). Report = manual SQL count for every filter: upcoming 163 = 163, today 5 = 5, 19 Sep–10 Oct 87 = 87, past 40 days 59 = 59 (a per-booking view would have said 250 / 8 / 146 / 176). Assigned-a-minute-ago: range report 30 → assign a single tour via `tours.php` PUT → 29, assign a group via `tour-groups.php` PUT → 28, both gone from the report at once; restored → 30. Browser: the button requests `action=unassigned-report&upcoming=true`, file has 163 lines, 0 console errors. Production: report = manual count (upcoming 131 = 131, today 1 = 1); first cron after the deploy (sync_logs 17152, completed, 827 found, 131 s): guide-less bookings 216 → 188, "no guide although the group has one" 36 → 8, the mixed-guide group logged and left alone. vitest 143 green, smoke 12/12 on both. Notes: (a) the remaining 8 rows all sit in 5 MANUAL merges (`is_manual_merge = 1`), which auto-grouping must never touch — a tour merged by hand into an already assigned group does not get the guide; the report / `action=unassigned` already treat them as assigned (effective guide), per-tour views do not → fix the manual-merge path in 3.6; a one-off fill for these 8 (and the ~320 past rows, which would change guide-report counts) is proposed to the owner, NOT run; (b) the banner on the Tours page ("N tours still need a guide") is still computed from the page state and is inflated when more than 50 groups exist (staging 197 vs 163 real) — that is §2.9 / step 5.2, worth pulling forward; (c) "Upcoming" means today..+60 days: production has 19 more unassigned departures beyond that window that no view shows; (d) `needs_guide_assignment` is now read by nothing that matters (only dead `UI/Card.jsx`) — candidate for removal in 3.9; (e) group ids still change on every regroup (3.7).
- **3.6a Manual merges pass on the group's guide + one-off fill of the 8 leftover tours** (follow-up of 3.5, owner request 2026-09-18). — [x] DONE 2026-09-18 (commit d66ad78, merge 54a2ee8; live on staging + production). Root cause: a manual merge always builds a NEW group and `syncGroupGuideFromTours()` copied a member's guide onto it but never onto guide-less members. It now calls the shared `fillMissingGroupGuide()` from 3.5 (fill only, never overwrite a different guide); the same function serves the Auto-Group button path. Auto-grouping still never touches `is_manual_merge = 1` groups. One-off fill, UPCOMING rows in MANUAL merges only, ids saved first to `~/backups/guide_fill_3_6a_<site>_20260918_*.txt`, transaction rolls back unless affected = saved: staging 0 rows (it had none; its 17 guide-less members were in auto groups and heal with a sync), **production 8 rows filled → 0 left** (manual 0, auto 0). **Owner decision: the ~305 PAST rows (guide-less member of an assigned group, date before today) are deliberately NOT touched** — filling them would change historical guide reports and pending-payment attribution. Proof on staging through the real API: assigned 2-booking departure (group 1299445, guide 19) + a guide-less single tour 4649 → `POST tour-groups.php?action=manual-merge` → new manual group 1299702, the joiner has guide 19 / `needs_guide_assignment 0` immediately; a real sync of that date afterwards (17 bookings, 2 other groups rebuilt) left group 1299702 exactly as it was (same id, 3 members, guide 19, `is_manual_merge = 1`). Test data restored (group dissolved, joiner un-assigned, date regrouped). Note: a manual merge takes `group_date`/`group_time`/title from the tour with the lowest id, not from the departure it was dropped on (my test group showed 11:45 instead of 09:30) — cosmetic, for 3.6.
- **3.6 One auto-grouper (§3).** `tour-groups.php` "Auto-group" button calls `autoGroupAfterSync()`; `manualMergeTours` uses `getMaxPaxForTitle()`. **Verify:** merging two Accademia bookings totalling 12 PAX succeeds; Uffizi 10 PAX still refused.
- **3.7 Stable group identity (§2.2).** Incremental regroup: for each computed bucket, if an existing auto-group has the identical member set, keep it; else update members; only create when new. **Verify:** group ids on a date are identical before/after a sync with no changes; P&L overrides survive a sync. — [x] DONE 2026-09-20 (step commits eac5034 + e5e3afd, merge 22c44de; live on staging + production; migration run on both). **Measured before (production):** 1,098 live auto groups holding ids 1…1,508,222 — **1,507,124 ids burned**, AUTO_INCREMENT at 1,508,223. **10 of the 11** `pnl_tour_costs` overrides keyed on `'g<id>'` already pointed at a group that no longer existed (€50/€200/€294 ticket costs on 2026-07-09, 2×€175 on 2026-07-10, two `outsourced=1` on 2026-07-19, two `guide_cost=0` + one empty row on 2026-08-12). The 11th survived **because it points at a manual merge**, which auto-grouping never recreates — the cleanest possible confirmation of the cause. `tour_groups.notes` is empty on all 1,098 auto groups and there is no history, so it is impossible to say whether a note was ever lost; `display_name` and `max_pax` are recomputed from the bucket on every run, so they were rewritten rather than lost. Churn per sync: staging, one sync with no real Bokun change → **359 tours changed group id, 131 groups deleted, 137 created**; production's own log for the last old-code cron run reads `Auto-grouped 332 tours into 118 groups` — 118 brand-new group rows every 15 minutes. **What is deleted and recreated per sync (old):** every tour in range whose group is an auto group is detached (`tours.group_id = NULL`), auto groups with no members are deleted, then a fresh `tour_groups` row is INSERTed for every departure and the members re-assigned. **Keyed on `group_id`:** `pnl_tour_costs.tour_unit = 'g<id>'` (the only place where the id is stored long-term), `tour_groups` itself (`display_name`, `notes`, `max_pax`, `total_pax`, `guide_id`/`guide_name`), and the transient `tour_unit` string computed on the fly by `tours.php`, `payments.php`, `guide-payments.php`, `payment-reports.php`, `guide-tour-report.php`, `guide_digest.php`, `pnl.php` and the frontend — those recompute per request and only ever suffered indirectly. **Changed.** `group_helpers.php` gained the pure core: `groupBucketKey()` (`product_id|YYYY-MM-DD|HH:MM`), `normalizeGroupTime()`, `buildGroupBuckets()`, `splitBucketByPax()` (the PAX-cap logic, moved unchanged) and `matchDesiredToExistingGroups()`, which hands each desired departure the existing group row it shares most members with (a row is claimed once; ties go to the lower id, so two runs over unchanged data decide identically). `autoGroupAfterSync()` no longer detaches first: it reads the current membership, computes the desired departures, reuses the matching row, writes **only** the tours whose group actually changes, updates a group row only when one of its values differs, detaches only tours no departure wants, and deletes a group only when its departure has no members left. Manual merges are still never touched. Guide inheritance (3.5/3.6a) runs only for groups that were created or gained members, so an unchanged run writes nothing at all. The result now reports `groups_reused / groups_updated / groups_deleted / tours_moved / tours_detached / rows_written`. New `tour_groups.bucket_key` (self-provisioned + `database/migrations/20260920_group_bucket_key.sql`), indexed, **not unique**: the per-product PAX cap legitimately splits one departure into several groups (production has two 14:30 groups of product 961801 on 2026-08-12), so uniqueness would be wrong. **Item 5 — overrides stop depending on a surrogate id. Chose (a)**: keep `tour_unit` as the handle and store `bucket_key` beside it (`pnl_tour_costs.bucket_key`, backfilled). (b), migrating `tour_unit` to the natural key, would have rewritten the shared unit string that `payments`, `guide-payments`, `payment-reports`, `guide-tour-report` and the digest all build the same way — a much larger blast radius for the same benefit. The lookup tries `tour_unit` first (so every existing `'g<id>'`/`'t<id>'` row keeps working) and falls back to `bucket_key` for **group units only**, and only when the bucket resolves to exactly one group: applying a departure's cost to the wrong half of a split would be worse than losing it. Migration stamped 1,098 auto groups and 37 single-tour overrides on production. **Verified.** 23 pure checks (`tools/grouping_check.php`) and **25 end-to-end scenarios against a real database** (`tools/grouping_scenarios.php`, synthetic rows on 2030-02-11/12 with a fake product, cleaned up afterwards): same member set twice → same id and `rows_written = 0` (three times in a row); one booking added → same id, exactly one tour moved, `total_pax` 5→7; one booking cancelled → same id, exactly one detached; all bookings gone → group deleted and members detached; a manual merge row byte-identical before and after and its members kept; a new bucket → new group, stable on the next run; a departure over the PAX cap splits in two and the half holding the original members keeps the id; a booking joining an assigned departure still inherits the guide; the P&L override still resolves throughout. vitest 159 green, `npm run build` ok, smoke 12/12 on both. **Staging, three consecutive syncs, no Bokun change:** `rows_written 0, groups_created 0, groups_reused 137, tours_moved 0, tours_detached 0` on all three, and the membership snapshots show **0 tours whose group id changed, 0 groups gone, 0 new** (the same measurement gave 359 / 131 / 137 with the old code). One real change (a booking moved to another time) wrote **4 rows** and created/deleted **no** group: the bucket's PAX split shifted by one member; putting the time back restored the membership exactly. **Production, the two cron syncs after the deploy** (18:30 and 18:45 UTC): `Auto-grouped 332 tours into 0 groups` both times (the last old-code run: 332 into **118**), **0 tours changed group id**, 0 group rows changed, duration 10.44 s and 9.50 s (before: 8.2–14.8 s — unchanged). Figures identical before and after: tours upcoming 701/701, total 462/462, unassigned report 567/567, groups upcoming 378/378, P&L July retail €4,717.67 profit €125.45 and September €2,585.80 profit €41.36, payments 88 transactions / €6,859.92; the guide digest for 2026-09-20 still resolves to the same 6 guides. **Not done, waiting on the owner (item 4):** none of the 10 orphaned overrides can be re-pointed safely. Every one of their dates carries several departures today (8, 5, 6 and 5 respectively), so no bucket resolves to exactly one candidate, and the row stores only `tour_unit` + `date` — the product and time that would identify the departure were never recorded. The affected rows are saved at `~/backups/pnl_orphans_prod_20260920.tsv` (mode 600). Proposal: **run no UPDATE**; the owner re-enters the five that carry real values in the P&L page if they still matter (2026-07-09 €50/€200/€294, 2026-07-10 2×€175, 2026-07-19 two outsourced flags, 2026-08-12 two guide_cost=0), and `g717223` — which holds no amounts at all — can simply be deleted. From this step on it cannot happen again. Notes: (a) the `bucket_key` is deliberately non-unique, see above; (b) a P&L GET never writes — the bucket_key fallback resolves an override without healing the row, so an old row is repaired the next time it is saved, not by a read; (c) **the duplicate hPanel cron entry is gone**: `sync_logs` shows 96 cron rows over 96 distinct minutes in the last 24 h = exactly 1.00 runs per 15-minute slot, so there is nothing left to delete (the pairs seen in 4.0/3.4 have stopped).
- **3.8 Finish products-table filtering in `guide-payments.php`; amount validation `is_numeric && > 0`.** **Verify:** Borghese ticket no longer appears in Pending Payments; POST payment with `amount:"abc"` → 400. — [x] DONE 2026-09-20 (step commit 070c199, merge 8118868; live on staging + production). **Measured first:** `guide-payments.php` decided “guided tour or museum ticket?” in two different ways — seven WHERE blocks using five title keywords, two using `products.product_type`. Over all tours the two rules disagree on **2,580 bookings**, all of them product **961802 (Uffizi Gallery Reserved Ticket + Digital Audio Guide)**: a ticket in the products table whose title matches none of the keywords, so the keyword filter waved every one of them through as a guided tour (examples: tours 7217 / 7213 / 6039, all 2026-09-19). In the other direction the rules never disagree (**0** rows), so moving to the products table cannot hide a real tour. **Wrong rows visible in Pending Payments today: 0** — none of those 2,580 ticket bookings currently carries a guide, so the fault was latent, not something the owner could see; it becomes real the moment a guide is assigned to one, which the UI allows. Production before/after: 1,201 pending rows either way, €6,859.92 over 88 transactions unchanged. All seven blocks now use the products table (0 title keywords left, 9 `product_type` filters). **Amount validation:** new `public_html/api/payment_helpers.php` holds `paymentAmountError()`, used by both payment writes. Before, POST let `"abc"` through — `empty("abc")` is false and in PHP 8 `"abc" <= 0` compares two strings and is false — so it reached the prepared statement and was bound as **0.00**; `amount: 0` was reported as a *missing field*; and PUT silently skipped a bad amount instead of rejecting it. Observed on staging now: POST `"abc"` → **400** “Amount must be a number greater than 0”, `0` → 400, `-5` → 400, `""`/`null` → 400 “Amount is required…”, `45.50` → **201** created; PUT `"abc"`/`0`/`-5` → 400 each, `51.25` → 200 updated (the test payment was deleted again; 88 rows / €6,859.92 restored). 22 checks in `tools/payment_amount_check.php`. The payment model, 1 group = 1 payment and the 409 duplicate rule are untouched. **`payment-reports.php`** now has the self-healing guard `guide-payments.php` got in 4.1a, for `monthly_payment_summary` (left outstanding there): on MySQL 1146 for that view only it is recreated once from `database/migrations/20260920_monthly_payment_summary_view.sql` (definition captured from the live view, DEFINER dropped) and the request retried; every other error is rethrown. The migration was **not** run — the view is healthy on both environments and running `CREATE OR REPLACE` would only change its DEFINER.
- **3.9 Sync mutations to POST + date validation; delete legacy `autoAssignGuide`, `BaseAPI`, `Logger`, `Validator` duplicate, `HttpClient`.** **Verify:** php -l, smoke test. — [x] DONE 2026-09-20 (step commit 03fc3d4, merge 8118868; live on staging + production). **Sync mutations are POST.** `GET action=sync` / `full-sync` / `backfill-names` now answer **405** with `Allow: POST` and a message instead of changing data on a GET (observed on staging: 405 for all three; `sync-info`, `sync-history` and `config` still 200 on GET). `POST action=sync` validates its window: `2026-02-31` → 400 “not a real date” (`createFromFormat` would otherwise shift it to 2026-03-03), `20-09-2026` and `abc` → 400 “must be a date in YYYY-MM-DD format”, `2099-01-01` → 400 “outside the supported range (2015-01-01 .. +5 years)”, start after end → 400. **Callers:** `src/services/bokunAutoSync.js` was the only GET caller (it backs the Dashboard and Bokun-page “Sync Now” and the Tours “Refresh”) and now POSTs `{type, triggered_by}`; its test asserts the POST and that no GET is made. `mysqlDB.js` (`sync`, `full-sync`) and `BokunSync.jsx` (`sync`, `config`, `auto-assign`) already used POST; `sync-history` / `sync-info` stay on GET. **Deleted, each with zero references proved by grep first:** `public_html/api/BaseAPI.php` (0 references anywhere), `Logger.php` (used only by BaseAPI — the `SentryLogger` hits are a different class), `Validator.php` (used only by BaseAPI; the live `class Validator` is the one in `Middleware.php:377`, so this file was the duplicate the plan meant), `src/utils/bokunDataExtractors.js` (0 importers), `src/components/CardView.jsx` (0 importers), `TourCards.jsx` (imported only by CardView), `UnpaidToursModal.jsx` (0 references), `UI/TourCard.jsx` (0 importers — the other `TourCard` mentions are a different component exported from `UI/Card.jsx` and a local one in `GuideRespond.jsx`), `UI/StatusBadge.jsx` (0 references), and `twilio_reminders.php`. The four API files were also deleted from both servers by hand, because `deploy.sh` has no delete scope for `api/` and would have left them lying there. **NOT deleted, because they are referenced** (the plan text was stale): `HttpClient.php` — `BokunAPI.php:2,137` uses it as the fallback when cURL is unavailable; and `autoAssignGuide()` — live, `BokunSync.jsx` POSTs `action=auto-assign` from two buttons on the Bokun Integration page. **The 60-minute reminder machinery** (3.10 said “delete it in 3.9”) is gone: `reconcileGuideReminders()` and its three call sites (after a sync in `bokun_sync.php`, on a guide assignment in `tours.php` and `tour-groups.php`) are removed. The digest needed three functions out of that file, so `normalizeWhatsapp()`, `twilioPost()` and `twilioDryRun()` moved verbatim into the new `public_html/api/twilio_helpers.php`; `guide_digest.php` requires that instead. The digest itself is untouched and still builds (staging preview for 2026-09-22: 2 messages, 3 departures, 8 bookings). The `guide_reminders` table is left alone (354 canceled rows of history). **Verified.** php -l on every API file; 30 digest + 23 grouping + 22 amount + 16 sync-cache checks green; vitest 159 green; build ok; smoke 12/12 on both. Figures identical before and after on both environments — production: tours upcoming 701/701, total 462/462, unassigned 567/567, groups 379/379, pending payment rows 1,201/1,201, payments 88 / €6,859.92, P&L July €4,717.67 / €125.45 and September €2,585.80 / €41.36, monthly report and guide reports both 200; staging identical too, plus Payments and Daily P&L rendered in the browser with the same numbers. A real CLI sync on staging (820 bookings, 6.46 s) and the first production cron after the deploy (**9.81 s, 820 bookings, `Auto-grouped 333 tours into 0 groups`**) both completed normally. Note: `bokun_cron.php` still logs `PHP Warning: Undefined array key "REQUEST_METHOD" in config.php:250` on every CLI run (first seen in 2.4 note (b)) — one line to guard, left alone here because it is outside this step.
- **3.10 Guide reminders: one per guide per departure + nightly digest — [x] DONE, LIVE 2026-09-19 (details below).** (a) Reminders are keyed on `(guide_id, date, HH:MM)`, never on `group_id` or `tour_id`: a departure with several bookings or several groups sends ONE message per guide. (b) Replace the 60-minute reminder with a nightly digest at 21:30 Europe/Rome per guide listing every assigned departure for the next day, one line per departure: `HH:MM · tour name · N pax` (+ `Private` when `is_private`), built from that guide's groups (two groups at the same time with two guides produce two separate digests). (c) Requires a new Meta-approved WhatsApp template with 3 variables (guide first name, date, multi-line list) — proposed name `guide_daily_digest_it`, language `it`, category UTILITY, body `Ciao {{1}}, ecco i tuoi tour di domani {{2}}:\n{{3}}\nBuona serata!`; Content SID goes into the credentials file under "Twilio digest template", never into the repo. (d) The old 1-hour reminder stays active until the new template is approved and the digest has run correctly on staging in dry-run for 3 nights. (e) **Verify:** on staging with `TWILIO_DRY_RUN=true`, a guide with 3 bookings at 09:00 → exactly 1 scheduled message; a guide with two next-day tours → one digest containing both lines; a guide with no tours → no message. **Rollback:** keep the 1-hour reminder code path; feature flag `TWILIO_DIGEST_ENABLED` off.
  **â [~] PART 1 BUILT AND DEPLOYED 2026-09-18, DORMANT, WAITING AT THE OWNER'S SAFETY GATE** (step commits e884970 + 29b0899, merges 81dc68f + f594f34; live on staging + production but **sending nothing**: `DIGEST_LIVE` is absent â false, so the sender refuses every destination except the test number before any Twilio call, and no cron entry exists yet). Built: `public_html/api/guide_digest.php` â `collectDigestDepartures()` (one row per DEPARTURE / tour unit, effective guide as in 3.5, cancelled bookings and ticket products excluded, PAX = SUM(participants) per unit), pure `buildDigestLines()` / `digestItalianDate()` / `digestIsSendWindow()`, `sendGuideDigests()`; `guide_digest_cron.php` (CLI-only, `--date` / `--to` / `--guide` / `--dry` / `--any-time`); `guide_digests` table (self-provisioned + `database/migrations/20260918_guide_digests.sql`), UNIQUE `(guide_id, digest_date)`, status `pending|sent|failed|refused_not_live`, `sent` is final and `attempts >= 2` on a failure is never retried again (no 15-minute loop); `tools/digest_preview.php` (prints what each guide would receive, contains no Twilio call) and `tools/digest_check.php` (30 pure checks). Line format `HH:MM Tour name Â· N pax` (+ ` Â· privato`); **same-start-time disambiguation chosen: ` Â· gruppo A` / ` Â· gruppo B`** in departure order â a numeric group id would mean nothing to a guide and there is no meeting-point column. Long titles are trimmed at 70 chars and the list is capped at ~900 chars with "â¦ e altri N tour" (WhatsApp body limit). **The server clock is UTC but the digest must go at 21:30 Europe/Rome**, so the cron fires twice (19:30 and 20:30 UTC) and `digestIsSendWindow()` lets through the run that is 21:30 in Rome â correct in summer and winter, and the job is idempotent anyway. Cron line for hPanel â Advanced â Cron Jobs, schedule `30 19,20 * * *`: `/opt/alt/php82/usr/bin/php /home/u803853690/domains/deetech.cc/public_html/withlocals/api/guide_digest_cron.php >> /home/u803853690/logs/guide_digest.log 2>&1`. Measured before (production): reminder rows created in the last 7 days 44 over 43 departures = duplication 1.02 (30 d 1.06, 90 d 1.08; all-time 21 departures got 2 messages and 2 got 3), worst single guide-day **5 messages**, 323 rows `canceled` of which 321 after their send_at (the missing `sent` status), 0 stuck / 0 failed rows, 33 Twilio scheduled messages in the future, **0 active guides with an unusable phone** (all 30 validate). Tomorrow on production the digest would be **4 messages instead of 5 per-departure reminders (9 bookings)**. Staging proofs (dry run): a group with 3 bookings â exactly 1 line; a guide with 2 departures â 1 message with 2 lines; 4 guides on one date â 4 messages, each only its own departures; a guide with no tours â no row and no message; re-run immediately â `already_sent: 4`, same `sent_at`, still 4 rows; with `DIGEST_LIVE=false` â `refused_not_live: 4` and no Twilio call at all; the cron entry point outside the window prints "outside the 21:30 Europe/Rome window" and does nothing. 30 pure checks + vitest 151 green, smoke 12/12 on both. **Still owed (owner gate):** (a) the Content SID of `guide_daily_digest_it` is **missing everywhere** â not in `~/.florence/credentials.md` (there is no "## Twilio digest template" heading and no `HXâ¦` anywhere) and not in either server `.env`; it must be added as `TWILIO_DIGEST_CONTENT_SID` before any send. (b) `DIGEST_TEST_NUMBER` (or `--to=`) for the approval test â the number is in the credentials file under "## Digest test number". (c) after "go live": set `DIGEST_LIVE=true` on production (owner edits the env), add the cron entry, then **remove the old ~60-minute path** and cancel the 33 future Twilio scheduled messages it created. Until then the old reminder keeps running unchanged. **Approval test done 2026-09-18 23:38 Rome (owner authorised one env line + "send test"):** the owner added the Content SID to `~/.florence/credentials.md`; it was appended as `TWILIO_DIGEST_CONTENT_SID` to `~/env/withlocals/.env` (backup `~/backups/env_withlocals_pre_digest_sid_20260918_213129.env`, mode 600 kept, +61 bytes / +1 line, value md5 verified equal to the source, nothing else touched); `health.php` 200 and the sender reports "digest template configured: yes". First test send to the test number → Twilio **error 21656**, message `failed`, nothing arrived: **a WhatsApp template parameter may not contain a newline**, and the approved template's own sample for `{{3}}` is comma-joined → default separator changed to `", "` (commit ee16bb3; `DIGEST_LINE_SEPARATOR` still overrides). The same test exposed a reporting bug worth more than the typo: Twilio answers the create call `201/accepted` and fails the message later, so `guide_digests` said `sent` for a message that never arrived → `digestVerifySent()` now re-reads every just-sent message and downgrades `failed`/`undelivered` rows to `failed` with the Twilio error code (stats `verified` / `failed_after_send`). Retry to the same number → `sent: 1, verified: 1`, Twilio status **delivered**. Test rows deleted afterwards (`guide_digests` is empty again) so tomorrow's real digest for that guide is not suppressed. **Readability note for the owner:** with the newline ban, a guide with several tours gets them on ONE line separated by `", "` (e.g. `08:30 Uffizi… · 4 pax, 14:00 Uffizi… · 4 pax`). If that reads badly on the phone: `DIGEST_LINE_SEPARATOR=" | "` (one env line) or a new template with one variable per tour line. **GO-LIVE 2026-09-19 14:31 Rome (owner authorised the second env line).** `DIGEST_LIVE=true` appended to `~/env/withlocals/.env` (backup `~/backups/env_withlocals_pre_digest_live_20260919_123105.env`, +1 line / +17 bytes, mode 600 kept, key-list diff = that key only); both cron entries (`30 19 * * *` and `30 20 * * *` UTC) are in place on the owner's side. The ~60-minute reminder is RETIRED: `reconcileGuideReminders()` returns `retired_step_3_10` immediately — no Twilio call, no writes, so nothing new is ever scheduled; its callers (sync, tours PUT, tour-groups, guide-requests) are untouched and the old machinery stays in the file so the step reverts in one commit (delete it in 3.9). Commit e5b0f76, merge 537bd50; smoke 12/12 on both. Twilio cleanup: **28 scheduled messages cancelled, 0 failures** (33 were counted on 2026-09-18; 5 had fired or been cancelled in between), a full 13-page sweep of the account now finds **0** scheduled messages, and the 4 `guide_reminders` rows left in `scheduled` were marked `canceled` (354 rows, all `canceled`). `health.php` 200. Tonight's run (19:30 UTC = 21:30 Rome, 2026-09-19) sends the digest for **2026-09-20: 6 messages for 7 departures from 19 bookings** — the old reminder would have sent 7, one per departure (and 19 if it had been per booking). Note for the owner: guide “Ajantha” (id 16) has a phone ending in the same 4 digits as the digest test number, so that number receives a real guide digest tonight. Deferred on purpose: the ~305 past guide-less rows (3.6a), and deleting the dead reminder machinery (3.9).

---

## Phase 4 — Mobile speed & low-signal use (~2–3 days) — *your #1 pain point*

Why it is slow today (measured from the build and the code): the main JS chunk is **1.47 MB** (jsPDF, datepicker, Sentry Replay all eager, no route splitting); the Tours page requests **500 rows with the full `bokun_data` JSON** for each (multi-MB on a busy month) and the Dashboard fetches that twice; the app cannot show anything until those requests finish, and the service worker deliberately never caches `/api/`. On a weak signal every open is a cold start.

- **4.0 Stop browser-started syncs.** The server cron already syncs every 15 minutes; the in-app startup, focus and periodic syncs only add 100+ long-running PHP workers a day, leave `started` rows in sync_logs and reload the list mid-session. Remove them; keep the manual Sync Now button and the webhook. **Verify:** 24 h after deploy, `sync_logs` has only `cron` and `webhook`/`manual` trigger rows; the Bokun page still shows last sync time from cron; Sync Now still works. — [x] DONE 2026-09-18 (step commit 6829632, merge a117c50; live on staging + production; **24 h check still open** — re-run the `sync_logs` trigger count on 2026-09-19 after 10:40 UTC). Triggers found: `bokunAutoSync.initialize()` (startup after 2 s + 15-min interval), window `focus` / `visibilitychange` listeners, and a second startup + 15-min timer inside the `useBokunAutoSync` hook (mounted twice: provider + Bokun page); all called `GET bokun_sync.php?action=sync`. All removed together with the code they leave behind in `bokunAutoSync.js` (`config`, `startPeriodicSync`, `onAppFocus`, `onVisibilityChange`, `shouldSyncOnFocus`, `lastAttemptTime`, `stop`, `updateConfig`); manual `syncNow()` unchanged (Dashboard, Bokun page, Tours "Refresh"). `sync-info` had no last-sync field at all (the labels showed the browser's own `localStorage` time) → it now returns `last_sync {completed_at (UTC), sync_type, triggered_by}` from the latest completed `sync_logs` row; the hook reads it on mount and at most every 5 min (throttle shared between hook instances). Bokun page texts are informational ("Syncing runs on the server every 15 min"). No schema change: `sync_logs.status`/`sync_type`/`triggered_by` are VARCHAR and already carry `cron`, `webhook`, `manual`. Observed: vitest 124 green (no sync on mount / focus / visibilitychange / 1 h of timers; Sync Now → `action=sync&type=manual`); stuck `started` rows older than 1 h → `abandoned`: staging 2,694, production 3,867; staging browser /tours 104 s incl. tab switch + focus/visibility events = 0 `action=sync`, 1 `sync-info`, Sync Now = exactly 1 `action=sync`, label = server row (2026-09-15T23:52:09Z → "Sep 16, 01:52"); production /tours 75 s = 0 `action=sync`, label "Sep 18, 12:34" = cron row 10:34:17Z, then "12:46" after the next run; production `started` rows in the last hour: 8 before the deploy (4 startup + 4 periodic), 12 at +31 min (11:11 UTC) = 2 `manual` (my one Sync Now click, doubled by the edge retry) + 8 `periodic` + 2 `startup`; of the rows created AFTER the deploy the automatic ones are `periodic` at 10:53:39 and 11:08:39 (exactly 15 min apart = one tab still running the old bundle) and one `startup` at 11:00:04 (each with its +55 s retry twin) — old-bundle clients, they end when those tabs / the PWA are reloaded; everything else since the deploy is cron / webhook / manual; smoke 12/12 on both (one transient `000` on staging row 5, green on re-run). Notes: (a) **Sync Now does not complete on production** — pre-existing, not caused by 4.0: one click produced TWO server runs 55 s apart (rows 17067/17068, 10:42:38 and 10:43:33 UTC) because the hosting edge retries the long GET, both PHP runs were killed before grouping/logging, and the browser got 504 after 110 s; same pattern in older rows (2026-09-17 19:00:59 completed 172 s + 19:01:35 never finished). This is the real origin of most `started` rows and of the "307" note in 1.2 → fix in 3.3/3.9 (POST + short/async manual sync, `ignore_user_abort`); until then cron is the reliable path. (b) Tours "Refresh" still fires a manual sync after reloading the list — a user click, left as is; consider dropping it in 3.3. (c) Browser tabs opened before the deploy keep the old bundle and may log `periodic` rows until they are reloaded. (d) hPanel has two cron entries for `bokun_cron.php`: job `kTTXMCYu9M` runs it with `/usr/bin/php` = **PHP 7.4.33**, job `6OW1I9MDmz` with `/opt/alt/php82/usr/bin/php` = PHP 8.2.33 — owner to delete the `/usr/bin/php` one. (e) `tours.bokun_booking_id` has no index (full scan per booking in every sync) → 3.4.
- **4.1 Code-split.** `React.lazy` per route; dynamic `import('jspdf')` inside the PDF handlers; Sentry Replay sampling 0 in prod / `tracesSampleRate 0.1`; remove `showDialog`. **Verify:** `npm run build` main chunk < 400 KB gzip < 130 KB; Lighthouse mobile performance on staging before/after (record both numbers). — [x] DONE 2026-09-18 (step commit 94c9e1d, merge 1c1b372; live on staging + production). `React.lazy` for all 11 page routes behind one `<Suspense>` + `PageSpinner` (Login, ModernLayout, AuthContext, ProtectedRoute/AdminRoute stay eager); `jspdf` + `jspdf-autotable` behind `await import()` in `utils/pdfGenerator.js` (all four generators are async now) and in `GuideReports.exportPDF`, with a "Preparing…" button state; Sentry `replayIntegration` only referenced in the non-production branch so Replay is dropped from the shipped bundle (`replays*SampleRate 0`, `tracesSampleRate 1.0 → 0.1`, `showDialog` removed from the ErrorBoundary); `vite.config.js` `manualChunks` → `vendor-react` (react / react-dom / scheduler / react-router*), `base: '/'` unchanged. Observed — local build: main chunk 1,471.6 KB raw / 426.3 KB gzip / 355.4 KB br → `index` 236.1 KB raw / 77.0 KB gzip / 65.3 KB br (target < 400 KB raw, < 130 KB compressed ✓); first-paint JS 1,437.1 KB raw / 347.1 KB br → 403.7 KB / 115.1 KB br (index + vendor-react), first-paint CSS 90.5 → 69.4 KB raw (the datepicker CSS now rides with Payments/Tickets/EditTour). Wire bytes for first paint (curl, brotli at the edge): production 421,021 → 138,884 B (−67 %), staging 421,064 → 138,896 B; every asset `Content-Encoding: br` + `Cache-Control: public, max-age=31536000, immutable` (2.3 holds). Lighthouse mobile (simulate, `/login`) on a local `vite preview` of the same build: performance 80 → 98, FCP 3.0 → 1.7 s, LCP 3.2 → 1.8 s, TBT 330 → 120 ms, Speed Index 3.0 → 1.7 s, total byte weight 441 → 150 KiB. Browser walk-through on both environments (login → tours → modal → dashboard → payments → guide-reports → daily-pnl → bokun-integration → guides → tickets → priority-tickets): every route fetches only its own chunk, 0 console errors, 0 CSP violations, 0 asset 404s in the whole session; on staging the PDF export fetched `pdfGenerator` + `jspdf.es.min` + `jspdf.plugin.autotable` only on click; /tours 295 rows staging, 381 production; vitest 125 green (new `src/__tests__/App.lazyRoutes.test.jsx`: Suspense fallback, then the lazy page). Notes: (a) **Lighthouse cannot run against staging or production** — the Hostinger edge (`hcdn`) answers headless Chrome's document request with 403 (`ERRORED_DOCUMENT_REQUEST`) while curl with browser headers gets 200; the numbers above come from a local preview of the identical build, so before/after is comparable. Plain `curl` on `/assets/*` is also 403 unless `Referer` + `Accept` + `Sec-Fetch-*` are sent. (b) What stays in the 236 KB main chunk and why: Sentry ~137 KB raw (init must run before the app renders; Replay removal already saved ~127 KB raw / ~28 KB gzip), axios ~35 KB (AuthContext verifies the token at boot), react-icons ~22 KB, app shell (App/Login/ModernLayout/contexts/services) ~42 KB — deferring Sentry init is the next lever but would lose early errors, so it was left alone. (c) `src/pages/Payments.jsx` still defines `downloadReport()` which nothing calls — left untouched. (d) The service worker needed no change: it precaches only `/` and `/index.html` and caches chunks as they are fetched, so new chunk names can never 404 (verified: 29 cached entries, all 200). (e) **Found during the production walk-through, NOT caused by this step:** `GET /api/guide-payments.php` (no action) returns HTTP 500 — `Table 'u803853690_withlocals.guide_payment_summary' doesn't exist` — so the Payments page shows "Failed to load guide payments" on production (staging is fine). Same error logged 2026-09-17 21:44, before this deploy, and step 4.1 changed no PHP; `database/migrations/fix_guide_payment_summary_view.sql` exists → needs its own step/fix.
- **4.1a Hotfix: payment views missing on production.** `GET /api/guide-payments.php` answered 500 (`guide_payment_summary` doesn't exist) so the Payments page showed "Failed to load guide payments". Recreate the view, find out why it vanished, add a guard. — [x] DONE 2026-09-18 (commit 032e5e8 on master; live on staging + production). Production had NO views at all: `guide_payment_summary` (guide-payments.php) and `monthly_payment_summary` (payment-reports.php monthly report) were both gone; both recreated with `CREATE OR REPLACE VIEW` from staging's `SHOW CREATE VIEW` (= `database/migrations/fix_guide_payment_summary_view.sql` for the first one) — no table or data change. Observed: `guide-payments.php` 500 → 200 with 30 rows (sum received €6,859.92), `?guide_id=` 200, `payment-reports.php?type=monthly` 200; browser /payments renders with figures (Guides tab 30 rows), /daily-pnl and /guide-reports load, 0 console / API errors. **Dropped, not "never created":** both views are in the two production dumps of 2026-09-10 21:20 and 21:56 (definer = the production app user) and were gone by 2026-09-17 21:44 (first logged 1146). Nothing in the repo, `deploy.sh`, cron, sync code or the server-side scripts drops or recreates views (the only `DROP VIEW` statements are in hand-run `.sql` files: `create_payment_views.sql`, `payment_system_migration*.sql`, the migration above — none is deployed or executed by code); table CREATE_TIMEs rule out a dump re-import; no binlog access and the PHP error log only exists since 2026-09-17 01:58, so the exact moment cannot be proven. The only database-level events in that window were the step 0.3 credential work on 2026-09-16 (production DB password changed in hPanel, old DB users / leftover DB deleted in hPanel) — the most likely cause, unproven. Guard: `guide-payments.php` wraps its dispatcher; on `mysqli_sql_exception` 1146 naming `guide_payment_summary` (and only then) it logs one line, runs `ensureGuidePaymentSummaryView()` (`CREATE OR REPLACE VIEW`) and retries once — every other error is rethrown to the existing 500 handler. Proven on staging: view dropped → 200 with 27 rows, view present again, exactly one log line; the `?guide_id=` path recovers too; smoke 12/12 on both. Notes: (a) `payment-reports.php` has no such guard for `monthly_payment_summary` (not asked for here) — add with 3.8; (b) the root-level `create_payment_views.sql` / `payment_system_migration*.sql` still contain `DROP VIEW` / `DROP TABLE` and should move to `database/` or be deleted (3.9); (c) after any hPanel database operation, check `SELECT TABLE_NAME FROM information_schema.VIEWS` on production.
- **4.1b Hotfix: a failed route chunk showed the red error screen.** Reported from the owner's phone 2026-09-20 07:38 UTC (Sentry issue 148176284, Mobile Safari 26.6.1 / iOS 18.7, production, `fwl@0.0.2`): tapping Tours gave “Something went wrong”, `TypeError: Importing a module script failed.` — a lazy route chunk from 4.1 failed to load. **Verify:** unit test for the retry helper; on staging a chunk that fails must recover instead of showing the error screen; a tab left open across a deploy must land on the right page. — [x] DONE 2026-09-20 (step commits c5eb5d7 + 12f842b + 32e337e, merge f424c36; live on staging + production; triage note `docs/triage/2026-09-20-tours-module-import-failed.md`). (Tracked as **4.1b**, not 4.1a: that id belongs to the 2026-09-18 payment-views hotfix.) **Which cause it was.** Two were plausible. **B — stale chunk after a deploy: ruled out**, with evidence: `deploy.sh` really did delete the previous release's `assets/` (delete scope `assets` in the remote sync helper), but production `index.html` still pointed at the exact file the phone was running (`index-BzSOUwhB.js`), **all 12 chunks it references were present** (checked name by name, 0 missing), every asset was dated 2026-09-18 20:19 UTC with no frontend deploy since (both 3.4 deploys reported `0 updated, 42 unchanged, 0 removed` for the frontend — backend only), and `GET /assets/Tours-B-YVitPs.js` answered **200, 66,744 B, 0.41 s** with a valid JS MIME type. That leaves **A — a dropped request on a weak mobile link**: `React.lazy` had no retry and React caches the rejected promise, so one lost request kills that route for good and “Try Again” (`resetError`) replayed the same rejection. A cannot be proven after the fact (there is no client-side network log), so it is the surviving hypothesis, not a proof — and the fix covers both. **Two more causes found by actually breaking a chunk on staging**, neither visible in Sentry: **C** — a missing hashed asset does not 404, the SPA rewrite answers `index.html` with **HTTP 200** (`GET /assets/nope-does-not-exist-123.js` → 200 `text/html`), and `sw.js` cached every 200 under the requested URL, so the chunk URL ended up holding HTML and every later import failed with “Expected a JavaScript-or-Wasm module script but the server responded with a MIME type of text/html” — permanently, cache before network. Observed: the tab still showed the error screen after the file was restored and served 200 `application/x-javascript`. **D** — `Header **always** set Cache-Control "…immutable"` for `/assets/` also stamps the error header table, so a failed response for a chunk URL (a 5xx, or the host's own `403 “Checking your browser”` challenge — both happened during this verification) is cacheable **for a year**. Proven in the browser afterwards: a normal `fetch` of that chunk returned the cached **503 `text/plain`**, while `fetch(url, {cache:'reload'})` and a cache-busted URL both returned **200 `application/x-javascript`**. A device poisoned that way cannot be healed by retrying or reloading. **Changed.** (1) `src/utils/lazyWithRetry.js` — one helper for all 11 lazy routes (no route-specific code): retries the import at 300 ms and 900 ms, then reloads the tab **once**, guarded by a `sessionStorage` flag that a successful chunk load clears; unusable storage (private mode) means never reload, so a loop is impossible. (2) `ErrorFallback` “Try Again” now reloads the page instead of `resetError()` (which could never fix this class of error) and adds a line about checking the connection. (3) `scripts/deploy.sh` — retiring a hashed asset takes **two** deploys: files this deploy orphans are kept and listed in `.deploy-assets-retained.log` (denied by the site `.htaccess`, which blocks `*.log`), and deleted only by the next deploy that still does not want them. (4) `public/sw.js` — never stores and never serves an HTML response for an `/assets/` request, drops an already-poisoned entry, `CACHE_VERSION` → `fwl-v2`. (5) `public/.htaccess` — `Header set` instead of `Header always set` for the immutable rule. **Verified.** 8 new vitest cases (resolves after one failure; the 300/900 backoff order; gives up after N, reloads **exactly once** and still rejects; never reloads twice in a tab; a success clears the flag so a later failure gets its own reload; blocked storage never reloads), suite **159 green**, build ok, `bash -n deploy.sh` ok. Staging, chunk deleted under a running tab: three real import attempts (three MIME errors for the same URL in the console), then **one** automatic reload that preserved the path (`/daily-pnl`), and with the file still missing it degraded to the error screen without looping. Staging, retention: after a deploy that orphaned 20 files (`20 kept-one-more, 0 removed`), the **old tab** (still `index-DMPCeiy9.js`, no reload, flag clear) opened Payments and loaded `Payments-CLc93gIl.js` **200** — a chunk the newest build no longer references; before this change it would have been deleted. Staging, stale build: a second deploy removed the old tab's Guide Reports chunk (`removed stale: assets/GuideReports-Cq2-ark-.js`); clicking Guide Reports in that same old tab **reloaded itself once, came back on the new entry (`index-CB6ey7gK.js`), loaded `GuideReports-D2sQTFD8.js` 200, landed on `/guide-reports` with the page rendered, and cleared the flag** — no error screen. Header check after the `.htaccess` change: a real chunk still answers `public, max-age=31536000, immutable`, a missing one answers `no-cache, no-store, must-revalidate`. Smoke 12/12 on both environments. Production deploy kept 20 old files, including `index-BzSOUwhB.js` and `Tours-B-YVitPs.js` — the exact build the owner's phone was running this morning still loads. Notes: (a) a device poisoned by cause D **before** this deploy stays poisoned for that URL until its site data is cleared — but every frontend deploy changes the entry hash (and the hash of any page that changed), so those URLs stop being referenced; the owner's phone is already past it. (b) The retry cannot bypass an HTTP-cached failure, because `import()` gives no access to the resolved URL — that is why cause D had to be fixed at the header instead. (c) Service-worker precaching of route chunks would remove the network from this path entirely; that is **step 4.5** (`vite-plugin-pwa` / `generateSW`) and was deliberately not done here. (d) How often this Sentry issue has fired and for how many users is still **unanswered**: the Sentry connector needs an interactive sign-in that this session cannot do, and only the DSN (not an API token) exists in the credentials file or either server env — the owner can read both numbers off the issue page.
- **4.2 Light list endpoint.** `tours.php?view=list` returns only the columns the list needs (no `bokun_data`, no notes) plus embedded group summary; `BookingDetailsModal` fetches `tours.php/{id}` on open. Enable `mod_deflate`/`gzip` verified on JSON. **Verify:** payload for a 300-booking week drops from MB to < 150 KB; modal still shows all sections. — [x] DONE 2026-09-18 (step commit 3aeb65c, merge 88733e5; live on staging + production). `tours.php?view=list` returns 33 whitelisted fields per row (no `bokun_data`); `total_participants`, `start_time_str` and `language` are derived once in PHP (`deriveListFields()` in `tour_classification.php`, mirrors the old `getParticipantCount/getBookingTime/getTourLanguage` browser logic; `pax_adults/children/infants` were already server-side); default output without `view=` is byte-identical (staging md5 before = after). `GET tours.php/{id}` did not exist (the id was ignored on GET) → added `/{id}?view=full` (`{success,data}`, 404 when missing); `BookingDetailsModal` fetches it on open (spinner, falls back to the list row with a notice when the request fails). `Tours.jsx` + `Dashboard.jsx` use `getTours({view:'list'})`; `PriorityTickets.jsx` stays on the default view. Compression needed no change: the Hostinger edge already serves `Content-Encoding: br` on JSON. Observed (`upcoming=true&per_page=500`, raw / brotli, curl): staging 376 rows 3,104,868 / 663,216 B → 352,716 / 46,837 B (1.23 s / 0.53 s → 0.42 s / 0.34 s); production 454–455 rows 3,731,899 / 797,339 B → 431,883 / 57,951 B (1.08 s / 0.53 s → 0.52 s / 0.39 s) = −93 % on the wire; `tour-groups.php?upcoming=true` unchanged (82.7 KB / 13.3 KB br, 50 of 98 groups per page). Same ids in the same order and 0 field mismatches list vs default on 845 staging rows (+500 April rows incl. 73 with NULL `language`) and 455 production rows. Browser: staging /tours rendered text identical before/after the deploy (295 rows, fingerprint equal, 231 tours · 759 PAX · 64 cancelled), dashboard 56 / 250 / 565 unchanged; production same-minute A/B (list payload vs full payload in the same page) 374 = 374 rows, 295 tours · 889 PAX · 79 cancelled, 0 differing rows; modal shows every section from a ~3 KB request; smoke 12/12 on both; vitest 116 green. Notes: (a) `notes` IS in the list view although the step text says "no notes" — the list renders and edits it inline (avg 23 B, 1 of 534 upcoming rows set); there is no `booking_notes` / `meeting_point` column; (b) the local stack is still unusable (no local MySQL access) so the list-vs-default comparison was done on staging; (c) the Dashboard still calls `getTours` twice and the second, unfiltered call is answered from the poisoned `tours_v1` cache (§2.8) — left as is for 4.3, counts unchanged; (d) the `tours_v1` localStorage copy shrank with the payload (3.7 MB → 0.43 MB); (e) `tour-groups.php` still returns only the first 50 groups (§2.9) — 3.5/5.x; (f) `src/utils/bokunDataExtractors.js` has no importers (dead code) — 3.9/5.x; (g) a production page load still logs one 504 from the startup sync (2.3 note d) — 3.3.
- **4.3 Offline-first data.** Replace the localStorage cache with TanStack Query + `persistQueryClient` (IndexedDB): the last successful Tours/Dashboard/Guides data renders **instantly** with a "Updated 12 min ago — refreshing…" banner, then refreshes in the background; `staleTime` 60 s; mutations invalidate. Service worker: keep app-shell precache; API stays network-only (the query cache handles offline). **Verify:** phone in airplane mode → open PWA → today's tours visible with the banner; back online → banner updates; assign a guide with bad signal → optimistic update, rolled back with a toast if the request fails.
- **4.4 Stop unmounting on background sync.** Tours page keeps the list mounted with a small "syncing" indicator; request sequencing with `AbortController`. **Verify:** typing a note while the 15-min sync fires keeps focus and text.
- **4.5 Precache + install prompt.** Vite PWA plugin (`vite-plugin-pwa`, `generateSW`) instead of the hand-written `sw.js`; precache hashed assets so the second open is fully offline. **Verify:** Chrome DevTools "Offline" → app shell loads.
- **4.6 Meeting-point mode (small, high value).** A `/today` route: today's departures, time-sorted, guide, PAX, meeting point, radios needed (Phase 6.3), one-tap call/WhatsApp guide — a single ~10 KB API call, cached offline, shown as the PWA start URL on mobile. **Verify:** loads in < 1 s on 3G throttling with a warm cache; works offline.
- **4.7 Field instrumentation (Phase 4 Part 2A — measurement only).** Part 1 measured the app from a datacenter and could not answer the one question that decides the fix order: on a bad Florence morning, does the owner's phone **stall** (a connection that is accepted and then delivers nothing, which the app waits on forever because it has no timeout anywhere) or is it merely **slow** (eight serial round trips at high RTT)? This step records what his real phone experiences, one row per page load, and changes nothing else — no timeouts, no retries, no auth change, no fallback change, no round-trip reduction; those are Part 2B and must not contaminate the baseline. `src/utils/perfBeacon.js` collects six marks (navigation start, entry script executed, verify start/end, route chunk start/end, list start/end), the outcome of each (ok / failed / **pending** / none), `navigator.connection` (`effectiveType`, `rtt`, `downlink`), `navigator.onLine`, whether a service worker controlled the page, whether this was the first load after a new release, the release, a coarse device string and a 429 counter; `navigator.sendBeacon` posts one row to `api/client_perf.php` → `client_perf`. **Verify:** a normal load writes one row with six sensible timings; a load whose list never returns still writes a row marked `pending`; a 500 from the endpoint changes nothing on screen; a non-admin cannot read the page; an unauthenticated post is rejected.
- **— [x] DONE 2026-09-21** (step commits 38080eb…362 67f1, merge 6781dc4; live on staging + production). **Measurement only — nothing about how the app behaves was touched**: no timeouts, no retries, no auth change, no fallback change, no round-trip reduction. Those are Part 2B and were deliberately kept out so the baseline stays clean. **What is recorded**, one row per page load: the six marks (navigation start = 0, entry script executed, verify start/end, route chunk start/end, list start/end), each phase's outcome (`ok` / `failed` / **`pending`** / `none`), `navigator.connection` (`effectiveType`, `rtt`, `downlink` — null on iOS Safari, which has no NetworkInformation), `navigator.onLine`, whether a service worker controlled the page, whether this was the first load after a new build, the release, a coarse device string (`Android/Chrome 120`, never the full UA) and a 429 counter. The “list” phase covers the whole `Promise.all` the user actually waits for, and only the first load of a page is recorded. **A load that never finishes still reports itself** — the point of the step. Three independent triggers, deduped to exactly one row: a 30-second deadline timer, `visibilitychange`→hidden and `pagehide`. Those last two are the pair Safari documents as reliable (`beforeunload`/`unload` are not); all three were tested in the Chromium-based in-app browser and in unit tests, **not on a real iPhone** — the deadline timer alone is enough even if the other two never fire. **Transport:** one `navigator.sendBeacon` to `api/client_perf.php` → `client_perf` (numbered migration + self-provisioned). It is fire-and-forget by construction, so the endpoint’s outcome is not observable and cannot affect the page — **proved** by making the endpoint return 500 and then hang: the page rendered normally both times, no error screen, no stuck spinner. A trap found by testing rather than reading: an `application/json` body is not CORS-safelisted, so the browser preflights it and a beacon cannot answer a preflight — in the harness the beacon was blocked outright. The body is sent as `text/plain`, which can never be preflighted. **Auth:** the bearer token travels in the body (a beacon cannot set a header) and is resolved with `Middleware::findSessionUser()`, a plain SELECT that **neither creates nor extends a session**; only `user_id` is stored, so a row belongs to the owner or to a guide without holding anything about the person. Unauthenticated POST → 401 quietly (no token and a bogus token both verified), viewer GET → 403, admin GET → 200. **Privacy:** no tour data, no customer data, no names, no free text, no URLs — the route is stored with its identifiers stripped (`/tours/123` → `/tours/:id`, `/respond/<secret>` → `/respond/:token`) and every string is filtered to a narrow character set. **Volume:** 3 users with live sessions × ~20–40 loads/day ≈ **60–120 rows/day**, ~250 B each ≈ 15–30 KB/day, so 60 days of retention is 1–2 MB; retention is a 60-day delete run on ~1 insert in 50, and `idx_client_perf_created_at` is in place on both environments (production table 48 KB at the start). **Reading end:** admin-only `/load-measurements` — one row per load newest first, the three phases as bars on a common 0–10 s scale where a phase that never finished runs red to the edge and says “never finished”, plus connection type, device, “after deploy” and “service worker” badges, date and “did not finish” filters, and a summary line (loads today, median to list, worst, how many never completed). **The test that mattered, on the deployed build with real hanging requests** (a Node stub that accepts the connection and never answers — a single-threaded `php -S` could not do it, because a hang there blocks the beacon too): list hangs → row written, `reason=deadline`, **`list=pending (2131..null)`**, verify and chunk `ok`; auth check hangs (the white-screen case) → row written, **`verify=pending (217..null)`, `chunk=none`, `list=none`**. Both hangs produced a row rather than silence. **Normal loads, staging:** 8 rows, e.g. verify 196–359, chunk 382–610, list 643–954 ms — the same shape Part 1 measured by hand. The post-deploy flag was rewritten mid-step after it failed on real data: it compared `fwl@<package version>`, which we bump rarely, so the staging load right after this step’s own deploy took 3.9 s to the list and was **not** flagged; it now compares the entry script’s content hash, which changes on every deploy, and the flag fires correctly. **Production:** 12/12 smoke, first real row written (verify 706–4946, chunk 4961–17959, list 18941–**26441** ms on the first load after the deploy) — a striking number, but the tab was backgrounded for part of it and Chrome throttles hidden tabs, so it is a first data point, not a clean measurement. **Verified:** 11 beacon tests + 3 page tests, suite **199 green** (was 185), build ok. Notes: (a) `release_tag` holds the package version (`0.0.2`), the build hash is what detects a deploy; (b) staging holds one hand-posted `pending` row used to check the red rendering and the filter — it is fake, production has none; (c) nothing here answers the question yet — that needs several days of the owner’s real mornings.
- **4.8 Mobile fixes 1–3 (Phase 4 Part 2B, first fix step).** The first bad morning (2026-09-23, owner on mobile data: `/tickets` did not load) left no recorder row and a forced re-login at 11:02 while his session was still valid: the auth check hit a network error, the old `catch` deleted the token (Part 1 cause 3) and with it the beacon's row. Fix 2 first — **the token is cleared only by a real 401** from `auth.php?action=verify`; a network error, timeout, 5xx/504 or abort keeps it, the app renders logged in and the check is repeated quietly in the background. Fix 1 — **every request has a timeout** with a plain message and a Retry button (reads 15 s, writes 30 s, auth check 10 s, sync 180 s, server PDF/CSV 90 s, route chunk 20 s, page load 5 s in the service worker then the cached shell); one automatic retry for reads, **never for a write** — a timed-out write (payment POST included) says the outcome is unknown and to refresh first. Fix 3 — **no silent stale data**: `getTickets` no longer returns `[]` and `getTours` no longer falls back to `localStorage` on failure; pages show "Could not refresh — showing data from HH:MM" or "Could not load …" instead of an empty list. Recorder repairs: token captured at page start, every page's own data fetch marked, verify failure reason / timeouts / automatic and user retries / cached-shell start recorded, post-deploy flag not consumed by a token-less load. Not in this step: 4.3 offline-first data, 4.5 precaching, 4.6 `/today`, fix 4 round trips. **Verify:** unit tests for every rule (payment POST timeout → one request + unknown message; tickets failure → banner, never an empty list; verify network error → still logged in); staging with a hanging stub — verify, Tours list, Tickets fetch and page navigation each hang → time to message + a recorder row with the new fields; production P&L July/August/September unchanged to the cent. — [x] DONE 2026-09-23 (step commits 2faddb5 + 34b754e, merge ff4cc4d; live on staging + production). **Token removal, complete list:** (1) `AuthContext` verify non-OK branch — was ANY non-OK status incl. 5xx/504, now 401 only; (2) `AuthContext` verify `catch` — was every network error (the 2026-09-23 cause), now never; (3) `AuthContext` background re-check — clears only on a real 401, then the normal session-expired toast + redirect; (4) `AuthContext.logout()` — the user's own action, unchanged, its server call now has a 10 s timeout; (5) `sessionExpiry.notifySessionExpired()` — only ever called on HTTP 401 by the axios interceptor and `authFetch`, unchanged. No `localStorage.clear()` anywhere. **Timeouts:** auth check 10 s; reads 15 s (+1 automatic retry → message at ~31 s); full tour rows (`tours.php` without `view=list`: Priority Tickets, Edit Tour — 314,469 B br on production) 45 s; writes 30 s, never retried; server PDF/CSV 90 s; every `bokun_sync.php` call 180 s (longer than the edge's ~110 s cut); route chunk 20 s then the one-shot reload; service-worker page load 5 s then the cached shell. **Staging, real deployed build behind a local stub that accepts and never answers:** verify hangs → page rendered logged in at 11.7 s, token kept, row `verify_error=timeout verify_retry=failed timeouts=2`; Tours list hangs → "Could not load the tours list" + Retry at 31.5 s, no "No tours found" under it, row `list=failed 31408 timeouts=2 auto 0/1`; Tickets fetch hangs → "Could not refresh — showing data from 01:01 PM" with the 702 tickets and Retry at 31.7 s, row `list=failed 31619`; page navigation hangs → cached shell at 5.08 s, tickets rendered at 5.76 s, row `shell_fallback=1`. Before this step all four were infinite. **Cached shell × retained assets, proved:** the previous deploy's `index.html` (19b70dd, entry `index-C9KRG6pi.js`, never fetched by that browser) planted as the cached shell with the navigation hanging → entry, `Tickets-3_0ivZu-.js` and 11 more old assets all 200 from the server's retained copies, page rendered 702 tickets. vitest 268 green (was 219), php -l clean, build ok, smoke 12/12 staging (both commits) and production. **P&L:** July 86,419.44 / 5,980.53, August 58,408.87 / −67.13, September 108,208.48 / 6,852.20 (retail / profit, every field of `data.totals`) identical before and after the deploy. Notes: (a) a first after-snapshot showed September +1 unit / +€60 guide cost: departure 809836 2026-09-23 16:30 (GYG bookings 7313 + 7317) was ungrouped at 11:10:52 UTC — before the deploy started at 11:13:09 and not by a sync (no sync_logs row) — and the 11:15 cron regrouped it (group 1508359); cause not investigated here; (b) the in-app browser pane refuses service workers on `http://localhost`, so the navigation case ran in Playwright's Chromium; (c) `Payments.jsx` was committed with CRLF endings and `text=auto` normalised it (whole-file diff, as in 5.3; real change ~110 lines); (d) the Dashboard (`/`, 13 of the 26 recorded loads) got the same treatment although not on the list — it showed zero counters on failure; (e) **left for later** (swallowed errors not on the daily pages): `addTour` / `deleteTour` / `updateTourPaidStatus` / `updateTourCancelStatus` local-cache fallbacks (no callers; the last two also re-send a PUT), `getTourLanguages` → empty dropdown, `getUnassignedCount` → no number, `loadOpenRequests`, Payments `loadGuides` dropdown, Guide Reports guide dropdown, Bokun Integration `getSyncInfo`, Dashboard recent-responses and pending count (shown as 0), `BokunMonitor` plain `fetch` without timeout, `EditTour` treating the paginated `getTours()` answer as an array (looks broken), ticket writes clearing `tickets_cache` while reads use `tickets_v1`.

---
- **4.9 Fix 4 — cut the serial round trips (Phase 4 Part 2B, second fix step).** Two avoidable hops before the Tours list paints: (a) `AuthProvider` rendered nothing until `auth.php?action=verify` returned, so the page and its data requests waited a full round trip; (b) `tourGroupsAPI.listAll()` walked 100-per-page with an `await` in a loop — production has 102 upcoming groups, so a second serial trip. Change: with a stored token AND the cached user (role, name, `pnl_access`) the app renders at once and verifies in parallel; no cached user → waits as before; P&L item only from an explicit cached `true` or the server; 401 → normal session-expired path; network error/timeout → 4.8 unchanged. Groups: one request of up to 500 (server cap 100 → 500), extra pages in parallel. Not in this step: 4.3, 4.5, 4.6, any 4.8 rule. **Verify:** before/after at 300 and 700 ms RTT, 5 runs, /tours and /tickets, median + worst + serial hops; 4.8's four hang cases unchanged; unit tests; P&L July/August/September unchanged to the cent. — [x] DONE 2026-09-23 (step commit 281d992, merge f4a1621; live on staging + production). **Method:** Playwright Chromium, warm client (service worker controlling, logged in, assets cached), through a local proxy that adds a fixed delay to EVERY request incl. the service worker's own navigation fetch (CDP `emulateNetworkConditions` does not reach the SW, so it was replaced): added = target RTT − 150 ms measured base RTT. mysqlDB's 1-min `tours_v1` and ticketsService's 5-min `tickets_cache`/`tickets_v1` were cleared before each load (a morning open is older than both); runs spaced 5.5 s (bokun_sync limiter 10/min/IP); production beacons dropped by the proxy (no test rows in client_perf). Time = first data row in the DOM (MutationObserver injected before any app script); hops = longest chain of non-overlapping network trips before it. **Before → after, median / worst ms (hops):** staging /tours 300: 2186/2697 (3) → 1507/2250 (2); /tours 700: 2970/3100 (3) → 1871/2350 (2); /tickets 300: 1344/1451 (3) → 875/976 (2); /tickets 700: 2525/2674 (3) → 1921/1960 (2). Production /tours 300: 2184/2249 (4) → 1496/1548 (2); /tours 700: 3685/4117 (4) → 2134/2384 (2); /tickets 300: 1299/1543 (3) → 1151/1181 (2); /tickets 700: 2654/2699 (3) → 1824/2120 (2). Staging has 98 upcoming groups (< 100, refreshed by a gated staging sync), so only production showed the second groups trip. One staging-before run hit a real edge 504 and is excluded (4 of 5). **Item 4 (route chunk):** after the change the chunk starts 20–60 ms after the entry script and is served by the SW cache — nothing avoidable left, so no entry-module preload was added. **Item 5 (Sentry):** left alone. The boot request seen on every load is the release-health session ping at `Sentry.init`, to its own host, in parallel, starting ~15 ms before the page's data request and never delaying it; the sampled tracing transaction is only sent after the page goes idle (after the data paint). Deferring either would gain nothing measurable and risks boot-time error capture. **401 glimpse:** a dead token shows the shell for 0.26 s at 300 ms RTT and 0.65–0.85 s at 700 ms, then the session-expired toast + /login; 0 data rows ever rendered (the data calls get 401 too). **4.8 hang cases re-run on staging:** verify hangs → list painted at 0.72 s (4.8: 11.7 s), still logged in after 14 s, token kept, recorder `verify_error=timeout`; Tours list hangs → "Could not load the tours list" at 30.4 s (4.8: 31.5 s), row `list=failed timeouts=2 auto 0/1`; Tickets hangs → message at 30.4 s (31.7 s); navigation hangs → page from the cached shell at 5.2 s (5.08 s), `shell_fallback=1`. **Groups payload:** production upcoming 27.1 + 0.9 KB br in 2 serial requests → 27.6 KB in 1 (0.18 s server); past-40-days 27.9 + 18.8 KB → 46.3 KB in 1 (0.22 s) — far inside the 15 s read timeout. 12 new vitest cases (9 auth, 3 groups) + 2 updated, suite **301 green**, php -l clean, build ok, smoke **12/12** staging + production. **P&L unchanged to the cent:** July 66,261.53 / 60,281.00 / 5,980.53, August 42,723.87 / 42,791.00 / −67.13, September 78,807.51 / 71,931.50 / 6,876.01 before and after. Notes: (a) `getAllGuides()` still pages serially (per_page 100) but production has fewer than 100 guides, so it is one request; (b) the 1-minute tours / 5-minute tickets localStorage caches are Phase 4/5 work, untouched.

## Phase 5 — Frontend correctness (~2 days)

- **5.1** Delete localStorage cache + all "fallback success" branches in `mysqlDB.js` / `ticketsService.js` (§2.8, §2.11) — covered by 4.3's query layer. **Verify:** stop the API on staging → every write shows an error toast, nothing "succeeds".
- **5.2** Groups in date-range view + pagination (§2.9). **Verify:** date range covering 3 weeks shows all groups grouped. — [x] DONE 2026-09-18 (commit d66ad78, merge 54a2ee8; live on staging + production), pulled forward because the on-screen "needs a guide" number was wrong. `tour-groups.php` list: `start_date`+`end_date` and `past` with the same priority and windows as `tours.php` (it already returned pagination metadata and accepted `per_page` up to 100, but the page never asked). Frontend chose **paging** over a bigger page: `tourGroupsAPI.listAll()` requests 100 per page until `has_next` is false and throws when fewer groups arrived than `pagination.total`. `Tours.jsx` no longer swallows a failed group request: tours + groups are cleared and an error card with "Try again" is shown instead of loose rows. The banner is `needGuideCount` = `tours.php?action=unassigned-report&count_only=true` (the 3.5 report's own total, same query, same filters minus the guide filter; hidden while a guide filter is active) — nothing is counted in the browser any more. Observed, banner (count_only) = full report = manual SQL, Today / Upcoming / All / 19 Sep–10 Oct: **staging 5 / 164 / 619 / 88, production 1 / 131 / 579 / 68**; groups loaded = server total in every case (production Upcoming 106 in 2 pages, All 1,268 in 13 pages). The page has no "All" view, so that figure is API-only. Page before → after: staging Upcoming banner 197 → 164, group rows 41 → 64, FULL 4 → 6, bookings represented 380 → 381 (+1 = a new booking); staging range 0 → 45 group rows, FULL 0 → 5, 246 → 247 bookings, banner 146 → 88, "205 tours" → 122. Production Upcoming banner 156 → 131, group rows 42 → 82, FULL 6 → 10, 465 bookings accounted for before and after (232 loose + 232 active in groups + 1 cancelled booking that now sits inside its manual group instead of showing as a loose row); production range 0 → 55 group rows, FULL 0 → 7, 283 = 283 bookings, banner 91 → 68, PAX 558 = 558, "236 tours" → 133. Console clean on both, vitest 151 green, smoke 12/12 on both. Notes: (a) "Upcoming" on the page still stops at +60 days; (b) the "Total: N tours" line is still computed in the browser — correct now that every group loads, but it is a second source; (c) the tours list itself is still capped at 500 rows per page (production Upcoming 465) — close to the cap, belongs to 4.3/5.x; (d) the category chips (Combo / Uffizi / Accademia) are browser-side too.
- **5.3** Batch payment semantics (§2.10): "Amount per tour" + live total; split by each tour's guide. **Verify:** record 3 tours at €50 → three €50 rows, total €150 shown before submit. — [x] DONE 2026-09-20 (step commit 5118f6b, merge 103ca34; live on staging + production). **What it did before:** the Record tab posted one payment per selected departure with the SAME amount each time, so three tours at €50 wrote €150 — and the screen showed neither the words “per tour” nor the number €150 (label “Amount (€)”, button “Record Payment”). When the selection spanned two guides it asked for ONE guide and recorded every tour against that guide, moving one guide's money to another. Each POST wrote exactly one `payments` row for the tour unit (a group posts its first member and the server's group-aware duplicate check keeps 1 group = 1 payment). **Measured on production, read-only:** **0 payments of any kind in the last 90 days** — the newest payment is 2026-04-08, so the requested window is empty. Over all 88 payments there are **14 batch-shaped clusters covering 35 rows** (40% of every payment ever recorded came from a batch), e.g. 2026-03-30 Ajantha 4 × €20.00 = €80.00, 2026-04-08 Caterina Cavalcaselle 3 × €90.00 = €270.00, 2026-04-08 Ilaria Di Gesualdo 3 × €90.00 = €270.00. **No evidence of an accidental multiplication:** each cluster's per-row amount matches what that guide is normally paid for a single departure (Caterina €90.00 vs a usual €89.54, Ilaria €90.00 vs €100.00). The only one worth a second look is Anna Marchetti 2 × €200.00 = €400.00 on 2026-04-08 against a usual €96.25 — the data cannot settle whether that is a per-tour rate or a total typed once; nothing was changed. And **0 payments have ever been recorded against a guide who is not the tour's guide**, so the mixed-guide override never actually mis-attributed money. **Changed:** new pure `src/utils/batchPayment.js` (`buildBatchPlan`, `batchTotalLine`, `batchButtonLabel`, `batchRequests`, `summariseResults`) so the arithmetic is testable without a browser. The field is **“Amount per tour (€)”**; under it a live line **“3 tours × €50.00 = €150.00 total”** that follows both the selection and the amount; the confirm button reads **“Record €150.00”**; when the selection spans several guides an amber box lists the split (one line per guide with that guide's count and subtotal) and **each departure is posted against its own guide** — the guide picker now appears only for departures that have no guide at all, and submit is blocked until one is chosen. The success toast sums what the server reported writing. `createPayment()` now returns the row it wrote (`tour_id`, `guide_id`, `guide_name`, `amount`); one row per tour unit, the group-aware duplicate check, the 409 and `force_group_payment` / `force_payment` are untouched, and the amount still goes through `paymentAmountError()` from 3.8. **Verified.** 12 new vitest cases (3 × €50 = €150 with the exact strings the screen renders; the total follows selection and amount; a two-guide split adds up to the total; a tour is never moved to another guide; an unassigned tour blocks until a guide is chosen; `abc`/0/-5/empty refused; a group counts as one departure), suite 171 green, build ok, smoke 12/12 on both. **Staging, real batch through the browser:** with three Caterina departures selected the screen read “Amount per tour (€)”, “3 tours × €50.00 = €150.00 total” and “Record €150.00”; adding a fourth tour belonging to Anna Sgobbi changed it to “4 tours × €50.00 = €200.00 total”, “2 guides — see the split below” and a split of Anna Sgobbi 1 × €50.00 = €50.00 / Caterina Cavalcaselle 3 × €50.00 = €150.00. Submitting the three wrote exactly **three €50.00 rows** (payments 91/92/93, tours 5646/6928/6185 in three different groups, all to Caterina; 88 rows €6,859.92 → 91 rows €7,009.92), and the three test rows were deleted again (back to 88 / €6,859.92). **Production, read-only check (nothing submitted):** selecting three departures at €50 showed “Amount per tour (€)”, “3 tours × €50.00 = €150.00 total”, “Record €150.00” and, because those three happen to have three different guides, the split Ajantha €50.00 / Amanda €50.00 / Caterina Cavalcaselle €50.00. Production payments unchanged at 88 rows / €6,859.92, max id 88.
- **5.4** Remove the `!important` block from `index.html`. **Verify:** Payments overview shows 4 cards in a row on desktop, 1 column on phone.
- **5.5** One sync engine with `destroy()` on logout; `lastSync` only on success. **Verify:** failed sync shows "Last sync failed at …", retries on next focus.
- **5.6** `parseYmd()`/`formatYmd()` helpers replacing every `new Date('YYYY-MM-DD')` and `new Date(date+' '+time)`; ESLint `no-restricted-syntax` rule. **Verify:** vitest with `TZ=America/New_York npm run test:run` — dates unchanged; dashboard sorted on iPhone Safari.
- **5.7** Dashboard counts per departure, drops keyword ticket filtering (§3). **Verify:** Dashboard "needs guide" equals Tours summary "need guide".
- **5.8** Delete dead frontend code (`EditTour`, `BokunMonitor`, `bokunService`, `localStorageDB`, `dateFormatting`, `bokunDataExtractors`, paid/cancelled helpers). **Verify:** build green, no route 404s.

---

## Phase 6 — Features (~1 week)

### 6.1 Language filter
- Backend: `tours.php` and `tour-groups.php` accept `language=` (exact match on `tours.language`; group language = `GROUP_CONCAT(DISTINCT language)`); `GET tours.php?action=languages` returns distinct values for the current range. Backfill `tours.language` from `bokun_data` for rows where it is NULL (the extraction logic in `Tours.jsx:220-273` moves into `BokunAPI.php` so it runs once at sync time, not per render).
- Frontend: language dropdown next to the guide filter; chip on each card; included in the unassigned report and `/today`.
- **Verify:** filter "Spanish" shows only Spanish departures; a mixed group shows "EN/ES"; count matches SQL.
- **— [x] DONE 2026-09-21** (step commit 8797cca, merge e011119; live on staging + production; backfill run on production after the owner's confirmation). **Audited first (production, read-only):** 7,252 tours, 667 upcoming. 6,813 carry a language, 439 are empty — and **every empty row is in the past**, so the upcoming list was already complete. The values are **not** messy: exactly five spellings (English 6,283 · Spanish 189 · Italian 133 · German 118 · French 90), correct case, no codes, no stray spaces, no free text. **So the normalisation was a no-op: 0 rows changed.** The mapping table is five identity rows. `tourLanguageCanonical()` (tour_classification.php) exists as insurance — it folds EN/eng/inglese → English, es/espanol/spagnolo → Spanish, fr/francais/francese → French, de/deutsch/tedesco → German, it/italiano → Italian, pt → Portuguese (plus ru/zh/ja), keeps an unknown language in one predictable shape, returns null for nothing usable, and is idempotent. The sync writes through it (`BokunAPI::transformBookingToTour`) and so does `deriveListFields`, so a new channel sending “EN” can never become a sixth entry in the dropdown. **Backfill:** of the 439 empties, 17 could be derived from the stored payload (16 English, 1 Spanish); **422 stay Unknown** and are filterable as such. Run on staging, then on production after the owner said yes; the affected ids were saved first to `~/backups/step61/prod_language_rows_20260921.tsv` (mode 600). Production now holds English 6,299 · Unknown 422 · Spanish 190 · Italian 133 · German 118 · French 90. **Backend:** `tours.php` and `tour-groups.php` accept `language=` (the literal `Unknown` selects the rows that have none); a group matches when ANY member matches — that is what makes a mixed departure appear under both languages — and every group now carries its language SET (e.g. `English, Spanish`, or `Unknown`) built from the member rows it already loads, with no extra query. `GET tours.php?action=languages` returns the languages in the range in view with a count of departures and bookings, deliberately ignoring the language filter itself so a choice can never empty the dropdown. The 3.5 unassigned report now carries the language per departure and prints it: `  10:30  Uffizi (English, Italian)`. **Frontend:** a “Filter by Language” dropdown beside the guide filter, reloaded exactly like the other filters; the table chip shows `Unknown` in grey instead of an empty pill; the mobile card already had the chip (4.2) and is unchanged. The browser-side `getTourLanguage()` that re-parsed `bokun_data` on every render is **deleted** — since 4.2 the list does not even carry `bokun_data`, so its fallbacks could never fire; the page reads `tour.language`. The Radios page is untouched. **Verified.** 44 checks in `tools/language_check.php`, 5 new vitest cases (dropdown offers only what exists in range; a choice is sent to the server, never filtered in the browser; `Unknown` is a real choice; the default sends nothing; a row with no language shows the Unknown chip), suite **176 green**, build ok, smoke 12/12 on both. **Staging, API vs SQL:** English 434/434, Spanish 2/2, Italian 6/6, Unknown 0/0 — all MATCH. **Production:** English 440/440, Italian 7/7, Spanish 2/2 — all MATCH. A mixed group was confirmed to appear under both of its languages (`language=Spanish` returns groups reading `English, Spanish`). Note: the first API-vs-SQL comparison looked wrong (English 434 vs 453) purely because my SQL used MySQL's `CURDATE()`, which is **UTC** while PHP runs Europe/Rome (the split made deliberately in 2.1) — at 01:06 Rome that is the previous day. Comparing with the same Rome-based window gives an exact match. Worth remembering for any future count check.

### 6.2 Merged-tour cost in Daily P&L
- Today each P&L unit is `g<group_id>` or `t<tour_id>` (`pnl.php:46`). Add a `pnl_unit_links` table (`date, unit_a, unit_b, created_by`) keyed by **natural keys** (`product_id|date|time` — depends on 3.7) so a link survives syncs. When two units on the same day are linked, `pnlBuildRows()` emits **one** combined row: revenue = sum, ticket cost = sum, **guide cost = one guide at the highest applicable rate**, radios = combined PAX + 1 transmitter, gelato = combined. Unlink restores two rows.
- UI: in Daily P&L, select two rows on the same day → "Merge for costing" → combined row with a "merged" badge and an unlink button.
- **Verify:** two 4-PAX Uffizi tours at 09:00 and 09:15 merged → one guide cost, 8 radios + 1 transmitter, revenue = both; day total drops by one guide fee; still merged after a sync.
- **— [x] DONE 2026-09-20** (step commit fd3dc65, merge 551102b; live on staging + production). **Checked before building:** manual merge already does this and the owner already uses it — **131 of the 181 manual merges on production span more than one product**, and `pnl.php` costs such a group as one unit under the Mixed rule (highest member rate). What manual merge cannot do is leave the departures separate everywhere else: it makes them one group in Tours, one payment, one digest line, and it is capped by the per-product PAX limit. 6.2 is the P&L-only version, which is what the owner asked for. **Data model:** `pnl_unit_links` (self-provisioned + `database/migrations/20260920_pnl_unit_links.sql`) — one row per member departure, rows sharing `link_key` are one costing unit, `tour_unit` UNIQUE so a departure belongs to at most one link, `link_date` per link, `bucket_key` carried beside `tour_unit` exactly as `pnl_tour_costs` does since 3.7. Never created automatically. **P&L:** `pnl_links.php` folds the members into **one row** — chosen over “two rows in a marked block” because `pnlTotals()`, the category tiles and the day sections all sum rows, so one row keeps every total correct for free — carrying a `merged` block with the per-product breakdown (title, PAX, revenue, ticket cost, and what that departure's guide fee would have been alone). Guide fee = the existing precedence applied to the merged unit as a whole (outsourced > ticket > private > highest category rate) and never less than the dearest member on its own; `guide_rule` states which rule won. Tickets, radios, gelato, staff, other, revenue and PAX add up; an outsourced member means ONE handling fee. Overrides: an override on the merged row (`m<id>`, the override key regex widened to `[gtm]`) wins, otherwise each member's own overrides still count — the UI says this in one sentence. **Interface:** Daily P&L day view gets a checkbox per departure, a “Merge N for costing” bar with that sentence, a **“Merged — one guide”** badge, the per-product breakdown with the rule that decided the fee, and an Unmerge link. **Nothing in Tours, grouping, payments, guide reminders or the digest changes** — this is a P&L-only concept, confirmed by the fact that the only table written is `pnl_unit_links`. **Guard rails** (all verified live on staging): only real departures, two or more, all on the same date — a cross-date attempt answers **400** “… is not on 2026-10-03 — a merge only works within one day”; a departure already linked answers **409** “Already merged: g1300398. Unmerge it first.”; one unit answers **400**; unlink deletes only `pnl_unit_links` rows. Admin only (pnl.php requires admin for every action). **Staging proof, 2026-10-02, merging the two 13:00 Uffizi departures g1300398 (8 PAX) and t5839 (5 PAX):** rows 20 → 19; revenue €2,510.53 → **€2,510.53 (unchanged)**; ticket cost €1,535.00 → **€1,535.00 (unchanged)**; guide cost €960.00 → **€870.00 (−€90.00, exactly one fee)**; total cost €2,567.00 → €2,477.00; profit −€56.47 → +€33.53. The merged row shows 13 PAX, guide €90.00 once, tickets €261.00 (174+87), net €513.05 (328.03+185.02), rule “category rate for Uffizi”, both members listed. **A real Bokun sync then ran (821 bookings) and the merge survived untouched** with identical totals — the 3.7 guarantee in practice. Unmerging restored every figure exactly (20 rows, €2,510.53 / €1,535.00 / €960.00 / €2,567.00 / −€56.47) and left 0 links behind. **Production:** smoke 12/12, **0 links created** (the owner will make his own), and the regression figures are unchanged — tours upcoming 701, total 462, unassigned 567, groups 379, and the P&L range figures identical to the pre-deploy capture. With no links the fold is skipped entirely, so a day without a merge produces exactly the rows it did before. Note: the small P&L numbers quoted in earlier step reports (“July retail €4,717.67, profit €125.45”) come from a grep in my own `figs.sh` that picks the FIRST `retail` in the payload — a single day's row, not the month. They were captured the same way before and after every deploy, so they were valid as a regression comparison, but the real month totals are July **retail €86,419.44 / profit €5,422.73** and September **€102,852.75 / €4,890.40**.

### 6.3 Radio planner & radio account
- Rule (your answer): every departure needs **PAX receivers + 1 transmitter per guide**; a merged unit (6.2) counts once with combined PAX.
- `GET radios.php?date=` → per time slot (15-min buckets, configurable) and per departure: receivers, transmitters, cumulative "in use" curve (assuming a configurable tour duration per product, default from the product), and the **daily order line**: peak concurrent receivers/transmitters + safety margin (setting). Same data feeds `/today` (4.6) and the AI assistant.
- Radio **account**: `radio_orders` (date, supplier, receivers_ordered, transmitters_ordered, unit cost, returned, lost, notes) with a small page under Daily P&L; P&L `radio_cost` can then use actual order cost instead of `radio_per_person × people` when an order exists for the day.
- Optional: WhatsApp/email the order line to the radio supplier from the page (Twilio path already exists).
- **Verify:** a day with 3 departures (5, 8, 9 PAX at 09:00/09:15/14:00, 2 h each) → 09:15 slot shows 13 receivers / 2 transmitters in use, 14:00 shows 9/1; order line = 13 + margin; entering an order updates the day's P&L radio cost.
- **— [x] DONE 2026-09-21** (step commit a6eb45a, merge 1e6f10e; live on staging + production). **Built from the owner's real workflow, not from the sketch above** — the slot curves, peak-concurrency order line, supplier unit costs and the P&L `radio_cost` link were NOT built; he simply sends Vox Firenze a WhatsApp every afternoon listing tomorrow's departures grouped by museum, and that is what the page now writes. The rest of the old 6.3 text is superseded; if the concurrency view is ever wanted it is a new step. **Museum mapping, shown to the owner before any UI was built** (`tools/radio_museum_map.php` prints it from production): 21 guided products, 11 unambiguous, 10 a guess. His answers 2026-09-21: a tour that names a museum gets that heading, **Bargello and Palazzo Vecchio included**; a tour that names none (Medici, Michelangelo, the two Renaissance walking tours) goes in one catch-all section, **“Altro”**, last; the Highlights tour (David, Duomo, Santa Croce) goes under **Accademia**. The two big guesses were settled by data rather than opinion: 22 Sep really has two 9:30 departures of the Uffizi+Accademia combo (9 and 7 PAX) — exactly the “9:30 - 9 / 9:30 - 9” shape of his own example — so combos and the Vasari-option tour both sit under Uffizi. **Rules implemented:** one line per DEPARTURE (a group counts once with its members' PAX added up), two departures at the same time are two lines, no reuse during the day, ticket products (`products.product_type`, the single source of truth since 3.8) and cancelled bookings produce nothing, the number is guests only (Vox adds the guide's transmitter), times carry no leading zero, sections run Uffizi → Accademia → other museums alphabetically → catch-all last and are sorted by time inside, greeting is “Ciao,” before 15:00 Europe/Rome and “Buonasera,” from 15:00, closing “Grazie” — both editable. **Backend** `radios.php` (admin only: viewer → 403, no token → 401, both verified): `GET ?action=plan&date=` (defaults to tomorrow in Rome) returns the sections, every departure with unit/product/guide/PAX for the on-screen table, the totals (receivers = guests, transmitters = departures with a guide, units = both) and the rendered message; `GET ?action=order`; `POST ?action=sent` records what was sent into **`radio_orders`** (self-provisioned + `database/migrations/20260921_radio_orders.sql`). Pure helpers in `radio_helpers.php`. **Frontend** `/radios`, admin-only menu item: date picker defaulting to tomorrow, the message in an editable box exactly as it will be sent (last-minute lines are typed straight in), **Copy** and **Download .txt** (`radio-YYYY-MM-DD.txt`), **Mark as sent**, the last order for that date with a note when the text has changed since, and beside it the detail table (museum, time, product, guide, PAX) with a “?” on any museum that had to be guessed. **Verified.** 27 checks in `tools/radio_check.php`, including **both of his example messages reproduced character for character**, a group of 3 bookings / 9 PAX rendering as the single line `9:30 - 9`, two departures at the same time as two lines, a cancelled booking not counted, a ticket product producing no line, a departure with no guide still listed but adding no transmitter, and the 15:00 greeting switch. vitest 171 green, build ok, smoke 12/12 on both. Staging 2026-09-22 generated 9 departures / 42 receivers / 3 transmitters and the message matched the data line for line; Mark as sent stored the order, the page then reported it and flagged that the text had changed. **Production: 0 orders recorded** (nothing was sent from here) — tomorrow's message is 8 departures / 42 receivers / 6 transmitters, pasted in the report for the owner to compare with what he would have written. Note: the catch-all heading is the single word “Altro” (`RADIO_OTHER_SECTION` in `radio_helpers.php`) — a one-line change if he prefers “Firenze” or “Centro”.

### 6.4 Missing GetYourGuide tours
- First check (you): are these products connected to Bokun as a GYG channel product? If **yes**, sync already handles them (SELLER + SUPPLIER roles) — then the likely culprit is classification (`products.product_type`) or a title keyword; one booking reference will show which.
- If **no** (sold directly on GYG): add a **manual booking form** (`tours.php` POST already exists; add a proper UI with product, date, time, PAX, names, language, channel = "GYG direct", `external_id = GYG-<ref>` for de-dup) and, as a second step, a **GYG email parser**: forward GYG booking emails to a mailbox polled by `tools/import_gyg_mail.php` (IMAP) that creates the same rows. GYG's Supplier API is a third option if you have partner access.
- **Verify:** a manually added GYG booking appears in Tours, groups with a same-slot Bokun booking, is counted in radios/P&L, and is not duplicated by a later sync.
- **— [x] DONE 2026-09-21** (step commits 432fb18…f7a6865, merge 635326d; live on staging + production). **Built item (a) only** from the 6.4 investigation — not the channels editor, not the portal-CSV reconciliation, not the nightly integrity check, not the SELLER-role change. **Confirmed cause:** the owner's GYG listing “Florence: Michelangelo's Life and Legacy 3.5 Hr Guided Tour” (GYG Option 1035080) shows *Connectivity Settings: Not connected*; Bokun's `externalBookingReference` search returns **0 hits for GYG83XL4BKH5** while a known reference returns 2, and Bokun's 17 bookings for 2026-08-21 match ours exactly. So no sync can ever produce it. **Model — NULL product_id, not a locally-created product row.** Every consumer already tolerates `product_id IS NULL`: the Tours list and the digest say so in SQL (`pr.product_type='tour' OR t.product_id IS NULL`), radios/payments use `NOT EXISTS(... ticket)` which a NULL can never match, the unassigned report reuses the list conditions, the language filter reads `tours.language`, and P&L derives ticket cost and category from the TITLE with `is_ticket` from a LEFT JOIN. The one consumer that excludes NULL is the auto-grouping pass (`bokun_sync.php`: `AND t.product_id IS NOT NULL`) — which is exactly what this step wants, since a manual row must stay out of the sync's grouping. A local product row would instead need a fake `products.bokun_product_id` (an int PK), and when the listing is finally connected Bokun would mint a *different* real id, so the rows still would not group — a permanent fake product for no benefit. Manual merge works on manual rows either way (it needs no product). **Columns:** `tours.source` ('manual', NULL for everything synced), `manual_revenue`, `manual_currency` — self-provisioned by `ensureManualColumns()` + `database/migrations/20260921_tours_manual_source.sql`. **The sync never touches them:** `source <> 'manual'` is now on both existence lookups, the grouping SELECT, the `bokun_total_price` backfill and the participant-names backfill; the sync only *counts* manual departures for one report line. **Proof, not reasoning** (`tools/manual_sync_survival.php`): a real `syncBookings()` over 2026-08-21 on staging (17 bookings updated, 1.18 s) left the row the form had created **byte for byte identical — all 49 columns, fingerprint `88c114815befc431613a864561621087` before and after**. **Duplicates are flagged, never merged.** Match key = same date + same HH:MM + same PAX, both sides not cancelled, two manual rows never paired. Computed on READ (`toursAttachDuplicateFlags`), never written, precisely so the sync stays hands-off; both rows get a red “Possible duplicate” badge and the manual one gets **“Same tour — remove mine”** (DELETE `?action=manual`, which refuses any non-manual row). **False-positive risk is real and was observed:** on staging the entry matched **3** unrelated 09:30 / 2-PAX bookings that day (Bargello 6189 and two Uffizi 6461/6462). It costs a badge to ignore; tightening it (channel or title similarity) is a one-line change if the owner wants it. **P&L honesty:** the amount entered is what the channel actually pays, so it is taken as **net with no commission subtracted** (`revenue.manual`), and where nothing can be known the row prints **“—”**, never €0.00: `ticket_unknown` when a manual title names no museum, and `guide_unknown` when a guide is assigned but the automatic rate is 0 (`guide_rate_other` is 0.00 on **both** environments — pre-existing, unchanged; flagged for manual rows only). **Verified on staging, the owner's exact tour:** 2026-08-21 09:30, 2 PAX, English, “GetYourGuide (direct)”, €418.29 (597.56 − 30%), no guide → then Ajantha assigned. Digest: “09:30 Florence: Michelangelo's Life and Legacy 3.5 Hr Guided Tour · 2 pax” for Ajantha. Radios: under **Altro**, `9:30 - 2` (its title names no museum — the 6.3 rule). Unassigned report: **1** while it had no guide, **0** with one, language shown. P&L 2026-08-21: net **€418.29**, tickets **—**, guide **—**, radio €3.00, profit €415.29. Language filter `English` finds it. Roles: viewer POST → **403**, no token → **401**; bad date/time/PAX/amount → **400** with the exact messages. **Tests:** 43 PHP checks (`tools/manual_check.php`), 9 vitest cases, suite **185 green**, build ok, smoke **12/12** on both. **Production: 0 manual rows created** (the owner adds his own); July **€86,419.44 / €5,422.73** and September **€104,060.53 / €5,206.84** identical before and after, tours upcoming 453, unassigned 123, groups 50 unchanged. Notes: (a) PAX is also guarded by the browser (`min="1"` stops the submit before React sees it), so that rule is asserted against the validator directly; (b) the staging test row (id 7250) was left in place as the verification artefact; (c) `tools/` is never deployed — the survival check refuses any database that does not look like staging unless `--force`.
- **6.4a — [x] DONE 2026-09-21** (merge cde897b): the possible-duplicate rule now also requires the titles to look like the same tour. Date + HH:MM + PAX alone flagged **3 unrelated bookings** against the one hand-entered departure on staging. `manualTitleTokens()` keeps only identifying words (generic ones — tour, guided, private, gallery, ticket, florence, durations — are dropped) and `manualTitleSimilarity()` scores them with the **overlap coefficient** (shared ÷ shorter set) rather than Jaccard, so a short Bokun title contained in a long channel title scores 1.0: Michelangelo vs “Michelangelo Private Guided Tour” **1.00**, vs Bargello / Uffizi+Vasari / Uffizi combo **0.00**. Threshold **0.3**, so two different Uffizi products (0.33) still warn each other. Channel is deliberately **not** part of the rule — all three false matches came from the same channel as the real one, so it discriminates nothing. Still computed on read, never written. **Staging: 3 false matches → 0**, and a genuine duplicate still flags on both rows (the manual row renamed to a Bargello tour Bokun really sends at 09:30 that day → 6189 ↔ 7250). 67 PHP checks (was 43), suite 185 green, smoke 12/12 on both.
- **NOTE (not fixed, 2026-09-21) — `guide_rate_other` is 0.00 on both environments.** A shared (non-private) departure whose title names none of Uffizi / Accademia / Pitti falls through `pnlGuideRateForCategory()` to `guide_rate_other`, which is **0.00**, so the P&L costs it at no guide fee. Measured read-only on production: **last 90 days 1 of 303** guided departures with a guide assigned, **July 1 of 123** (2026-07-14 09:30 “Renaissance Florence with Expert Guided Walking Tour”, guide Isabella, 2 PAX), **September 0 of 128**. So July's profit is overstated by one guide fee (≈€60–120 against €5,422.73) and September's not at all. `guide_rate_private_other` is 120.00, so private Other tours are costed properly; `pnlGuideRateForCategory()` also has no `case 'Borghese'`, harmless today because the only Borghese product is a ticket and tickets never reach guide costing. The owner decides the rate before anything changes. Measured with `tools/guide_rate_other_check.php` (read-only, asks the live P&L endpoint day by day).

---


- **6.6 Direct sales were charged a commission the owner never paid.** Raised by the owner: “i need to know accurate cost for all tours.” `pnlExtractRevenue()` read Bokun’s `resellerInvoice` for OTA bookings (correct to the cent) but a **direct sale has no reseller invoice** — he pays nobody — and the code read that absence as “no information” and fell through to `comm_default = 30%`. Bokun held the truth all along in `productBookings[0].customerInvoice` (`totalCommission` 0, `totalSansCommission` = `total`). Measured on production: **205 bookings, EUR 7,852.21 all-time, EUR 2,159.89 in 90 days, profit understated and never overstated.** **Verify:** t6054 / WEB-98701751 reads 239.12 instead of 239.12 − 71.74; OTA channel totals identical before and after; July/August/September move upward by exactly the direct-sale commission; every manual override still wins.
- **— [x] DONE 2026-09-21** (step commit f397838, merge b93040a; live on staging + production). **Scope:** items 1, 2, 3 and 5 of the investigation. Item 4 (a card-processing fee on direct sales) is **deferred on purpose** — the owner has not given his real rate and nothing was invented; item 6 (a Tours “show tickets” toggle) is not in this step. **Measured first, before touching anything.** Only **one** code path reaches the percentage (`pnlExtractRevenue()` path 3) and only **three** channel values ever get there with rows: `www.florencewithlocals.com` (214), `Payment links` (6), `Backend` (1) — **205 live bookings, EUR 26,174.05 retail, 0 of them carrying a non-zero customer-invoice commission**. **Airbnb would land on `comm_default` by the ladder but never reaches it**: all 298 of its bookings arrive with a reseller invoice, as do GetYourGuide (4,416), Viator (2,324) and Headout (9). So no ladder change could move an OTA — confirmed afterwards by the numbers. **The fix:** a new step 1b reads `customerInvoice` (`totalCommission` / `totalSansCommission`, deriving whichever is missing) when there is no reseller invoice; path 1 is untouched and still returns first for all 7,047 OTA bookings. The percentage is now a genuine last resort and returns `estimated = true`. The ladder names `florencewithlocals` / `payment link` / `backend` as direct (0%), adds `comm_airbnb` at 20% and corrects `comm_headout` 25 → 20 (default **and** the stored row on both environments, 1 row each) — all safety net, none of it reachable today. **Honesty in the UI (item 3):** the “estimated” chip existed but was grey-on-grey and went unnoticed for months while 30% was wrongly deducted; it is now amber and reads **“estimated — not from an invoice”**, and `pnlTotals()` gained `estimated_units` so the day and month headers say **“N of M departures are estimated, not from an invoice”**. **Staging, the exact booking from the investigation:** t6054 / WEB-98701751, Bokun `customerInvoice` total 239.12 commission 0 — **BEFORE 239.12 − 71.74 = 167.38 (estimated YES), AFTER 239.12 − 0.00 = 239.12 (estimated no)**; that day’s totals moved by exactly that much (commission −125.01, net +125.01, profit −350.10 → −225.09) and `estimated_units` is **0 of 16**. **Production, before → after** (retail and total cost identical in all three months, as they must be): **July** net 65,643.73 → 66,294.06, profit 5,362.73 → **6,013.06 (+650.33)**; **August** net 42,211.53 → 42,750.84, profit −579.47 → **−40.16 (+539.31)**; **September** net 75,412.38 → 76,700.99, profit 5,155.88 → **6,444.49 (+1,288.61)**. Each delta equals that month’s direct-sale commission and nothing else: expected 668.32 / 539.29 / 1,288.61, and July is 18.00 lower because the owner’s two `revenue_override` rows (t5657 EUR 40, t5658 EUR 20, both pinned to net 0.00) keep their override — verified live, both still `overridden = YES`. The remaining 1–2 cent gaps are per-booking rounding (`round(retail×0.30, 2)` summed) and nothing else moved. **Per channel, 90 days, before → after:** GetYourGuide 523 units / 47,768.45 commission / 30.0% — **identical**; Viator 232 / 0.00 / 0.0% — **identical**; Airbnb 32 / 971.69 / 20.0% — **identical**; Headout 1 / 7.17 / 20.0% — **identical**; **www.florencewithlocals.com 41 units, commission 1,138.56 → 0.00, net 2,614.55 → 3,735.11**; mixed-channel units 11,640.57 → 10,619.20 (25.4% → 23.1%). **Units with a guessed figure: 63 → 0.** Total given back over 90 days 2,159.93 against 2,159.89 predicted. (The website’s net is 60.00 below its retail at 0% commission — that is the two overridden rows, not a leak.) **Verified:** 31 checks in `tools/revenue_check.php` (which reads the two functions out of `pnl.php` by name so it can never drift into testing a copy), 3 new vitest cases, suite **202 green** (was 199), build ok, smoke **12/12** on both. Read-only evidence scripts: `tools/comm_ladder_check.php`, `tools/pnl_revenue_snapshot.php`. Notes: (a) **the card-processing fee on direct sales is still missing and is the only remaining deduction** — it needs the owner’s real rate and is deliberately not guessed; (b) `comm_airbnb` has no stored row, so the 20.0 default applies until someone edits Rates & Costs, where it now appears; (c) nothing in Tours, grouping, payments or the 4.7 measurement baseline was touched.
- **6.7 Card processing fee on direct sales (the deferred item 4 of 6.6).** 6.6 stopped deducting a 30% commission the owner never paid on his own sales, leaving them with **no deduction at all** — but a card sale really does cost him the processor's fee. His rate: **Stripe, 1.5%**, a percentage only, no per-transaction component. **Verify:** t6054 / WEB-98701751 reads 239.12 − 3.59 = 235.53; an OTA booking gains nothing; setting the rate to 0 removes the deduction; a manual override still wins; July/August/September move **down** by exactly 1.5% of card-paid direct revenue.
- **— [x] DONE 2026-09-21** (step commit 0fbaa64, merge d16a1e4; live on staging + production). **Who gets the fee is read from the booking's own payment record, never guessed from the channel** — a fee charged on a payment that never touched Stripe would just be a new error replacing the old one. Measured across every direct sale we hold: **202 carry `paymentProviderType: STRIPE_TOKEN` with an `authorizationCode` beginning `pi_`** (a Stripe PaymentIntent), **2 are `VOUCHER` redemptions** and **1 is a Backend booking with no payment at all and `paymentStatus: NOT_PAID`**. Only the first group is charged. So the question the prompt asked — whether a Backend booking went through Stripe — did not need to be put to the owner: the data answers it, and that one booking is excluded. In the last 90 days the split is 66 card-paid website bookings (EUR 7,121.59) and 1 voucher (EUR 78.03, no fee); `Payment links` and `Backend` have no rows in that window (6 and 1 all-time, the payment links all card-paid). **How it is applied:** `pnlPaidByCard()` reads `customerInvoice.payments` and accepts either a provider naming Stripe or `paymentType = WEB_PAYMENT` (so changing gateway cannot silently switch the fee off, while a voucher, a cash sale or an unpaid booking never matches). `pnlCardFee()` takes the rate from the new **`fee_card_direct`** setting — default **1.5**, editable in **Rates & Costs → “Card processing fee — direct sales (%)”**, and **0 turns the deduction off entirely**. Nothing is hard-coded and **no per-transaction cent amount was invented**. The fee comes off **on top of** the invoice figure 6.6 reads, never instead of it, and only where there is no OTA commission — **an OTA gains nothing, including Viator**, whose commission is 0 only because it reports net; it lands on the reseller path and returns before the fee is ever considered. **Named honestly in the UI (item 4):** `revenue.card_fee` is a separate field from `revenue.commission` everywhere. The row carries a blue **“card fee €3.59”** chip, the row detail reads **“Retail … − <channel> commission … − card fee …”**, and the Net Revenue tile now reads **“Retail X − commission Y − card fee Z”** instead of pretending the fee is an OTA commission. **Staging, the booking from the investigation:** t6054 / WEB-98701751 — after 6.6 it read 239.12; now **239.12 − 0.00 commission − 3.59 card fee = 235.53**, and that day's totals moved by exactly the day's card fee (net 2,772.91 → 2,767.83, card_fee 5.08). **0 OTA-only rows carry a fee.** Overrides untouched: t5657/t5658 still net 0.00, `overridden = YES`. **Production, before → after** (retail, commission and total cost identical in all three months, as they must be): **July** net 66,294.06 → 66,261.53, profit 6,013.06 → **5,980.53 (−32.53)**; **August** net 42,750.84 → 42,723.87, profit −40.16 → **−67.13 (−26.97)**; **September** net 76,700.99 → 76,638.32, profit 6,444.49 → **6,381.82 (−62.67)**. Expected from 1.5% of card-paid direct revenue: 33.43 / 26.97 / 62.67 — **every month matches to the cent**, July being 0.90 lower because the two overridden rows show their fee but keep their pinned net. September's fee covers 32 of its 34 direct bookings; the other two were not card payments, which is the exclusion doing real work. **Verified:** 50 checks in `tools/revenue_check.php` (a Stripe payment is recognised, a voucher and an unpaid booking are not; rate 0 removes the fee, rate 2 recomputes to 4.78; an OTA and Viator gain nothing), 4 vitest cases, suite **203 green** (was 202), build ok, smoke **12/12** on both. Notes: (a) **the fee is charged on the row's invoice total, not on the exact amount settled** — one booking in 205 (WEB-82075945, a DEPOSIT of 113.18 against an invoice of 119.18) is therefore overcharged by **€0.09**; using the settled amount would need splitting multi-product invoices and was not worth the complexity. (b) **Card origin is not knowable from this data**: `customer.country` and `nationality` are empty on all 198 website bookings, and the payment record holds only the last four digits — no card country, no brand. Stripe's 1.5% is the European-card rate; non-European cards and non-euro settlement cost more, so the true blended rate is probably higher than 1.5% and **must be read off his Stripe statement**, not computed here. (c) `fee_card_direct` has no stored row, so the 1.5 default applies until he edits it.
- **6.8 A printable participant list per departure.** Requested by the owner: he currently writes the museum door list by hand from the Bokün booking list, once per departure. One button per departure on the Tours page downloads one A4 PDF for that departure — a group or a manual merge is one sheet, a 10:00 and a 14:15 Uffizi are two. Columns fixed with him: booking reference, lead/traveller names, PAX, channel, reselling agency, adults/children/infants; above them the guide and their phone, below them a blank “Tickets used” box. His own visual style (his reference B) with the content of his hand-made list (reference A).
- **— [x] DONE 2026-09-21** (step commits 6442cfe…1fa0427, merge fd504f9; live on staging + production). **`GET /api/participants.php?unit=g<id>|t<id>`** → `application/pdf` (`&format=json` returns the same data for tests). The departure is the same tour unit the rest of the system uses — `IF(group_id IS NOT NULL, CONCAT('g',group_id), CONCAT('t',id))` — so no second notion of a departure was invented; **6.2's merged *costing* units (`m<key>`) are rejected with 400**, because one guide fee shared by two departures does not make them one group at a door. **Any logged-in user may print one** (the two roles are admin and viewer, and the guide needs the sheet as much as the owner); nothing is ever written. **Engine: FPDF 1.86 vendored in `public_html/api/lib/fpdf`** — public domain, no composer, no C extension, and every file is a `.php` file, so the existing deploy allowlist ships it with **no change to `deploy.sh`**; `api/.htaccess` denies `^lib(/.*)?$` (verified 403). No font is embedded, so a sheet is ~3 KB — the cost is CP1252 text: `participantsText()` transliterates (Thaís, Yildiz, Gabrić) and prints **“(name in another script - see reference)”** for the 8 production names out of 7,270 that cannot be carried, rather than boxes or a faked name. **Content rules:** the reference prefers the channel's own code (`externalBookingReference`, what a GetYourGuide voucher shows) over Bokün's; **every traveller** is listed where `participant_names` has them, the lead alone where it does not; the agency column reads `resellerInvoice.issuer.title` **only** — never `seller.title`, which for an OTA is just the channel again — and is blank on every booking today (0 of 2,627 in 90 days), which is correct, not missing; cancelled bookings are excluded and counted in a footnote. **Two runtime faults were found on staging and fixed before production**, both because nothing built a PDF outside the endpoint: `radioMuseumForTitle()` returns `['museum','confident','why']` and was used as a string (“MUSEUM: Array”, `array-….pdf`), and the sheet class declared `private $w`, which collides with **FPDF's own page-width property** — a fatal that produced a 0-byte 500. The renderer therefore moved to **`participant_sheet.php`**, which needs no database and no session, and `tools/participants_check.php` now renders **six real PDFs** (normal, no guide, no live bookings, no museum, long → 2 pages) asserting `%PDF`, page count and size. **Verified on staging:** 9-pax Uffizi group → 200, `application/pdf`, 3,093 B, 1 page, `uffizi-09-24-2026-0930-participants.pdf`, 5 rows with a 3-traveller row wrapping to two lines and í surviving as CP1252 0xED; mixed cancelled+live → “1 cancelled booking(s) excluded”; all-cancelled → “No participants on this departure”; no guide → **NO GUIDE ASSIGNED**; a hand-entered (6.4) row prints; a walking tour that names no museum falls back to the product name (6.3's catch-all “Altro” is not printed on an English sheet) with the slug capped at a word boundary; `m1` → 400, unknown → 404, no token → 401, viewer → 200. Button verified in the real UI on staging: 20 buttons labelled “Participant list (PDF)”, a click issued `GET participants.php?unit=g1300436` → 200. **Production: nothing written** — one sheet generated as proof (tomorrow 22/09 14:15 Uffizi & Accademia, 5 bookings, 9 pax, 3,069 B, one page, guide and phone printed). 39 PHP checks, 5 vitest cases, suite **208 green**, build ok, smoke **12/12** on both. Note: the sheet prints the guide's phone number, so it is a document to hand to a guide, not to leave on a counter.

- **6.9 The Viator account switch — do not lose the bookings he still has to honour.** URGENT, raised by the owner 2026-09-21. He has opened a **new Viator account** (empty) and is retiring the old one, which still holds real future bookings as far out as **2027-05-30** that he must still run and ticket. The old account is still connected to Bokün, so this is a prevention job, not a recovery one. **Part A (investigate + protect) DONE 2026-09-21** on branch `step/6.9`; no sync behaviour changed, no row touched.
- **— Part A finding 1 — the sync is purely additive; an unreturned booking is LEFT ALONE.** `syncBookings()` iterates **only over what Bokün returns** and, per booking, either `UPDATE tours SET … WHERE id = ?` or `INSERT … ON DUPLICATE KEY UPDATE`. There is **no reconcile-missing pass anywhere**: `grep "DELETE FROM tours"` across `public_html/api/` returns only the two admin endpoints in `tours.php` (the 6.4 manual-row delete and the explicit `DELETE /tours/{id}`), and `cancelled` is written in exactly one place in the sync — `'cancelled' => ((\$productBooking['status'] ?? \$booking['status'] ?? '') === 'CANCELLED') ? 1 : 0` in `BokunAPI::transformBookingToTour()` — which can only be reached **by a booking Bokün actually returned**. The same is true of all three triggers, because all three run the same function: cron (`bokun_cron.php` → `syncBookings`, −7..+60 days), “Sync Now” (the endpoint → `syncBookings`), and the webhook (`bokun_webhook.php:187` → `syncBookings($d, $d, 'webhook', …)` per affected date). The only thing that runs on an **empty** result is step 3.3's `autoGroupAfterSync()`, which touches `tours.group_id` and deletes **empty `tour_groups` rows** — never a booking. **Proved on live production data, not by reading alone:** tours 5321 and 5325 (30 Dec 2026) were last returned by Bokün **89 days ago** and are still present, live and uncancelled; 5920 (5 May 2027) 63 days. Everything past the +60-day window has been in the “not returned by Bokün” state for months with no effect.
- **— Part A finding 2 — the exposure.** Production, 2026-09-21: **2,324** Viator bookings held (224 cancelled); **50 live future bookings** across **48 distinct departures**, **143 PAX**, latest **2027-05-30**; **4 departures already have a guide**; **10 future departures mix Viator with another channel**, so they cannot simply be treated as “Viator departures”. Full list exported (see finding 4).
- **— Part A finding 3 — nothing stored distinguishes the OLD Viator account from a NEW one.** Every future Viator row carries the identical `channel {id: 12296, title: "Viator.com", channelType: "OTA"}` and `seller {id: 5194, title: "Viator.com"}` — these are Bokün's **marketplace** channel and seller, not his account, and a second Viator connection on the same Bokün account has every reason to reuse them. `externalBookingReference` is a bare 10-digit Viator number (`1426885809`) — same shape for both accounts; `confirmationCode` (`VIA-98167710`) is Bokün's own counter; `vendor` is **his** Bokün vendor (96929) either way; `agent` is empty on all 50. The one field that **may** differ is `productExternalId` — today `426324P1/P2/P4/P14/P16/P28/P30`, where `426324` is his Viator product base — but whether a new account mints new codes cannot be verified until he connects it, so it is a hope, not a marker. **Conclusion: the only reliable marker is one we write ourselves, on the existing rows, before he disconnects.** (`bokun_data.creationDate` is a usable second line of defence — every old booking predates the switch — but it lives inside the JSON blob and nothing reads it today.)
- **— Part A finding 4 — backed up.** `tools/viator_backup.php` (CLI, never deployed) wrote, outside the web root, mode **0600**: `~/backups/viator_bookings_production_20260921_174110.jsonl` — **2,324 rows, all 49 columns, `bokun_data` inlined as real JSON, 18,088,603 bytes**; and `~/backups/viator_future_departures_production_20260921_174110.csv` — **48 departures, 6,765 bytes**, with date, time, product, PAX, guide + phone, Bokün refs, **Viator's own reference numbers** and lead names, i.e. a sheet he could work from on paper. `tools/viator_probe.php` is the read-only measurement.
- **— Part A finding 5 — the danger is (b): the sync leaves unreturned bookings alone.** Disconnecting the old account cannot silently cancel or delete these rows. The residual risks are different and smaller: (i) a departure could stop being *updated*, so a reschedule or cancellation made in the old account after the switch would never reach us — he must treat those 48 as frozen and check them by hand; (ii) **`booking_channel` is rewritten by the sync's UPDATE on every run**, so any label written into that column would be wiped for in-window rows — the label must live in its own column the sync never writes.
- **— Part A finding 6 — labelling (PROPOSED, not applied).** Add `tours.viator_account ENUM/VARCHAR NULL` (self-provisioned + a numbered migration), stamp **`'legacy'` onto the 2,324 existing Viator rows before he disconnects**, leave it NULL for everything else, and have the sync write **`'current'`** only on rows it *inserts* from then on — never on update, so a legacy row can never be relabelled. Display: a small grey **“old Viator”** chip next to the channel in the Tours list and the booking modal, and a separate **“Viator (old account)”** line in the P&L channel breakdown. `booking_channel` itself stays `Viator.com` untouched, so 6.6/6.7's commission path (`strpos($ch,'viator')` → `comm_viator`) and every existing figure are unaffected. **This is the urgent item: after he disconnects, the information needed to write it no longer exists.**

- **— Part B DONE 2026-09-21** (step commits d57f691…02a6ad4, merges f47187a / 4981fde / a862292; live on staging + production). **Item 1 shipped by itself and first**, because it was the only piece with a deadline: `tours.viator_account` (VARCHAR(16), indexed, `database/migrations/20260921_tours_viator_account.sql` + `ensureViatorAccountColumn()`), stamped `'legacy'` **in the same call that creates the column** — at that moment every Viator row in the table belongs to the old account by definition, so the stamp cannot be forgotten, run twice or run late. **Staging 2,323 of 2,323; production 2,324 of 2,324, 0 unlabelled, 0 non-Viator rows labelled**, 50 live future bookings / 143 PAX / latest 2027-05-30. **Item 2, the guard:** `viator_account` is written on INSERT only — absent from the sync's UPDATE column list and from the INSERT's `ON DUPLICATE KEY UPDATE` clause, beside `paid`/`payment_status` (step 3.1). Which label a new insert gets is decided by the booking's **own `creationDate`** against a recorded cutover (`viator_switch`, one row): until he connects the new account there is no cutover and every Viator insert is still `legacy`, because a booking arriving on the old account today is still one he must honour. **Proved against real Bokün data, not by reading:** a real `syncBookings('2026-09-29','2026-09-29')` on staging rewrote four legacy rows — `last_sync` advanced and every fingerprint changed, so Bokün genuinely touched them — and all four labels stayed `legacy`; deleting a row by reference and letting the sync re-INSERT it returned **`legacy` with no cutover recorded and `current` with one dated 2020-01-01**. **Item 3, the watchdog:** `viatorWatchdogRun()` runs once a day from the sync (never allowed to fail it) and writes one row to `viator_watchdog`. Expected = the previous count **minus the departures whose date has since passed and the change in Bokün cancellations**, both computed from rows we still hold, so a tour running is not a disappearance; below that is an ALERT with a loud line in `~/logs/api-error.log`, and the latest reading rides on `sync-info` (`viator_watchdog`). Staging: baseline 49 → quiet at 49 → **ALERT at 48** → quiet again at 49. **Production baseline taken: 50 bookings / 48 departures / 143 PAX / latest 2027-05-30.** **Item 4, the display:** a grey **“old Viator”** chip in the Tours list, the mobile card and the booking modal (`ViatorLegacyChip`, renders nothing for anything else), and **“Viator (old account)”** as its own line in the P&L channel breakdown (confirmed live on 2026-09-18: `www.florencewithlocals.com`, **Viator (old account)**, GetYourGuide, Airbnb). `booking_channel` is never touched, so the 6.6/6.7 commission path is unmoved — **July 86,419.44 / 66,261.53 / 5,980.53, August 58,408.87 / 42,723.87 / −67.13, September 105,507.81 / 76,689.88 / 6,385.38, units 404/246/324, identical to the cent before and after**. **Item 5:** admin-only read-only `viator_legacy_export.php` builds the paper fallback **live** from the labelled rows (date, time, departure, product, PAX, guide + phone, Bokün refs, **Viator's own reference numbers**, lead names, UTF-8 BOM for Excel) and a panel on **Bokun Integration** downloads it, shown only while departures remain; production 401 / 403 / 200 `text/csv`, 48 departures, 6,768 bytes. 25 PHP checks (`tools/viator_check.php`), 4 vitest cases, suite **212 green**, build ok, smoke **12/12** on both. **Residual risk, operational not technical:** once he disconnects, those 48 departures stop updating here — a reschedule or cancellation made afterwards will never reach us, so they must be checked by hand against Viator.

### 6.10 The Daily P&L is the owner's alone
- Requested by the owner: `/daily-pnl` visible to him and nobody else — no other account sees revenue, cost or profit. Production has **two** admins (`dhanu`, `sudesh`), so "admin only" would not have meant "Dhanu only"; the owner chose a separate owner gate that leaves `sudesh` a full admin everywhere else.
- Backend (the part that actually protects anything, since the URL can simply be typed): `Middleware::requirePnlOwner()` = authenticated **and** admin **and** on the owner list (`PNL_OWNER_USERS` in the server .env, default `dhanu`, matched case-insensitively on username or email). `pnl.php` — day view, month view, `?action=settings`, `?action=costs`, `?action=links|link|unlink` — moves from `requireRole('admin')` to it. `pnl_links.php` is a helper, not an endpoint, and now answers 404 when requested directly. No figure, rate or calculation changes.
- Frontend: `auth.php` (login + verify) returns `pnl_access`, `AuthContext` exposes `canSeePnl()`, the "Daily P&L" menu item is `ownerOnly`, and `/daily-pnl` is wrapped in the new `OwnerRoute`, which renders a plain "You don't have access to this page" card **inside the layout** rather than redirecting — no bounce, no broken screen, no endless spinner.
- **Verify:** owner 200 on every P&L endpoint; viewer and non-owner admin 403 with a readable message; unauthenticated 401 (not 403); menu item absent for a non-owner, present for the owner; typing `/daily-pnl` as a non-owner shows the access message; July/August/September unchanged to the cent.
- **— [x] DONE 2026-09-22** (step commit 19b70dd, merge ce7b251; live on staging + production). Production has **two** admins (`dhanu`, `sudesh`, both active) — raised before building, and the owner chose the separate owner gate over demoting `sudesh`. **Staging, attacked three ways as the real `viewer` account:** menu — the Daily P&L item is absent (nav reads Dashboard, Tours, Guides, Priority Tickets, Tickets, Payments, Guide Reports); direct URL — `/daily-pnl` renders “You don't have access to this page” inside the layout; API — 403 on all four GET routes and all four POST routes, `pnl_links.php` 404, unauthenticated 401. A temporary session for **`sudesh` (admin, not the owner)** got the same 403s while still getting 200 on `tours.php` and `radios.php` and 404 (not 403) on `DELETE tours.php/999999` — he is a full admin everywhere else. Owner write paths exercised on staging and restored (gelato rate 3→3.25→3, an override on `t6449` set and cleared, `t4229`+`t6449` merged as `m5` and unmerged; `t6449` back to −€78.56). **Production unchanged to the cent** — 2026-07 net 66,261.53 / cost 60,281.00 / profit 5,980.53; 2026-08 42,723.87 / 42,791.00 / −67.13; 2026-09 77,284.98 / 70,672.00 / 6,612.98, identical before and after. Smoke 12/12 on both.
- **— Noted, NOT changed (a guide can still see these):** `tours.php`/`tour-groups.php` return `bokun_total_price` + `bokun_currency` (the “Customer paid” line in the booking modal); `payments.php`, `guide-payments.php` and `payment-reports.php` return every guide payment amount and the per-guide cash/bank totals, and **/payments is in a viewer's menu**; `tickets.php` returns ticket `price`. Guide Reports is counts only. Locking these would change what a viewer account is for, so it is the owner's call, not this step's.

### 6.11 Accademia and Uffizi tabs on Priority Tickets, with a printable day list
- Requested by the owner (prompts #49/#50): Accademia ticket bookings handled the way Uffizi ones are, plus a time-wise participant list per day. His decisions: tabs **All museums | Uffizi | Accademia** replace the Museum picker, All is the default and is today's page (Borghese and Palazzo Vecchio stay there); nothing remembered; **Download participants list** on the Uffizi and Accademia tabs only, for the selected date - one sheet per day, sorted by time, a headed section with a subtotal per product; the museum comes from each booking's own title (radios' rule), not the products table; cancelled bookings stay on screen but never print (footnote "N cancelled booking(s) excluded"); no "old Viator" chip here; the customer's own reference (GYG... / Viator number) prints large with our GET-/VIA- code small underneath, website bookings print the Bokun code alone.
- **Verify:** Accademia tab = Accademia ticket-only bookings for the date, no guided tours; the 23 Sep sheet = exactly the tab's bookings, time order, right total; two products = two headed sections with correct subtotals; cancelled absent from the sheet; Uffizi tab unchanged; a failed fetch shows the banner, not an empty list; July/August/September P&L unchanged to the cent.
- **— [x] DONE 2026-09-23** (step commits 2e9f15c, b7262ad, merge 41c408e; live on staging + production). **Tabs:** `All museums | Uffizi | Accademia` replace the Museum picker; All is the default and nothing is remembered. The museum is read from the booking's title by `src/utils/ticketMuseum.js`, a mirror of 6.3's `radioMuseumForTitle()`, so tab and sheet cannot disagree. **Sheet:** `GET participants.php?museum=Uffizi|Accademia&date=YYYY-MM-DD` (same file, same access as 6.8 and Priority Tickets = any logged-in user; `&format=json` for checks); `DaySheet extends ParticipantSheet` in `participant_sheet.php`, pure builder `participantsDayBuild()` in `participant_helpers.php`; file `accademia-participants-2026-09-23.pdf`. Sections are per booking TITLE, so product 961802's two titles ("Reserved Ticket" / "Priority Ticket") print as two sections. Long channel names shrink to fit. **Found and fixed on the way (the tab could not match the sheet otherwise):** the page re-filtered the server's ticket rows by title keywords (`filterTicketsOnly`) and silently hid every "Florence: Uffizi Gallery **Reserved** Ticket & Digital Audio Guide" booking — **79 live upcoming on production**, the page showed 66 of 159 ticket rows. The server's products-table filter is the rule (CLAUDE.md), so the keyword pass is gone from this page; All now shows all 159. **Verified:** 77 PHP checks (`tools/participants_check.php`, day sheet rendered for real: his 23 Sep example, two products 4 + 7 with subtotals, empty day, cancellation-only day, 3-page day), vitest **289 green** (7 page + 14 rule cases new), build ok; staging and production smoke **12/12**; tab-vs-sheet on production for 24 Sep, 23 Sep and 29 Sep, both museums — **all six identical**, time-ordered, cancellations footnoted (23 Sep Uffizi 14 rows / 3 cancelled → 11 printed, 28 pax, 2 sections 24 + 4; 29 Sep Accademia 6 / 2 → 4, 8 pax); 401 no token, 200 viewer, 400 Borghese / bad date, 6.8 `unit=` unchanged. **P&L unchanged to the cent:** July 66,261.53 / 60,281.00 / 5,980.53, August 42,723.87 / 42,791.00 / −67.13, September 78,728.75 / 71,870.50 / 6,858.25 before and after. Not changed: the stats cards still count Uffizi/Accademia by `title.includes()` (now including the Reserved rows); the 6.8 sheet's own Channel column can still overflow on `www.florencewithlocals.com`.

### 6.12 Auto-groups never mix languages
- Reported by the owner 2026-09-24 (screenshots): the 25 Sep 14:30 Uffizi small group showed as one group, Italian + English, 7 PAX, 3 bookings, Unassigned, “Manual”. His rule: “if 2 languages same time 2 groups based on the language. otherwise how i assign guide to the group.” A guide speaks one language, so one departure = one language.
- **How that group was made (read-only, production):** (a) at root. The cron at 23:45:11 Rome on 23 Sep created auto group **1508364** (`961801|2026-09-25|14:30`, the log's only “into 1 groups” in that window, the same second Theodoros's booking arrived) = Mario Cuomo (Italian) + Theodoros Perperidis (English); the Airbnb booking joined it at 17:05:43 Rome on 24 Sep (the 25 Sep webhook's grouped count went 12 → 13). The “Manual” group he photographed contained Daniela Andaluz, who is a **different product** (1130528, Vasari Corridor tour), so it can only be a manual merge: its id is one of 1508368–1508370, all created and deleted between 18:58:26 and 19:02:19 Rome on 24 Sep while only `dhanu` had the Tours page open (client_perf row 50, 18:58:18). Nothing ever turned an auto group into a manual one: `is_manual_merge = 1` is written only by the manual-merge INSERT. The 23 Sep 16:30 ungrouping of tours 7313 (English) + 7317 (Spanish) at 13:10:52 Rome is the same story: that auto group 1508359 mixed English and Spanish, `dhanu` had loaded /tours at 13:09:52 and the page reloaded its guide list at 13:10:53 (a dissolve or unmerge in the UI), and the 13:15 cron put the pair back together. No access log exists on the host, so the person is inferred from the page-load recorder, not proven.
- Rule: from **2026-09-24** (`GROUP_LANGUAGE_KEY_FROM`, fixed so a split cannot merge back once its day is past) the auto key is `product_id|date|HH:MM|language`. A booking without a language joins only when exactly one language is present at that departure, otherwise it stays on its own. Earlier dates keep the old key and behave exactly as before. Manual merges untouched. The Tours page “Auto-Group” button uses the same split. A group whose members speak two or more languages (only possible through a manual merge, or an auto group left for the owner) shows a **Mixed languages** chip on desktop and on the phone card.
- Identity: the sync never splits an existing mixed auto group (it is frozen, like a manual merge). The one-off `tools/group_language_migrate.php` rewrites `bucket_key` in place for single-language groups (id, guide, payments, links kept) and splits mixed ones: the language the guide speaks keeps the id (no guide: the larger PAX that still forms a group); a payment, or a guide language it cannot match, means **held and listed**; a mixed group where no language has 2 bookings (no guide, no payment, no P&L row) is dissolved. `pnl.php` reads a group's key from the group row from the cut-over on (past computed as before).
- **Verify:** EN + IT at one departure → two groups; 3 EN → one; a no-language booking joins with one language, stays alone with two; a manual mixed merge untouched + chip; single-language group keeps its id through the rewrite; a mixed group with a payment is not split; guide fill-only; a real sync after the change writes nothing; July/August/September P&L unchanged.
- **— [x] DONE 2026-09-24** (step commits d3dcfd7, 58429c6, 20589b8, f72321f; merge 1a78652; live on staging + production, migration run on both). **Before (production, today and later):** 104 groups, **2 mixed-language auto groups, 0 manual**, both product 961802 (Uffizi reserved ticket + audio guide, no guide, no payment): g1508252 29 Sep 16:15 (1 Spanish 4 PAX + 1 English 2 PAX) and g1508256 18 Oct 16:15 (1 Spanish 2 PAX + 2 English 3 PAX). **0 of 535** upcoming bookings have no language. **Migration (production):** 86 keys rewritten in place, g1508256 split (keeps English, Spanish tour 5271 on its own), g1508252 dissolved (two single bookings), **0 held**, 0 guides cleared, 0 P&L rows re-keyed; snapshot diff **tours whose group id changed: 0**. **One sync after:** `groups_reused 108, rows_written 0`, diff 0/0/0. **After:** 0 mixed groups. Staging: same two departures, same outcome; first sync caught up 28 new Bokun bookings (0 ids changed), second sync `rows_written 0`; the owner's 25 Sep 14:30 on staging now groups by itself as English (Theodoros + Airbnb) with Mario's Italian booking on its own. No real upcoming departure has 2+ bookings in each of two languages, so “two groups” is shown by scenario 11. **P&L:** July 86,419.44 / 5,980.53, August 58,408.87 / −67.13, 1–23 Sep 5,870.19 profit: identical to the cent. 24–30 Sep and October: +1 unit each (ticket units), every money figure identical: **upcoming guide cost change €0**, because both departures are ticket-only. Tests: vitest 309 (8 new), `grouping_check` 35/35, `grouping_scenarios` 45/45 (11–19 new), smoke 12/12 staging + production.
- **Noted, not changed:** a mixed ticket-only departure (audio guide) is now split too. Harmless (no guide fee), but it adds a line to the radio order/participant sheets. Holding ticket products out of the rule is a one-line change if he prefers.

### 6.5 Guide payment runs for accounting
- Record what was actually paid to each guide per period against the departures it covers, with an export for the accountant. The owner currently pays manually against a list the guides send him; the app should match that routine rather than replace it. A payment run = one guide + one period + the amount agreed, with the departures it covers attached, so the history is accountant-ready: what was paid, when, for which tours. **Not started — to be designed with the owner** (it is mostly a question of matching the real routine, not code). Depends on nothing; 5.3 and 3.8 already made the per-payment side accurate.

## Phase 7 — AI assistant (spec to be refined with you; ~1 week)

Architecture that is safe on shared hosting and keeps data in your control:
- **Backend** `api/assistant.php` (admin-only, rate-limited): receives the chat, calls the Claude API server-side (key in `.env`, never in the browser) with **tool use**. Tools are thin, read-only PHP functions over the existing queries — the same ones the UI uses, so answers always match the screens:
  `unassigned_departures(start,end)`, `guide_availability(date|range)` (from `availability_requests` + assigned tours), `guide_schedule(guide, range)`, `revenue(range, product?, channel?)` and `pnl(range)` (from `pnl.php`), `radios_needed(date)`, `tour_lookup(booking ref | customer name)`, `bookings_by_language(range)`. Later, write tools with confirmation (assign guide, send reminder).
- Output: text answer plus optional table/CSV/PDF (reuse `pdfGenerator`), and a "show in app" link that opens the filtered page.
- **Frontend**: chat drawer available on every page (and on `/today`), voice input on mobile (Web Speech API), history per user.
- Guardrails: tools only (no free SQL), per-day token budget, all calls logged (`assistant_logs`), PII minimised in prompts (names only when asked).
- **Verify:** "unassigned tours this weekend" returns the same list as the report; "how much did we earn from Uffizi tours in August" matches P&L; "how many radios tomorrow at 9" matches the planner.

---

## Phase 8 — Structure (ongoing, after the above)
Versioned migrations + `schema_migrations` (§3) · front controller with route table and uniform JSON envelope · split `Payments.jsx` / `Tours.jsx` / `Tickets.jsx` into feature folders · money as integer cents · flatten the repo (drop the nested folder, `public_html/api` → `api/`) · single README + `docs/history/`.

---

## Claude Code operating notes
- `CLAUDE.md` must be rewritten: remove credentials (0.3), put the **step loop** and **smoke test** at the top, link to this plan and the analysis, and cut the changelog-style sections into `docs/CHANGELOG.md` (the file is 58 KB and mostly history).
- Skills added to the repo under `.claude/skills/`: `implement-step` (the loop above), `staging-deploy` (deploy + smoke test against a target), `bug-triage` (reproduce on staging with SQL/curl before touching code). Use them by name: `/implement-step 3.5`.
- Never run `deploy.sh --target production` from a step branch; only from `master` after staging passed.
